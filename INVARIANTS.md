# db/ invariant registry

The `LDB-*` invariants for the layout-db service (`design/layout-db/`,
phase 1: `07-implementation-phase1.md`). This registry lives with the code
(it moves with `db/` when the service splits into its own repo, per
`00-plan.md` §7) rather than in the site's `design/INVARIANTS.md`, which
carries one pointer entry instead.

Every row here needs a test tagged `[LDB-*]` in its `test()`/`it()` title
under `db/tests/`; `tests/tools/invariants.test.ts` (LDB-T1) fails the
build when an id has no tagged test, or a tag names an id not in this
table. Each slice's PR adds its own rows (07 §6); this file starts with
the S1 rows only. LDB-T1's own rule leaves no room for a "retired, no
test" row (every id present still needs one), so a fully retired id is
simply deleted from the table below rather than left as a placeholder --
this paragraph is the retirement log instead.

**Retired by design/layout-db/21-formats.md (2026-09-11, F1 -- D5 deletes
the cmini export/`toCmini`/every alias; D10 deletes spark/1's free-form
`x`; D12 deletes the legacy carry-forward, the record migration and the
M1 strip route):**
- **LDB-F10** (`x` round-trip identity) -- `x` itself is gone; see
  LDB-F23 (MF-9) for its replacement.
- **LDB-F20** (the alias table and the wire-format relabel rule) --
  there are no aliases left to have a rule about.
- **LDB-F21** (legacy-stored rows read as spark via `storedAsSpark`) --
  no row is ever stored under a legacy format any more.
- **LDB-I2a** ("follows upstream" via the legacy historical-event rule)
  -- folded into LDB-I14's own restated wording (a plain field read now).
- **LDB-I12** (a magic-only PATCH doesn't count as "the latest write"
  for the legacy follow rule) -- the legacy rule it modified is gone;
  decision 6's underlying claim ("magic edits fork like any other write")
  was already just LDB-I14's ordinary rule with no special case for
  `kind`.
- **LDB-P12** (the record migration) -- deleted outright; after the D8
  wipe every stored row is already spark/1 from the moment it's written.

**Retired by LEDGER.md L4 (2026-09-12, design/layout-db/review/PROPOSAL.md
§2.1 -- "no consumer, ~2.5k src + ~4k test lines of surface for a spark/2
and third parties that don't exist"; nothing the bot or akl.gg called
lived here):**
- **LDB-H1** (webhook delivery at-least-once/in-order) -- the whole
  webhook subsystem (`core/webhooks.ts`, `routes/webhooks.ts`, the
  `webhooks` table) is deleted; the bot's keep-warm moves to the
  long-poll `GET /v1/changes?wait=` (LDB-CH1..CH5, below) once spark
  adopts it (Wave 2 B3).
- **LDB-H2** (the SSE stream reproduces the feed) -- `routes/stream.ts`
  and the `STREAM_*` vars are deleted; the long-poll replaces its
  keep-warm role.
- **LDB-H4** (a webhook secret never leaves the `webhooks` table) -- no
  `webhooks` table, no secret, nothing left to leak.
- **LDB-H5** (a drain's D1-write shape) -- no drain.
- **LDB-H6** (the claim-before-send lease) -- no lease, no drain.
- **LDB-P3** (a webhook-only follower matches a feed-only follower) --
  no webhook-only follower exists to build; the feed is still the one
  source of truth (LDB-P6 and this file's own `tests/events/feed.test.ts`
  "feed" describe cover what's left).
- **LDB-D5** (the Fly restore drill's report shape) -- `db/drill/` and
  `POST /v1/admin/drill` are deleted; `tests/rehost.test.ts` (LDB-G1) is
  the one remaining rehost proof, run in-CI daily.
- **LDB-D6** (per-major dump files, `latest.<name>-<N>.json`) -- no
  consumer was ever wired to fetch them; deleted along with
  `buildLatestMajorFiles`. The main dump (`latest.json`,
  `dump-YYYY-MM-DD.json.gz`, the monthly copy) is unchanged.
- **LDB-C3** (`[env.preview]` mirrors the top level) -- there is one
  layoutdb, production; the preview environment and its wrangler block
  are deleted (H13).

| id | invariant | enforced by |
|---|---|---|
| LDB-A1 | No write is accepted without a resolved actor; every non-GET route answers 401 to an anonymous request (enumerated from the router) | `tests/auth/routes.test.ts` |
| LDB-A2 | The Discord cache serves a success ≤ 5 min and a 401 ≤ 60 s; 5xx/429/network are never cached; the token is never stored. **Amended (20-spark.md S3s):** `resolveBearer` calls `GET /oauth2/@me` (not `/users/@me`); the cache also keys the token's `app_id` (`application.id`) beside `user_id`/`name`; a cached SUCCESS row with `app_id IS NULL` (written before 0005) is treated as a miss and re-verified; a 200 with no `user` key (the grant lacks the `identify` scope) is cached as a FAILURE exactly like a real 401, `token_invalid`, with a message naming the missing scope | `tests/auth/discord.test.ts`, `tests/api/me.test.ts` |
| LDB-A4 | Client-lane signatures (02 §3.2, 10 C1): every vector in `tests/vectors/client-signing.json` is accepted; each single-field mutation (method, path, query, timestamp ±301s, replayed nonce, body, actor, signature, key) is refused with the named 401; every malformed/missing header collapses to `bad_signature`; and every route in the sweep pins every client-lane 401 code | `tests/auth/client.test.ts`, `tests/tools/vectors.test.ts`, `tests/api/conformance.test.ts` |
| LDB-A5 | Every accepted write's event carries `via` (the actor's own lane -- 10 C1 widens this from the literal `"discord"` to `client:<id>` on the client lane); every admin action is an event with `admin = 1`; the admin client routes (register/revoke/list) follow the same admin-only, event-logged pattern, never leaking the pubkey into `detail`; the manual cron triggers (`POST /v1/admin/import/tick`, `POST /v1/admin/diff/tick`, `POST /v1/admin/nightly/tick`, X4 follow-up + follow-up 3) call the exact same `tick()`/`diffTick()`/`runNightly()` their own cron dispatches, refuse `409 import_paused` while paused (import only), and are event-logged the same way. **Restated by 21-formats.md D12**: the record migration's `POST /v1/admin/migrate/tick` and the M1 strip route's `POST /v1/admin/import/strip-cmini-magic` (both one-time manual triggers this row used to name) are deleted | `tests/api/admin.test.ts`, `tests/api/write.test.ts`, `tests/api/clients.test.ts` |
| LDB-A6 | The admins table never has fewer than two active rows after bootstrap (the count check and the delete are one statement, so two concurrent removes at three rows can't both commit) | `tests/api/admin.test.ts` |
| LDB-A7 | Owner changes only via `transfer`; a write body naming `owner` (or any field outside the verb's schema) is refused | `tests/api/bodies.test.ts`, `tests/api/write.test.ts`, `tests/api/transfer.test.ts` |
| LDB-A8 | A client-lane nonce is accepted once: the `nonces` PK insert is the replay check, run only after the signature verifies; rows are pruned after 900s | `tests/auth/client.test.ts` |
| LDB-A9 | A revoked client's requests are refused from the revocation onward -- `clients.status` is read on every request, never cached | `tests/auth/client.test.ts`, `tests/api/clients.test.ts` |
| LDB-C1 | `db.yml`'s shape (test job on PR/push under `db/**`; deploy needs test, main+push only, migrations before deploy; daily job runs rehost + diff; actions pinned) is asserted from the parsed YAML | `tests/tools/ciwiring.test.ts` |
| LDB-C2 | `canonical()` is key-order-invariant and lossless | `tests/core/canonical.test.ts` |
| LDB-C4 | The diff cron (hour=4, minute=0 slot of the one `*/5 * * * *` trigger) reads our side from D1 directly and never fetches its own origin; it is one invocation. **Narrowed by LEDGER.md L4** (the shrunk diff, LDB-P5): "our side" is now a `layoutCount()` read plus one `ORDER BY RANDOM() LIMIT n` sample query (`d1Ours`, `import/difftick.ts`) instead of a full-corpus page walk -- a handful of D1 reads regardless of corpus size, never one per record | `tests/import/difftick.test.ts` |
| LDB-C5 | The single `*/5 * * * *` cron trigger dispatches by `event.scheduledTime` (UTC), never `event.cron`: every invocation runs the import tick; hour=3/minute=0 also prunes + writes the nightly dump (unconditionally); hour=4/minute=0 also runs the diff (unconditionally); a cron string other than `*/5 * * * *` still throws; one job throwing is caught and logged (`runJob`), never blocking the jobs queued after it in the same invocation. **Narrowed by LEDGER.md L4**: the webhook drain (previously also run every invocation) is deleted along with the webhook subsystem (LDB-H1, retired). **Amended (LDB-D8):** every OTHER tick also checks the diff, then the dump, for catch-up -- see LDB-D8 | `tests/import/tick.test.ts` (`[matrix]`, `[property]`, `[isolation]`) |
| LDB-C6 | `.github/workflows/db.yml`'s `daily` job fetches the live dump and uploads it as an `actions/upload-artifact` with 30-day retention (an off-account copy, C.3), before the rehost drill runs against the same fetch | `tests/tools/ciwiring.test.ts` |
| LDB-C7 | B4 (design/layout-db/review/audit-db.md B4, review/PROPOSAL.md §2.1, review/LEDGER.md L1): overlapping import ticks are impossible -- `import/cmini.ts`'s `tick()` takes `import_state['cmini.running'] = {at, id}` (a CAS INSERT, or a CAS UPDATE against the exact stale value once the held lock is >10 minutes old) before doing any real work, and releases it in a `finally` (so a thrown tick body still releases it). A tick that finds the lock held (and not expired) returns `{quiet: true, stats: {skipped_locked: true}}` and logs, touching nothing else; `POST /v1/admin/import/tick` turns that into `409 import_running` instead. A quiet tick (meta token unchanged) never touches the lock at all -- only real tick bodies need it | `tests/import/tick.test.ts` (`[LDB-C7]`: held/not-expired, expired reclaim, the exact-10-minute boundary, release-on-throw, quiet-never-touches), `tests/api/admin.test.ts` (409 held, 200 after expiry) |
| LDB-CH1 | LEDGER.md L4: `GET /v1/changes?wait=<seconds>`, honored, with nothing happening in the meantime, returns within `wait`+1s carrying the normal immediate `/v1/changes` page (`{next, items}`) -- never a different shape, never an error | `tests/api/changes-longpoll.test.ts` |
| LDB-CH2 | A held `wait=` returns as soon as an event lands past `since` -- well short of the full budget, on the very next ~1s head-check, with that event present in the returned page | `tests/api/changes-longpoll.test.ts` |
| LDB-CH3 | `wait=` is honored for EVERY request authenticated on the Ed25519 client lane (`auth/client.ts`'s `verifyClientRequest`) -- saltorbit 2026-09-13: "any registered client should get this by default"; the `feed:wait` extra cap is still accepted in `caps` but implied, never required; every other caller naming `wait` -- no auth headers, a Discord bearer, or a client-lane request that fails to verify -- gets the SAME immediate, unheld answer any plain `/v1/changes` call gets, plus a response header `X-Wait-Ignored: unauthorized`, never an error. `caps` (`clients` table, migrations/0003) is a comma-separated set: exactly one scope cap (`act-as-user` \| `act-as-owner-only`, unchanged semantics) plus any extra caps; `core/clients.ts`'s `registerClient` refuses a `caps` string naming zero or two scope caps, or an unrecognized extra cap, with `400 bad_request` naming `/caps` | `tests/api/changes-longpoll.test.ts`, `tests/api/clients.test.ts`, `tests/auth/client.test.ts` |
| LDB-CH4 | `wait` is clamped to `[0, MAX_WAIT_SECONDS]` (25s) -- a caller asking for more (or a non-finite/negative value reaching `clampWaitSeconds` directly) never holds the request longer than the cap | `tests/api/changes-longpoll.test.ts` |
| LDB-CH5 | The number of D1 reads (`headSeq`) during a held wait is at most `ceil(wait) + 1` -- one per ~1s check, never more, regardless of how long the hold runs | `tests/api/changes-longpoll.test.ts` |
| LDB-D1 | The nightly dump is complete (every table, the whole event log), its `latest.json` sha256 matches the object served, and the monthly copy is written on the 1st (and only the 1st); the manual tick (`POST /v1/admin/nightly/tick`, X4 follow-up 3) and the cron write byte-identical dumps for the same state. **Amended (20-spark.md S3a):** the dumped `layouts` rows carry `upstream_source`/`upstream_id`/`upstream_state`. **Amended (migrations/0007, LDB-R9):** the dump's `meta` is built by the same `readMetaCore` as `/v1/meta` and carries `authors_version`; a restore sets `authors_head` to the dumped `authors_version`/`authors_modified_at` as its last statement (after the `authors` deletes and inserts have moved it by trigger), and a dump from before 0007 restores as version 0. **Restated by 21-formats.md (several formats per layout, §2.1):** the dump additionally carries the whole `layout_formats` table (one row per (layout, lineage), payload included) and `layout_revs` gains `lineage`; `meta` itself is read strictly BEFORE any table page starts (never in the same `Promise.all`), which is LDB-D7 (MF-13)'s own claim | `tests/api/dump.test.ts`, `tests/rehost.test.ts`, `tests/api/admin.test.ts`, `tests/api/authors-validator.test.ts` |
| LDB-D7 | MF-13 (21-formats.md §2.1/§4, restates LDB-B10's floor claim with two tables): every table in a dump is at or past the dump's `meta.seq` -- `buildDump` reads `meta` FIRST, then every table page in one `Promise.all` (never the reverse, never concurrent with `meta`'s own read), so a write landing between the two reads shows up in the tables already (ahead of the reported floor, never behind it); booting from the dump and draining `/v1/changes` from `meta.seq` therefore equals the live state | `tests/api/mf13-dump-floor.test.ts` (a `readHead()` spy forces a write into exactly that window, then folds the dump's own `events`/`layout_revs` and checks the result against both the dump's own `records`/`layout_formats` rows and live state) |
| LDB-D8 | Dump/diff catch-up (audit-db.md §H item 3): `scheduled()` (`src/index.ts`) runs the nightly dump/diff on ANY tick where `import_state`'s own record (`dump.last_at` / `cmini.last_diff`) is absent or its `at` is more than 24h before the tick's own `event.scheduledTime` -- `dumpDue`/`diffDue` (`dump/write.ts`, `import/difftick.ts`) are the pure decision, `writeDump`'s own state write happens LAST (after every R2 object is written) so a failed attempt is retried, never marked done. Under normal (non-manual) operation this never runs twice within 24h: a successful hour=3/hour=4 run always leaves the record fresh, so the very next tick's catch-up check is false. The diff runs BEFORE the dump on every invocation so a tick where both catch up leaves the dump's own snapshot including the diff's fresh state, not one write behind it. `dump.last_at` is excluded from the dump's OWN `import_state` snapshot (it would otherwise make a dump depend on itself) -- a restored/rehosted database therefore starts eligible for an immediate catch-up dump | `tests/import/tick.test.ts` (`[LDB-D8]` describe: `[property]`, non-preferred-hour catch-up, never-twice-within-24h, diff-before-dump ordering) |
| LDB-D9 | `clients` (pubkeys, caps, status -- no secret column, unlike the now-deleted `webhooks` table, LEDGER.md L4) is dumped and restored, round-tripping exactly; `auth_cache`/`nonces`/`ratelimit` stay deliberately excluded from both the dump and the restore (documented in README §Rehost) | `tests/rehost.test.ts` |
| LDB-E1 | Format edits are pure, identity on their own projection, and validity-preserving | `tests/formats/edits.test.ts` |
| LDB-F1 | Every stored payload validates against its format's frozen schema; a write that does not is refused with the failing path | `tests/formats/goldens.test.ts`, `tests/formats/mutations.test.ts` |
| LDB-F2 | `compileMagic()` (spark/1's compile step, was `lower()`) is deterministic across versions | `tests/formats/goldens.test.ts` (`.lowered.json` goldens) |
| LDB-F3 | Intent is never lowered on store | `tests/formats/intent.test.ts` |
| LDB-F4 | Idiom rows that share `inputs` resolve exactly as akl.gg's compile does (phase order scaffold < chiral < explicit rules < adaptive swaps, last wins; amended 2026-09-11, saltorbit: "should match what we already have with aklgg"); a collision a raw `rules[]` row is part of is refused with both sources named. Parity with `magicRulesFlatCompile` is the bot's LDB-B78 property test (bot/tests/magic/spark-parity.test.ts, bot's own registry) | `tests/formats/collisions.test.ts`, `tests/formats/mutations.test.ts` |
| LDB-F5 | **Restated by 21-formats.md D5**: the cmini round trip half (`cmini/1 → akl/1 → cmini/1`) is deleted along with `toCmini` -- see LDB-F23 (MF-9) for its test-local replacement. What remains: `mana2/1 → akl/1 → mana2/1` is identity (modulo row whitespace + unused fingermap padding) for every vendored fixture EXCEPT a widened loss list (23-geometry.md, restated by 24-spark-wire-review.md): a mana2 `space` token becomes a spark free position (spark refuses a space `char`, finding 11); a genuinely column-staggered mana2 board (`nstd-repeat`/`whirl`/`004-whirl`) loses its real per-column amounts once it reaches spark's `colstag` (§4.1: "colstag does not get to set stagger", word-only); a row-staggered board whose amounts match neither `ansi` nor `iso` nor all-zero (`nastic`) is written as `ansi`, losing its own amounts too; `mirrorLeftRowStagger`/`splitAngle`/`magicKeys`/`layers` (D10 removed spark/1's `x`, so these still don't round-trip). `akl/1 → mana2/1 → akl/1` is identity off the thumb row (LT/RT keep their LABEL now, never re-anchored by column, LDB-F29) for every akl/1 fixture, plus the SAME magic-idiom-structure/hatch losses, plus (23-geometry.md's duplicate-characters follow-up) a duplicate character's later occurrence(s) lowering to a `skip` cell (LDB-F33) | `tests/formats/mana2.test.ts` |
| LDB-F6 | Merged format majors are immutable. **Suspended by 21-formats.md D11** until layoutdb's first outside adopter: spark/1 is edited in place through this slice (D5, D10, D12) rather than frozen -- `frozen.test.ts` reads an explicit frozen list, empty today, instead of comparing against `origin/main` | `tests/formats/frozen.test.ts` |
| LDB-F7 | Every format has ≥ 1 fixture and a frozen golden per declared translation. **Narrowed by 21-formats.md D5**: the cmini adapter is import-only now (no more `from`/`toCmini`), so there is no `.cmini-1.json` golden for any format any more; `goldens.test.ts` loops over `validated-shapes.ts`'s shared shape list (every registered format plus the adapter), not `listFormats()` directly, so the adapter's own goldens (`validate`/`rows()`/`to["spark/1"]`=`fromCmini`/`to["mana2/1"]`) are covered generically again; the one direction no registered `to`/`from` map reaches -- mana2/1 fixtures' `toSpark` (mana2/1's registry `to`/`from` are `{}`) -- is covered by a dedicated block instead | `tests/formats/goldens.test.ts`, `tests/formats/validated-shapes.ts`, `tests/formats/mana2.test.ts` (X2: 74 vendored mana2/1 fixtures + `d5.jsonc`'s documented exclusion; its own "goldens (LDB-F7)" block for `.spark-1.json`) |
| LDB-F8 | `liftRules(compileMagic(m)) == (m, [])` for every valid idiom set; `compileMagic(lift(rows)) ≡ rows` for every typed row set; leftovers are exactly the rows that fail their tag's invariant | `tests/formats/lift.test.ts` |
| LDB-F9 | A held record keeps its layout-level fields (name/owner/`layout_rev`) and the format's own rev, and reads as its own native format | `tests/api/held.test.ts` |
| LDB-F11 | Every live upstream detail (snapshot) validates as `cmini/1` and `hasMagic` matches upstream's `has_magic` | `tests/formats/cmini-envelope.test.ts` |
| LDB-F12 | The DB's `akl/1 → mana2/1` grid and thumb strings equal the site's real `bridgecore.ConvertLayout` wasm output for every `cmini/1` fixture (modulo trailing `skip`s and the fingermap digit under a `skip` cell) | `tests/formats/mana2-convert-parity.test.ts` against the frozen `tests/fixtures/mana2-convert/*.json` snapshot (`scripts/check-convert-parity.mjs` records/re-verifies it from the live wasm) |
| LDB-F13 | The set of vendored `mana2/1` layouts held for `akl/1` is enumerated with reasons; a new held vendored file fails until listed | `tests/formats/mana2.test.ts` |
| LDB-F14 | `spark/1`'s `compileMagic()` emits the site's word-start row: a magic key whose `default` is a literal character also gets `{inputs: ' '+key, output: ' '+default}` (web/src/core/rules.ts's `magicRulesFlatCompile`, I-153, is the source of truth for the condition -- `repeat_previous` gets none, `except` does NOT suppress it since the site has no such list, an explicit `magic_keys[].rules[]` entry for `after: ' '` replaces it same as any board char); the row is absorbed back into the idiom by `liftRules` (LDB-F8) and participates in collision detection (LDB-F4) like any other scaffold row | `tests/formats/magic-word-start.test.ts`, `tests/formats/lift.test.ts` (LDB-F8's own property test, which generates and round-trips literal defaults) |
| LDB-F15 | `spark/1`'s `compileMagic()` matches the site's compile on two more scaffold rules (`web/src/core/rules.ts`/`magicScaffold.ts` are the source of truth): (1) a magic key's board-char scaffold (`repeat_previous` or a literal default) excludes EVERY magic key's own char AND every chiral key's own char, one GLOBAL set shared across every key's scaffold, not just that key's own char; (2) a chiral key's scaffold enumerates EVERY layout key with a resolvable hand, the chiral key's OWN char included -- always same-hand, so it takes `same` (never `opposite`), producing a self row. A frozen row only the old (too-narrow) scaffold used to reproduce (a real historical row like auditor's `b*->bb`, 'b' its own magic key) still round-trips: `liftRules` promotes it into an EXPLICIT `magic_keys[].rules[]` override, which relowers tagged `magic` instead of `repeat`/`default:<c>` -- same (inputs, output), intentionally relabeled (LDB-F8's frozen-fixture and property tests tolerate exactly this relabeling, nothing else) | `tests/formats/magic-special-scaffold.test.ts`, `tests/formats/lift.test.ts` (`003-auditor` frozen-fixture case + LDB-F8's property test) |
| LDB-F16 | **Restated by 21-formats.md (several stored lineages per layout, §2.2/§4):** a write stores the lineage `body.format` names, never another. Every accepted write resolves `body.format` through the registry (a plain registered-id lookup) and stores the resolved module's OWN id under ITS lineage's `layout_formats` row; every other lineage the layout already has is untouched by that write (MF-1 owns the byte-level independence claim; this row owns "the write went to the right row at all"). A format whose `role` is `"output"` (`mana2/1`) is `400 format_not_writable`; anything unregistered is `400 unknown_format` listing registered ids only. A write that carries an existing layout's fields forward with no payload of its own (delete, restore, transfer, PATCH's `{name}` branch) is layout-scope only and touches no `layout_formats` row at all -- there is no single `record.format`/`record.payload` left to carry forward verbatim, since a layout may have several | `tests/api/format-write-matrix.test.ts` (registry matrix: verb × format), `tests/api/write.test.ts`, `tests/api/restore.test.ts`, `tests/import/cases.test.ts` (import cases 1/4/8) |
| LDB-F17 | Spark stays lowerable: every valid `spark/1` payload translates to `mana2/1` without holding -- `spark1.to["mana2/1"]` never returns `{held: true, ...}` for a payload `validate()` accepts (`design/layout-db/20-spark.md` S1; mana2 is how every stats path gets numbers, plan §2.5's own words). **Moved to its own test file 2026-09-11** (F1 review: retiring this because its old home, `registry-aliases.test.ts`, was deleted along with the aliases it also covered would have inverted the covenant -- the claim's subject, spark/1's lowerability, was never touched by D5/D10/D12 and still matters exactly as much) | `tests/formats/lowerable.test.ts` |
| LDB-F18 | 20-spark.md S5 (19-upcast.md round 2 §8's F16, renumbered): chain contract. Every registered `<L>/<N>`, `N > 1`, has `<L>/<N-1>` registered and exports `up`/`down`/`edits`; `to`/`from` never name the module's OWN lineage (any major); over a fixture at `N`, `down(p)` is held or `up(down(p)) === p` (down is honest); over a fixture at `N-1`, `up(q)` never holds, validates at `N`, and `down(up(q)) === q` (up is injective). With only `spark/1`/`mana2/1` registered (each a one-major lineage), this is exercised entirely through a test-only stub lineage (`tests/formats/stub-lineage.ts`'s `t/1 -> t/2 -> t/3`), including deliberately-broken variants that prove `chainViolations` catches each missing piece (missing `up`/`down`/`edits`, a lineage gap, a self-lineage `to`/`from` edge) | `tests/formats/chain.test.ts` |
| LDB-F19 | 20-spark.md S5 (19-upcast.md round 2 §8's F17, renumbered): path composition. `translate()`/`path()`/`walk()` walk legacy-normalize -> chain -> pinned cross edge -> chain; the result is held iff any step is held; a `held` body's `see` names the record's own NATIVE format; `GET /v1/formats`' `can_translate_to` equals the set of formats `path()`/`hasEdge()` structurally reaches (same lineage always; a different lineage with a registered cross edge at ANY major, not just the format's own). Exercised through the stub lineage: `t/1 -> t/3` (up, 2 steps), `t/3 -> t/1` (down, 2 steps, held iff `b`/`c`), and a cross edge pinned at `t/2` -- a NON-LATEST major of `t` (whose latest is `t/3`) -- reaching a second stub lineage `u/1` | `tests/formats/path.test.ts` |
| LDB-F22 | spark/1's magic validation accepts what akl.gg's gate (`functions/_lib/rules.mjs` `validateRuleSet`) accepts and refuses what it refuses, keeping only `except[]` and raw `rules[]` as additions: named keys need not be on the layout, `notes`/`updated` are strings, a `null` chiral `same`/`opposite` reads as absent (saltorbit 2026-09-11: "layoutdb validation right now should match aklgg validation"; issue #321 would add the on-layout check to both together) | `tests/formats/magic-aklgg-validation.test.ts` (the compile side: the bot's LDB-B78 parity test) |
| LDB-F23 | MF-9 (design/layout-db/21-formats.md §4, F1's replacement for the deleted `toCmini` round trip; **restated by 23-geometry.md/24-spark-wire-review.md**): `fromCmini` is exact where spark has a place, MODULO §4.6's relabel (LDB-F28/F31). For every cmini layout in `upstream-100`, the multiset of `(char, row, col, finger)` -- over `keys`' entries now, `char` absent for what used to be a separate `free` array, with `TB`/mislabelled thumbs relabelled by `col < 5 => LT else RT` -- is preserved exactly; the cmini board word maps to spark's plain `board` word by the fixed table (`stagger`/`angle` -> `ansi`, `ortho`/`mini` -> `ortho`, no angle-family bump, LDB-F31); the fields dropped are exactly `tag`, `blame`, `combos` -- `link` is carried to the record's own envelope by the importer (moderated as an approved link, see LDB-MD3/MD5), never into the payload -- nothing else appears or disappears (in particular, no `x`: D10 deleted it). The ONE known real exception in `upstream-100`: `test12222` has a thumb-labelled key physically on a finger row (0-2, LDB-F27), so `fromCmini`'s multiset-exact output still fails spark's own stricter `validate()` by design (`tests/formats/cmini-envelope.test.ts`'s `[LDB-I13]` case) | `tests/formats/mf9-fromcmini.test.ts` |
| LDB-F24 | `design/layout-db/22-spark-spec.md`'s §11 "Worked examples" are three real payloads extracted straight from the markdown, never retyped: each parses as JSON and `validate()`s as a valid `spark/1` payload, and the first matches what the doc's own prose says about it (the `@` key's position, the board word, the one lowered magic rule, the duplicate `e`, the free position) -- so the doc can never silently drift from what the code accepts. **Restated 2026-09-13** (the 24-spark-wire-review.md skeleton rewrite): was one example under the old §7, now three under §11 (plain+duplicate+magic, iso+free+thumb, colstag+six-thumbs), each independently validated | `tests/formats/spec-example.test.ts` |
| LDB-F25 | MF-7 (21-formats.md §2.5/§4): derived formats are never stored -- a `?format=mana2/1` read changes no row anywhere (`events`, `layout_formats`, `layout_revs` counts and the layout's own fields are byte-identical before and after) and its response names `derived_from`; a read of a stored format (`?format=spark/1`) never carries `derived_from` at all, on the detail route, the list route, and `full=1` | `tests/api/mf7-derived-formats.test.ts` |
| LDB-F26 | MF-10 (21-formats.md §2.5/§4): unambiguous derivation -- each `role: "output"` format is reachable from exactly one `role: "stored"` lineage (`reachingLineages()`/`outputSourceLineage()` over the real registry); a test-only pair of stored lineages both claiming an edge to the same output format is caught (`outputSourceLineage` answers `undefined`, never an arbitrary pick). The read-time half (a layout missing its one reaching lineage answers `404 format_absent`, never a silent pick among several) is exercised live in `tests/api/held.test.ts` | `tests/formats/mf10-derivation.test.ts`, `tests/api/held.test.ts` |
| LDB-F27 | design/layout-db/23-geometry.md §4.4 (restated by 24-spark-wire-review.md finding 10, round 2's resolution item 10 -- the ORIGINAL 4th rule, "angle/nokwts/meteorite needs board: ansi", is NOT enforced by `validate()` at all; `classifyFingering` is a derived, read-time label, never a write-time refusal): `validate()` accepts a payload iff `board` is one of `ansi`/`iso`/`ortho`/`colstag` (the schema's own `enum`), on `iso` row 2 is at most one column wider than rows 0-1, and no `LT`/`RT` finger sits on a finger row (0-2, rows >= 3 unrestricted -- the 22 real number-row layouts). Each of the two real (non-schema) rules has a refusing fixture | `tests/formats/mutations.test.ts` (dedicated cases: the board enum, the iso-width rule, the thumb-row rule) |
| LDB-F28 | `TB` never reaches storage: the schema refuses it (dropped from the finger enum entirely); `fromCmini` maps every `TB`/mislabelled thumb by the import rule (`col < 5 => LT else RT`), one `import_relabel` info event per id whose relabel set is non-empty, naming every relabelled key/free position | `tests/formats/mf9-fromcmini.test.ts` (over `upstream-100`), `tests/formats/mana2.test.ts` ("adept: TB never reaches the stored spark fixture") |
| LDB-F29 | For every valid payload, `fromSpark` (`db/formats/mana2/1/translate.ts`) puts a key in the left thumb string iff its finger is `LT`, the right iff `RT` -- the label, never the column (`col < 4.5` re-anchoring is gone) | `tests/formats/mana2.test.ts` (`adjustForMana2RoundTrip`'s own re-derivation of the exact mechanism, asserted per fixture) |
| LDB-F30 | Physical coordinates (`(kind, row, col) → (x, y)`, `coords()`), the hand split (`handSplit(keys) -> number`, the board split; `handSplitRows(keys) -> number[]`, per finger row) and `classifyFingering(keys)` are each ONE function, exported by `db/formats/spark/1/geometry.ts` -- the site's drawer, the bot's grid/image and the mana2 lowering call them (numerically identical ports where a literal shared import isn't possible across the `db/`/`web/` boundary, LDB-G5); the lowering's `rowOrColumnStagger`/thumb-string split reproduce them on every fixture | `tests/formats/geometry.test.ts` (unit-level, incl. a fixture where rows disagree on their local split), `tests/formats/mana2.test.ts` (parity over fixtures, the round-trip matrix); archlint: no second definition inside `db/` |
| LDB-F31 | `fromCmini` maps the board word by §4.6's fixed table (`stagger`/`angle` -> `ansi`, `ortho`/`mini` -> `ortho`) and changes nothing else -- faithful to the word alone, no angle-family bump (24-spark-wire-review.md finding 10 dropped it; extends LDB-F23) | `tests/formats/mf9-fromcmini.test.ts` |
| LDB-F32 | `classifyFingering` returns `angle`/`nokwts`/`meteorite`/`standard` exactly when a layout's rows 0-2 equal the reference over a FIXED 3x10 grid (cols 0-4 the left-hand reference, cols 5-9 always `RI RI RM RR RP` -- never derived from `handSplit`/`handSplitRows`, no per-row split concept at all), else `custom`; a free (char-less) entry never participates, matching `scripts/build_web.py`'s `classify_fingermap` (`ll.keys.values()` only, `enumerate(right, start=5)`) byte-for-byte. **Corrected mid-slice**: an earlier port anchored the right-hand reference at each row's OWN `handSplitRows` column instead (a plausible-looking generalization with no basis in the real Python source) -- caught by actually running the parity check below, which read 82.77% before even the label-spelling difference ("angle mod" vs "angle") was normalized, 95.63% after, isolating this exact bug as the entire remaining gap; fixed to the real fixed-grid algorithm, then RE-RUN at 100.00% (4139/4139 compared, 52 keyless layouts skipped) | `tests/formats/geometry.test.ts` (shared-table unit tests + the iso-extra-column/missing-cell/free-entry/thumb-exclusion cases, plus a dedicated regression for a real catalog layout -- `alpha` -- whose left-hand extent falls short of col 4 on one row); parity over the full 4,191-layout catalog against `web/data/layouts.json`'s own `fingermap` field is reported as a one-off script result in the slice's own writeup (it reads `web/data/*.json`, outside this package, so it isn't a committed `db/` test) |
| LDB-F33 | design/layout-db/23-geometry.md's duplicate-characters follow-up (24-spark-wire-review.md, round 2's resolution item 4): every character a magic construct NAMES (a magic/chiral key's own `key`, an adaptive swap's `trigger` or either `swap[]` member, a magic key rule's `after`, any `except[]` entry) has AT MOST one entry among `keys`' char-bearing entries (zero is fine -- no scaffold row, no error); refused `400 magic_needs_unique_key` otherwise. A plain duplicate letter no magic construct names is unrestricted UNLESS its own entries span both hands, in which case any `chiral_keys[]` whose scaffold would enumerate it is refused the same way unless that key excepts the char or a raw `rules[]` row already covers the exact `(char, chiral key)` pair. Lowering to `mana2/1` (which refuses duplicate letters) picks the FIRST entry for a repeated char IN LIST ORDER (never resorted by row/col) as the analysed key; every later occurrence becomes a `skip` cell -- a documented, permanent loss (restates LDB-F5) | `tests/formats/mutations.test.ts` (magic_needs_unique_key, refused vs. unrestricted), `tests/formats/mana2.test.ts` (the duplicate-char round-trip case) |
| LDB-G1 | Restorable from a public dump + the public repo | `tests/rehost.test.ts` (daily job: against the real deployed dump) |
| LDB-G2 | No admin id is a constant in code (the migration seed is data) | `tests/tools/noconst.test.ts` |
| LDB-G3 | `.github/CODEOWNERS` is generated from every format's `OWNERS` file (one line per format major, no format missing, no stale handle) | `tests/tools/codeowners.test.ts` |
| LDB-G4 | Every binding/var the Worker reads is in the runbook table | `tests/tools/runbook.test.ts` |
| LDB-G5 | Nothing imports across the `db/` boundary in either direction | `tests/tools/boundary.test.ts` |
| LDB-G6 | The split repos (`git filter-repo`'d clones of `db/` and `bot/`) test green with no edits: `repoLayout()` resolves the right prefix and workflow file inside either shape | `tests/tools/ciwiring.test.ts` ([LDB-G6] case), `.github/workflows/db.yml`'s `split-dry-run` job (`scripts/split/split-db.sh --dry-run`) |
| LDB-G7 | `@akl/layout-formats` packs every format's built entry and schema.json and imports cleanly from a clean install; its `exports` map is derived from the registry, so a fourth format cannot be forgotten | `tests/tools/package.test.ts` |
| LDB-G8 | `db/INTEGRATION.md`'s error-code appendix is generated from `src/core/errors.ts`'s own `ApiError` factories (`db/scripts/gen-error-table.mjs`); the integration guide's error table cannot drift | `tests/tools/error-table.test.ts` |
| LDB-G9 | The docs hub is generated fresh on every build (never checked in -- design/layout-db/*.md is edited too often for a committed copy to stay current) and cannot drift from its own sources: every `design/layout-db/*.md` and `db/docs/*.md` is in the hub, and nothing else is; every page's navigation lists every doc exactly once, in reader order; every internal link and every "Markdown for agents" link resolves; `all.md` equals every doc's markdown copy concatenated in nav order; each page's `.md` copy equals the builder's own render of its source; two builds are byte-identical. `npm run build`'s assemble step produces `dist/layoutdb/index.html`. Skips once `db/` splits out (design/layout-db/ does not exist there); every CI job whose sparse checkout carries `design/layout-db` also carries `db/docs` and `design/federation`, and the builder fails on a missing source directory instead of skipping it | `tests/tools/docs-site.test.ts`, `web/tests/tools/assembledist.test.mjs` |
| LDB-G10 | `db/docs/adoption.md` (20-spark.md S8) covers the API exactly and cannot drift: the set of public routes enumerated from the live router (`app.routes`, `src/index.ts`) equals the guide's endpoint table (method + path, exact); every error code the guide's error table lists is one `src/core/errors.ts`'s factories can produce, and every factory code is in the guide; the guide's format-author checklist names every member a stored format needs and every member a major > 1 additionally needs, both enumerated from `db/tests/formats/stub-lineage.ts`'s own LDB-F18 fixtures (a chain-clean major-1 stub, and the three "missing piece" mutants `chainViolations` is proven to flag) rather than hard-coded | `tests/tools/docs-site.test.ts` |
| LDB-G11 | MF-4 (21-formats.md §2.4/§4): explicit format, no default -- every route that returns or changes a payload answers `400 format_required` without one; a generated matrix over the three `?format=`-bearing GET routes (list, detail, `/rev/:n`) x five scenarios (missing, stored and present, stored but absent on this layout, output/derivable, unregistered) pins each route's exact status/code, including `/rev/:n`'s own one narrower rule: an output format is refused outright (`400 bad_request`, never derived -- a revision number has no meaning for a format that isn't stored at all). A lineage this layout never stored is `404 format_absent`, the SAME vocabulary the detail route uses for exactly this fact (coordinator review, 2026-09-11: this used to fall through to a plain `not_found`); only a genuinely out-of-range rev NUMBER on a lineage the layout DOES have is `not_found` | `tests/api/format-required-matrix.test.ts`, `tests/api/format-read-labels.test.ts`, `tests/api/patch.test.ts` |
| LDB-G12 | Every fenced block in `db/docs/adoption.md` marked with the ` ```json spark-payload ` info string is a literal `spark/1` payload: it parses as JSON and `validate()`s as `{ok: true}` against the real, live `spark/1` module -- the doc's own worked payload examples can never silently drift from what the code accepts | `tests/tools/adoption-examples.test.ts` |
| LDB-G13 | `db/docs/adoption.md` §0's Quickstart is real and runnable: every literal `/v1/...` path in its fenced examples (interpolations normalized first) matches a route the live Hono router (`app.routes`) actually registers; the Quickstart's Node client-lane signing snippet (item 4), executed verbatim against a seeded test client and a real layout via `SELF.fetch`, gets a 2xx; every JSON payload embedded in the Quickstart's fences validates against `spark/1`'s real `validate()` | `tests/api/adoption-quickstart.test.ts` |
| LDB-H3 | The changelog page shows exactly the feed's events for its parameters, with every interpolated value HTML-escaped | `tests/api/changelog.test.ts` |
| LDB-I1 | The import is idempotent: the same upstream state twice appends zero events | `tests/import/tick.test.ts` |
| LDB-I2 | The import never overwrites a record that does not follow upstream | `tests/import/cases.test.ts` |
| LDB-I3 | Tombstoning more than `max(5, 5%)` of live records in one tick stalls the import instead | `tests/import/plan.test.ts` |
| LDB-I4 | Every import event carries `via = import:cmini` and `actor = system:cmini-import` (likes: the liking user) | `tests/import/cases.test.ts` |
| LDB-I5 | Imported names are stored verbatim (case kept, `check_name` not applied) and are unique case-insensitively | `tests/import/cases.test.ts` |
| LDB-I6 | A list shorter than half the live record count stalls the whole tick | `tests/import/plan.test.ts` |
| LDB-I7 | A tick whose `/meta` token is unchanged makes no further request and writes nothing | `tests/import/tick.test.ts` |
| LDB-I8 | Every upstream request carries the UA; 404 is never retried; other failures are retried 3x | `tests/import/upstream.test.ts` |
| LDB-I9 | Upstream JSON is parsed only through `core/safejson.ts`: Go's `\u003c`/`\u003e`/`\u0026` escapes are rewritten to literals before `JSON.parse` (a reproduced V8 bug decodes escaped object keys non-deterministically on the ~5 MB `?full=1` body, in Node and in workerd), an escaped backslash is never touched, and the parse is checked against a second parse | `tests/core/safejson.test.ts`; `tests/import/upstream.test.ts` |
| LDB-I10 | M1 (design/layout-db/17-magic-ownership.md): an imported payload never carries `magic` -- dropped from upstream's detail before validation (`import/apply.ts`'s `payloadFromRaw`), for `applyNew` and `applyMapped` alike; `has_magic` still derives from the format's `hasMagic(payload)`, which for a freshly imported record is now always false. **Narrowed by 21-formats.md D12**: the one-time strip pass (`POST /v1/admin/import/strip-cmini-magic`, `import/strip.ts`) that used to extend this to records imported before M1 landed is deleted -- after the D8 wipe no such pre-M1 record exists to strip | `tests/import/cases.test.ts` |
| LDB-I11 | M1: an import write preserves the record's own `magic` -- `applyMapped`'s case-4 write carries the EXISTING record's `magic` into the new payload, since upstream's own is never present to begin with (LDB-I10). `import/apply.ts`'s `contentDiffers` compares `{name, owner, created_at, modified_at, payload}` with `magic` dropped on both sides -- upstream's detail run through `fromCmini`, the record's own payload directly (21-formats.md D12 deleted the legacy `storedAsSpark` normalization this used to need first) -- and the D12 mirror diff's `import/diff.ts` `compareRecords` drops `magic` the same way over its own spark projection; neither upstream adding/changing/removing magic nor a followed-or-not record's own magic is ever mistaken for a content difference | `tests/import/cases.test.ts`, `tests/import/diff-unit.test.ts` |
| LDB-I13 | 20-spark.md S3b: the cmini adapter is exact for import purposes -- for a following record, `fromCmini(upstream detail)` equals the record's own spark payload minus `magic` (replaces the pre-spark cmini/1-level F5/F11 wording, which compared cmini/1 to cmini/1 directly); every LIVE upstream detail's `fromCmini` conversion validates as spark (`spark.validate`), not just as `cmini/1` -- a detail that's schema-valid per cmini/1 but fails spark's own (stricter: magic semantics, collisions) validate is reported as `invalidUpstream`, never thrown, by both `parseUpstreamRaw` (`import/diff.ts`, the D12 diff) and the live daily job over real upstream data. **Restated by 21-formats.md D10**: the one constructed negative example this row's tests used to carry (an oversized `blame` overflowing spark/1's `x` cap) is retired along with `x` itself -- no known input shape currently diverges (cmini's and spark's schemas now agree on every field `fromCmini` preserves), so the defensive branch stays real, exercised code with no live example, not a proven-impossible one | `tests/formats/cmini-envelope.test.ts` (every upstream-100 detail's `fromCmini` validates as spark), `tests/upstream-diff.test.ts` (daily, live) |
| LDB-I14 | Upstream write rule, one function (`core/upstream.ts`'s `nextUpstream(prior, kind, via)`): `null` stays `null` (a write with no known upstream link never acquires one); otherwise an import-system write (`via: "import:cmini"` -- create, update, revival, the rev-bumping `upstream_deleted` tombstone) sets/keeps `following`, and every user rev-bumping write (PUT, PATCH incl. `{magic}` per decision 6, rename, fingermap, transfer, delete, restore) forks (`state: "forked"`, same `source`/`id`); the importer never writes a `forked` record (every import call site is gated on `following` first, and every one guards its write with `expectRev`, LDB-P14). `prior` is `upstreamOf(db, rec)`. **Restated by 21-formats.md D12**: the legacy fallback (`legacyFollows`, `import_map`, `"migrated"` as a `kind`) is deleted entirely -- `upstreamOf` is now a plain read of the record's own `upstream` field, always (never null after a real import: `applyNew` always sets it at create time), so there is nothing left to fall back to | `tests/core/upstream.test.ts` (pure `nextUpstream` matrix: kind × prior {null, following, forked} × via), `tests/events/upstream.test.ts` (`upstreamOf` is exactly the field, an `import_map` row changes nothing) |
| LDB-I15 | The cmini import's name for an author id (`import/authors.ts`'s `planAuthorNames`) is a pure function of upstream's `/authors` CONTENT and the stored row -- never of the order upstream lists names in: a stored name that is still one of upstream's names for its id is kept; an id with no row, the client lane's placeholder, or a name upstream no longer lists gets `preferredName`, the code-point-greatest (= UTF-8 byte-greatest) of upstream's names -- on Go's byte-sorted wire order exactly the name the old last-write-wins applier ended on, so deploying it renames nothing a completed pass already settled. Idempotent: the same upstream authors applied twice leave the `authors` table, and so `/v1/meta.authors_modified_at`, unchanged the second time; a pass writes a row (moving its `last_seen_at`) only when that row's stored name really changes; D1's result equals the pure fold (`foldAuthorWrites`) | `tests/import/authors.test.ts` |
| LDB-I16 | Author-name convergence: under any sequence of import passes over the same upstream authors -- any listing orders, from any starting table -- each id's stored name changes at most once | `tests/import/authors.test.ts` |
| LDB-I17 | Author-name precedence (`authors.name_source`, migrations/0006): a name that arrived through the user lane (`auth/discord.ts`'s `resolveBearer`, `'user'`) is never overwritten by the import, including by a pass that read the row before the user lane wrote it (the import's insert is `ON CONFLICT DO NOTHING`, its rename a compare-and-set on the planned-from name and a non-`'user'` source); the user lane always sets its own name; the client lane sets `name = user_id` (`'client'`) only on first sight and never touches name or source afterwards, and the import may replace that placeholder, once; dump -> restore keeps `name_source`, and a dump from before 0006 restores as `'import'`; 0006's backfill marks placeholders `'client'` and names matching a successful auth-cache entry `'user'` | `tests/import/authors.test.ts` |
| LDB-I18 | MF-12 (21-formats.md §2.2/§4, restates LDB-I14/LDB-P11 for several formats per layout): follow scope -- `nextUpstream(prior, via, touches)` takes an explicit `touches` flag (true for a layout-scope write or a format-scope write on lineage `spark`; false for every other lineage) in place of the old kind-based switch. A user write forks a following layout iff `touches` is true; a write to a non-`spark` lineage never changes `upstream` at all (byte-identical before/after, any `via`); the importer never writes a forked layout (every touching import-via output is `following`, never `forked`) | `tests/core/upstream.test.ts` (pure matrix: `via` x prior x `touches`), `tests/events/fold.test.ts` (the shared write model: `upstream` bookkeeping built via the same `nextUpstream` matches the live row after every random op sequence) |
| LDB-I19 | B2 (design/layout-db/review/audit-db.md B2): an upstream rename (case 4), or a following tombstone's revival (LDB-I20) onto a name a LIVE local layout already holds, never wedges the import -- `import/apply.ts`'s `applyMapped` catches `commitWrite`'s `name_taken` for a following layout's layout-scope write, records an `import_conflict` info event on the AFFECTED (following) record naming `conflicts_with` (the holder's id), then retries the SAME write under `<name>~cmini` (`freeShadowName`, the same rule `applyNew`'s case 3 uses) -- the holder is never touched, and the tick proceeds and stores its meta token. **Sticky shadow (coordinator follow-up, 2026-09-12, migrations/0013): a shadow is assigned ONCE, never re-attempted (and re-collided, minting `~cmini2`, `~cmini3`, ...) on every later tick a standing conflict persists.** `import_map.upstream_name` records the LAST name UPSTREAM itself reported for this id (set at `insertImportMap` -- for a shadowed create, this is the CONTESTED name, never the shadow the layout actually lives under); `applyMapped`'s following path computes `nameChangedUpstream` against THIS recorded value, never against the layout's own (possibly shadowed) `name`, and only attempts a rename (risking a fresh collision) when upstream's OWN name has genuinely changed since; an unrelated owner/created_at/deleted-only write keeps the layout's CURRENT name (stable or shadowed) as its target, never re-litigating an already-lost rename. If the contested name later frees up, the shadow is never reclaimed automatically -- only upstream reporting a DIFFERENT name reopens the question (a shadow is stable once assigned; the owner can rename). `import/plan.ts`'s own "name changed" fetch trigger reads the same recorded value, so a standing collision stops being re-fetched every tick too | `tests/import/cases.test.ts` (`[LDB-I19]`: a rename collision, a revival colliding with a name since claimed locally, three ticks with the same colliding upstream name writing the shadow exactly once, and no auto-rename-back once the taken name frees up) |
| LDB-I20 | B3 (design/layout-db/review/audit-db.md B3): a following, deleted layout that upstream lists again (identical name/owner/created_at/payload, `deleted` the only thing that differs) is restored, not re-fetched forever -- `import/apply.ts`'s `applyMapped` folds `record.deleted` into its own `layoutDiffers` check (`layoutFieldsDiffer` itself never looks at `deleted`, on purpose: case 6/7's "is this new news" check for a NOT-following record must not fire on `deleted` alone), producing an `imported` LAYOUT-scope write that sets `deleted: false` even when every field `layoutFieldsDiffer` itself compares is unchanged | `tests/import/cases.test.ts` (`[LDB-I20]`: identical re-add revives; idempotent afterward; combined with LDB-I19's shadow rule when the freed name has since been claimed) |
| LDB-I21 | B5 (design/layout-db/review/audit-db.md B5): one id's apply throwing (a real bug, a D1 hiccup -- anything that ISN'T a `RevConflictError`, LDB-P14's own case) never aborts the rest of the tick. `import/cmini.ts`'s per-id loop (both the fetch loop and the delete loop) catches it, calls `import/apply.ts`'s `recordImportError` (an `import_error` info event on whatever local layout the upstream id maps to, or a silent no-op if it maps to none yet), and continues; `cmini.meta_token` is stored once every id in the tick has either succeeded or been recorded this way (never partial, never silently skipped) | `tests/import/tick.test.ts` (`[LDB-I21]`: one throwing id, the rest land, the `import_error` event is recorded, the token still stores and a following tick is quiet) |
| LDB-I22 | Hostile-upstream hardening (design/HARD-REQUIREMENTS.md's spirit, saltorbit 2026-09-13: "concerned about the cmini owner crashing out and deleting their db when this goes live"): the ROLLING 24h `upstream_deleted` tombstone budget -- `max(20, 2% of live)` (`import/plan.ts`'s `rollingDeleteBudget`), closing the "slow drip" gap the per-tick bound (LDB-I3) alone left open (5% every 5-minute tick, 288 ticks/day, empties the whole corpus in a day). `planTick` takes the trailing-24h applied count (`deletesAppliedLast24h`, computed by `import/cmini.ts`'s `countDeletesAppliedSince` -- one indexed D1 query over real, rev-bumping `upstream_deleted` events only, never the informational not-following variant) as an input and stalls ALL of this tick's deletes (never partially) the instant applying them would push the rolling total over budget -- the exact same `deleteStalled` shape LDB-I3 already produces, now carrying a `count` field (how many WOULD have been deleted) so a caller can report real severity even though `delete` itself is emptied out for safety. Numbers chosen against the catalog's real churn: production's entire event history (45,257 events, 2026-09-13) contains ZERO `upstream_deleted` events of any kind ever -- `max(20, 2%)` is generous next to that observed 0/day maximum while still bounding a hypothetical mass-deletion day to ~2% of the corpus instead of ~100% | `tests/import/plan.test.ts` (`[LDB-I22]` describe: exactly-at-budget applies, one-over stalls with fetches unaffected, `[property]` over random live/unlisted/prior-applied triples), `tests/import/tick.test.ts` (`[LDB-I22]` describe: 4 real rounds of 5 apply, the 5th -- still within the per-tick bound alone -- stalls end to end against a real D1-backed count) |
| LDB-I23 | `IMPORT_DELETES` (env var, `src/env.ts`/`wrangler.toml`'s `[vars]`, default `"on"`): a kill switch for a hostile/broken upstream -- `"off"` (`import/cmini.ts`'s `importDeletesEnabled`) makes the importer never tombstone anything, checked BEFORE `planTick`'s own stall verdict so it is never a partial "some deletes still went through." Reported with the same stalled-style visibility as LDB-I3/I22 (`cmini.stalled` set to an `IMPORT_DELETES=off` reason whenever a real pending deletion was suppressed, cleared when nothing was pending); never blocks fetches or any other tick work | `tests/import/tick.test.ts` (`[LDB-I23]` describe: a real pending delete never tombstoned + re-stalls identically across ticks + flipping back on lets it through; no stall reported when nothing was pending; an unrelated fetch still lands) |
| LDB-I25 | Hostile-upstream recovery (`import/recovery.ts`'s `unstallImport`): `POST /v1/admin/import/unstall` (admin lane) clears `cmini.stalled` deliberately and appends one `admin.import_unstalled` event (`admin: 1`, `rev` NULL, layout-independent) -- idempotent when nothing is stalled (`was_stalled: null`, still logs). A MANUAL OVERRIDE, not a fix: the very next tick re-plans from the SAME inputs LDB-I3/I6/I22 always use, so an upstream that is STILL short-listing or STILL mass-deleting re-stalls immediately, with no grace period | `tests/api/admin.test.ts` (`[LDB-I25]` describe: anonymous/non-admin/admin matrix, the no-op case, and the unstall -> re-stall equivalence proof against a real still-over-bound upstream) |
| LDB-I26 | Hostile-upstream recovery (`import/recovery.ts`'s `listRestorableUpstreamDeleted`): `POST /v1/admin/import/restore-deleted {since, limit?, dry_run?}` (admin lane) bulk-restores `upstream_deleted` tombstones at or after `since`, bounded per call (`limit`, capped at 500), via `restoreLayout`'s own path per record (never a bespoke bulk-write). Naturally idempotent (a restored layout's `deleted = 0` drops it out of the candidate query on its own) and NEVER touches a record the OWNER deleted (`kind: 'deleted'`) even if it also has an older `upstream_deleted` event in its history -- the candidate query joins each layout to the event AT its own current `layout_rev`, so only a layout whose LATEST layout-scope write is still `upstream_deleted` qualifies. `dry_run: true` returns the candidate list and writes nothing | `tests/api/admin.test.ts` (`[LDB-I26]` describe: 400 on a malformed `since`; dry-run-then-real-then-idempotent-repeat; never restores after the owner re-deletes; `limit` bounds one call) |
| LDB-K1 | L3 (`Idempotency-Key`, design/layout-db/review/PROPOSAL.md §2.1): a replay -- the same `(scope, key)` reused within 24h for the SAME `method`+`path`+`request_hash` as the request that first claimed it -- returns the exact stored `status`/body (`Idempotency-Replayed: true`) and writes nothing: the `events` count for the layout involved is unchanged, no D1 write happens beyond the idempotency row's own read | `tests/api/idempotency.test.ts` |
| LDB-K2 | L3: a mismatch -- the same `(scope, key)` reused within 24h for a DIFFERENT `method`, `path` or body -- is refused `422 idempotency_mismatch` and writes nothing (no event, no idempotency row overwritten) | `tests/api/idempotency.test.ts`, `tests/conformance/layouts-write/*-422-idempotency_mismatch.json`, `tests/conformance/layouts-like/*-422-idempotency_mismatch.json` |
| LDB-K3 | L3: scope isolation -- `(actor scope, key)` is the whole key; the Ed25519 client lane scopes on the client's own id (`Actor.via`, already `` `client:<id>` ``), the bearer lane on the Discord user id (`via` is the constant `"discord"`, which names no caller on its own). Two different scopes reusing the identical key are completely independent: neither's replay/mismatch check ever sees the other's row, and one can never replay the other's stored response | `tests/core/idempotency.test.ts` (`idempotencyScope` matrix), `tests/api/idempotency.test.ts` |
| LDB-K4 | L3: a key older than 24h (from its own stored `at`) is ignored -- treated exactly as if no row existed, so the SAME key can be reused for a genuinely new write once its window has passed, and a request that reuses it after expiry is never told `422` for a body that no longer matches an already-expired attempt | `tests/core/idempotency.test.ts` (`isExpired` boundary), `tests/api/idempotency.test.ts` (`TEST_CLOCK`-stepped expiry case) |
| LDB-K5 | L3: a request with no `Idempotency-Key` header is completely unaffected by this mechanism -- ordinary write behavior, no D1 read/write from `core/idempotency.ts` at all, on every mutating `/v1/layouts*` route (writes, likes, restore, transfer); the header is out of scope everywhere else (admin, webhooks) | `tests/api/idempotency.test.ts` |
| LDB-K6 | L3: property -- for any sequence of writes, each sent 1-3 times with the same `Idempotency-Key` (whether a real success or a stored 4xx), the resulting `events` are EXACTLY those of sending each one once; the accepted write-rate counter (`LDB-R6`) is charged once per key, never again on a replay (a genuine replay short-circuits before `rateLimitWrites` even runs, `src/auth/idempotency.ts`, mounted ahead of it in `src/index.ts`) | `tests/api/idempotency.test.ts` (`[property]`) |
| LDB-K7 | L3: reservation closes the concurrent-duplicate gap -- the `(scope, key)` pair is claimed (`status = 0`/pending, `core/idempotency.ts`'s `acquireIdempotencySlot`) BEFORE the handler runs, so of two genuinely concurrent identical requests, exactly one ever executes the write (one event, one D1 write) and the other gets either `409 idempotency_in_progress` (the reservation is still fresh) or the winner's own replayed response (the winner finished first) -- never a second real write, and never a `409 stale` from having been let through to run the handler for real. On completion the reservation becomes a real replayable row (2xx/4xx-except-429, `completeIdempotency`) or is dropped outright (429/5xx, `deleteIdempotency`) so a retry starts completely fresh rather than racing a doomed reservation. A pending row untouched for ~60s (`PENDING_STALE_MS`) is presumed abandoned and taken over by the next request for that key (a compare-and-swap on the row's own `at`, so two simultaneous takeover attempts can't both succeed) | `tests/api/idempotency.test.ts` (`[LDB-K7]`) |
| LDB-L1 | Likes move `like_count`, `likes` and `meta.revision`/`seq` only -- never `layout_rev`, a format's own `rev`, `modified_at`, a format's own `modified_at`, or `layouts_modified_at`. **Amended (21-formats.md D13 L1):** a like needs no version -- it succeeds if the layout is live and the actor hasn't liked it; a REPEAT like is no longer a silent no-op, it fails with `409 already_liked` and changes nothing (no event, `like_count` untouched); of two concurrent likes from the same user, exactly one succeeds and the loser gets the same `already_liked` (the `likes` table's own `(layout_id, user_id)` PK is what makes that atomic) | `tests/api/likes.test.ts`, `tests/events/races.test.ts` |
| LDB-L2 | 21-formats.md D13 L2: an unlike succeeds if that user has liked the layout; otherwise `409 not_liked`, changing nothing (no event, `like_count` untouched) -- mirrors L1/LDB-L1. **Amended (coordinator review, third batch, HIGH):** of two concurrent unlikes from the same user, exactly one succeeds -- unlike L1's like race, `likes` has no PK to make this atomic on its own, so `appendLike`'s own event insert is itself conditional (`INSERT ... SELECT ... WHERE EXISTS (SELECT 1 FROM likes ...)`, re-evaluated at batch-commit time, not at the caller's earlier read); an insert that lands 0 rows means the loser's own premise no longer held, and the DELETE/like_count recompute below it were no-ops -- the loser gets `409 not_liked`, one event only, fold equals the row. The same conditional insert also requires the layout be live (`EXISTS (... layouts WHERE id=? AND deleted=0)`), closing a like landing on a just-deleted layout the same way (a fresh read after a 0-row insert tells the two reasons apart -- `404` for a tombstone, `already_liked`/`not_liked` otherwise) | `tests/api/mf-l2-unlike-race.test.ts` (`db.batch` spy forces the injected unlike/delete to commit strictly between the outer call's stale reads and its own batch, the same technique MF-6's HTTP races use) |
| LDB-L3 | 21-formats.md D13 L3: likes never change any version, so a like never fails an edit and an edit never fails a like -- a like, a format write and a rename racing the same layout all land | `tests/api/likes.test.ts` (`[LDB-L3]`) |
| LDB-L4 | `like_count` always equals `COUNT(DISTINCT likes.user_id)` for the layout (H24, 2026-09-13: restored to this exact identity -- the L5 admin like-count override, and its `like_adjust` term, were removed entirely; mods can never move `like_count`). Checked after every step (including likes) of the shared write model, and specifically across a write racing a concurrent like/unlike (M1: `commitWrite`'s own `layouts` upsert self-heals `like_count` from a live `COUNT(*) FROM likes` subquery, never a caller-supplied value, and `foldLayout`'s replay carries its own running like-delta tally forward across a layout-scope event instead of reverting to that event's own baked-in count) | `tests/events/fold.test.ts` (`checkLayoutInvariants`), `tests/api/mf1-like-race.test.ts` (`[LDB-P22]`) |
| LDB-L5 | B1 (design/layout-db/review/audit-db.md B1, saltorbit 2026-09-12, LEDGER.md "likes \| union..."): **the importer never removes a like.** For a following layout (and, as before, a not-following one) `import/apply.ts`'s `applyMapped` only ever adds an upstream liker missing locally (`importAppendLike("liked", ...)`); it never calls `"unliked"` for any layout, following or not -- likes are a union of cmini's and ours by user id. Every `likes` row carries `via` (migrations/0011: `import:cmini` for a union-add, the actor's own lane -- `discord`/`client:<id>` -- for a real user's like), reusing the existing source-client vocabulary. `import/plan.ts`'s `planTick` only fetches on a like-count mismatch when UPSTREAM's count is higher than ours (a real signal we might be missing a like to union in) -- a local count higher than upstream's (likes made through us that cmini's own count can never independently reach) is not, on its own, a reason to fetch | `tests/import/cases.test.ts` (`[LDB-L5]`: union-add never unlikes even when upstream's like list shrinks to empty; the real-like-races-the-importer's-own-union-add case), `tests/import/plan.test.ts` (`[LDB-L5]`: upstream-higher fetches, local-higher does not), `tests/api/likes.test.ts` (the `likes.via` column on a real like) |
| LDB-L8 | review/LEDGER.md L8, review/LATENCY.md §14/§14b: every successful write route costs at most a documented, enumerated number of D1 round trips (one `.first()`/`.all()`/`.run()`/`.raw()` or one `.batch()`, whichever its own code path uses) -- `create` <= 2 (<= 3 when it also inherits a tombstoned name's likes, LDB-L8c), `putFormat`/`patchFormat`/`renameLayout`/`deleteLayout`/`restoreLayout`/`likeLayout`/`unlikeLayout`/`seedMagic` <= 2 each, `transferLayout` <= 3 (LDB-L8d: the extra one is a real authorization read, not redundancy). A generated-from-the-route-list matrix, not a sample -- one row per verb function `core/write.ts`/`core/likes.ts` exports | `tests/api/l8-roundtrips.test.ts` |
| LDB-L8a | review/LEDGER.md L8: name-clash enforcement is the `layouts_name_live` partial UNIQUE index ALONE -- `commitWrite` (`core/events.ts`) no longer runs a separate pre-read `SELECT` before its own batch to guess whether a create/rename/restore/transfer's name will collide; a real collision fails the batch itself (`UNIQUE constraint failed: layouts.name`, caught and mapped to the same `nameTaken()` the pre-read used to throw). Restates LDB-P4's own mechanism (`tests/events/races.test.ts`'s header already documents the batch as the true guard, and its own race case is unaffected by this change) -- this id tracks the round-trip cost of that mechanism, not a new correctness claim | `tests/api/l8-roundtrips.test.ts` (rename/restore cases, `<= 2` where the pre-read would have made it 3) |
| LDB-L8b | review/LEDGER.md L8: a like/unlike costs at most one D1 read (`core/likes.ts`'s `loadForLike`, to resolve `ref` and refuse a deleted/qwerty target) plus `appendLike`'s own one commit batch -- `appendLike` accepts the already-loaded `LayoutRow` from its caller instead of re-reading it, and no longer runs a separate pre-batch "already liked?" `SELECT`: the guarded batch's own `EXISTS`/`NOT EXISTS` conditions (LDB-L2) decide `already_liked`/`not_liked` at commit time either way, race or not (`tests/api/mf1-like-race.test.ts`/`tests/api/mf-l2-unlike-race.test.ts` still pass unmodified). Every existing already_liked/not_liked/race assertion (LDB-L1, LDB-L2) is unchanged -- this id is the round-trip count on top of that same behavior | `tests/api/l8-roundtrips.test.ts` (`<= 2`, was 4) |
| LDB-L8c | review/LEDGER.md L8, LDB-P9: reclaiming a tombstoned name copies that tombstone's likes in a NUMBER OF D1 ROUND TRIPS INDEPENDENT OF HOW MANY LIKERS IT HAD -- one combined query (`core/write.ts`'s `tombstoneLikers`, a CTE) finds the latest tombstone and every one of its likers in one round trip (replacing two separate lookups), and `core/events.ts`'s `appendInheritedLikes` writes one `liked` event per former liker plus a guarded `likes` copy for each, ALL in one `db.batch()` -- never one `appendLike` call per liker (a create reclaiming a name liked by 1 user and one liked by 5 cost the exact same round trips; `tests/api/write.test.ts`'s own `[LDB-P9]` cases confirm the resulting events/likes/feed shape is byte-identical to before) | `tests/api/l8-roundtrips.test.ts` (1 vs. 5 likers, same count) |
| LDB-L8d | review/LEDGER.md L8: `transferLayout`'s own read (`loadForWrite`) and its `to`-is-a-known-author check run CONCURRENTLY (`Promise.all`), not one after the other -- same two round trips, overlapped in wall time rather than paid sequentially | `tests/api/l8-roundtrips.test.ts` |
| LDB-L8e | review/LEDGER.md L8, review/LATENCY.md §14b: a FRESH `Idempotency-Key` reservation (`core/idempotency.ts`'s `acquireIdempotencySlot`, the common case: no row stored yet for this `(scope, key)`) costs exactly 1 D1 round trip. **Known gap, deliberately not closed:** a REPLAY (the row already holds a final, matching response) costs 2 -- the reserve-attempt and a second read of the existing row -- because no safe way was found to fold them into one `INSERT ... ON CONFLICT DO UPDATE ... RETURNING` round trip: D1's `meta.last_row_id` is a per-connection last-successful-insert marker, not per-statement (verified by hand against real miniflare D1: a losing/conflicting attempt against a row a PRIOR call in the same connection just inserted reports `last_row_id` equal to that row's own rowid too), so using it to tell "I just inserted this" from "this already existed" would misreport a genuine conflict as acquired -- exactly the double-apply hazard LDB-K7 exists to prevent. Left at 2 rather than risk that, on a path that only matters after a client's own retry | `tests/api/l8-roundtrips.test.ts` |
| LDB-M1 | `/v1/meta.last_diff` is `{at, ok}` written on every diff-tick run including failures; `/v1/meta`'s ETag folds it in so a poller can never get stuck on a stale 304. **Narrowed by LEDGER.md L4**: `.last_drill` is gone along with the Fly drill (LDB-D5, retired) | `tests/import/difftick.test.ts` |
| LDB-M2 | `/v1/meta.health` = `{dump: {last_at, seq, age_s, stale}, diff: {last_at, age_s, stale}}`; `stale` is `age_s > 48h`, and a record that has never run (`last_at: null`) is always `stale: true`. Deliberately NOT part of the ETag (`age_s` moves every second; hashing it in would defeat every 304/edge-cache hit) | `tests/api/meta.test.ts` (`[LDB-M2]` describe: fresh/stale/just-under-48h, fake-clock offsets) |
| LDB-M3 | Hostile/vanished-upstream visibility (saltorbit 2026-09-13): `/v1/meta.health.import` = `{stalled: {since, reason} | null, deletes_24h, deletes_budget_24h, deletes_planned, deletes_applied, deletes_disabled}` -- `stalled` is `import_state`'s own `cmini.stalled` row (renamed `at` -> `since` on the wire) and is folded INTO the ETag (same reasoning as `last_diff`/LDB-M1: a discrete value that only changes on a real tick, so a poller must see it move or it could 304 forever right after a zero-event collapse/stall); `deletes_24h`/`deletes_budget_24h` are the opposite -- continuously time-dependent, like `health.dump/diff`'s own `age_s` -- and deliberately stay OUT of the ETag. `deletes_24h` is a live count of REAL (rev-bumping) `upstream_deleted` events in the trailing 24h (`core/etag.ts`'s `readHead` folds this into the SAME one-query validator `/v1/meta` already reads, so the route's own "at most 4 prepares" budget, LDB-R9, doesn't grow); `deletes_budget_24h` is `rollingDeleteBudget(layout_count)` (LDB-I22); `deletes_planned`/`deletes_applied` are the last tick's own figures (`cmini.last_tick`); `deletes_disabled` is `IMPORT_DELETES`'s live current value (LDB-I23), read fresh every request | `tests/api/meta.test.ts` (`[LDB-M3]` describe: stalled since/reason + cleared, deletes_planned/applied from last_tick + null before any tick, deletes_24h counts only real in-window tombstones, deletes_budget_24h formula), `tests/import/tick.test.ts` (`[LDB-I22]` describe's own end-to-end `cmini.stalled` check) |
| LDB-N1 | `check_name` is the bot's rule set with the bot's strings (`NAME_SET` minus the space), plus the 64-char cap and the ULID-shape refusal, applied to `POST`, rename, and (20-spark.md S2) restore's optional `{name}` | `tests/api/names.test.ts`, `tests/api/patch.test.ts`, `tests/api/restore.test.ts` |
| LDB-P1 | `commitWrite`/`appendInfo`/`appendLike` (`src/core/events.ts`) are the only writers of `layouts`/`layout_formats` (besides `dump/restore.ts`'s own bulk restore); `appendInfo`/`appendLike` append an event that bumps no rev at all and moves neither table's row -- `liked`/`unliked`/admin-note events land in the feed with the layout's fields and formats otherwise untouched. **Narrowed by 21-formats.md (several formats per layout, §4):** the general "one write, one rev-bumping event, `seq` gapless" claim this row used to make is now MF-2's own (LDB-P17), which additionally states the exactly-one-event-PER-SCOPE partition a create/import needs; MF-3 (LDB-P18) owns "the record equals the fold of its events" | `tests/events/fold.test.ts`, `tests/events/races.test.ts` (`[LDB-P1]` case: `appendLike` alone, concurrent), `tests/tools/onlywriter.test.ts` |
| LDB-P2 | **Restated by 21-formats.md (§2.2/§2.3/§4):** `layout_revs`' PK, now on `(layout_id, n)`, is still the ONE concurrency guard -- shared across both scopes of one layout (a create's two events claim two consecutive `n` values in the same batch). `If-Match` compares the write's own scope only (MF-11 owns the scoped-token shape itself): an `If-Match` mismatch writes nothing and returns the current record; two writes racing in the SAME scope at one rev -> exactly one commits, the other gets `409 stale` naming that scope and its current rev (MF-6 owns the cross-scope half: writers on different scopes of one layout both land). An absent `If-Match` on an existing record (`PUT`/`PATCH`/`DELETE`/`transfer`) is refused with `400 if_match_required`, never treated as a blind overwrite -- checked before any read or mutation (`*` still means "overwrite on purpose", scoped or not, but the client must say so explicitly) | `tests/api/ifmatch.test.ts`, `tests/api/write.test.ts`, `tests/api/conformance.test.ts` |
| LDB-P4 | A name is released only by delete or rename | `tests/events/names.test.ts`, `tests/events/races.test.ts`, `tests/api/refs.test.ts`, `tests/api/names.test.ts`, `tests/api/patch.test.ts` |
| LDB-P5 | A sampled following record read equals upstream on the projection (likes sorted, magic excluded since M1 -- design/layout-db/17-magic-ownership.md §3; LDB-I10/I11 own why), and our total live layout count equals upstream's. **Narrowed by LEDGER.md L4 (2026-09-12, the diff shrink):** the diff no longer enumerates the whole corpus -- it checks `layoutCount()` equality, then draws a random sample (`DEFAULT_SAMPLE_SIZE`, 50) of live, `following` layouts (`OursSource.sampleFollowing`) and content-compares each against a single fresh upstream fetch by name; a sampled name upstream no longer answers for is `missing` (informational-but-failing, never silently dropped), a shape failure is `invalidUpstream`, everything else is `matched` or `contentDiffs`. `forked`/unlinked layouts are never sampled at all (only `following` ones are, by construction of `sampleFollowing`), so there is no more `divergent`/`unresolved` bookkeeping to report. Comparison happens in spark: `d1Ours` yields the record's own `spark/1` row directly, `httpOurs.sampleFollowing` reads each sampled id's `?format=spark/1` detail, and `parseUpstreamRaw` runs upstream's detail through `fromCmini` (LDB-I13 owns its own validate half) | `tests/upstream-diff.test.ts` (daily, live), `tests/import/diff-unit.test.ts` (unit half, over `upstream-100` and a fake `OursSource`) |
| LDB-P6 | `/v1/changes` serves from `since=0`, including after a restore | `tests/events/feed.test.ts`, `tests/rehost.test.ts` |
| LDB-P7 | Every error response carries `error` and `message`; every (route, status) pair has a conformance case | `tests/api/conformance.test.ts` |
| LDB-P8 | A tombstone is unreadable by name from the moment of deletion (phase 1) and restorable by id AT ANY TIME by its owner or an admin (20-spark.md §1 decision 8: the 30-day owner window is gone), keeping history. **Restated by 21-formats.md (several formats per layout):** delete and restore are LAYOUT-SCOPE writes only -- neither touches `layout_formats` at all, so every one of a layout's stored formats (payload, rev, `modified_at`, everything) is simply untouched by either, with no carry-forward step needed (there is no single `format`/`payload` left on a layout to carry). The restore body is optional (decision 9, refined §8 R-L1): absent, `{}`, or `{name}` (any other key is `400 bad_request`); without `name`, restoring under a reclaimed name is `409 name_taken` with `holder`; a `name` goes through `check_name` (LDB-N1 amended) and, when it differs from the tombstone's own, the `restored` event carries `detail: {renamed_from}` -- both records keep their own likes | `tests/api/refs.test.ts`, `tests/api/restore.test.ts` |
| LDB-P9 | design/layout-db/18-command-decisions.md §2 D1 (saltorbit: "tombstoned name carries likes for whoever takes it. it's a quirk people like"): `POST /v1/layouts` whose name is currently held by a tombstone -- ANY owner, not just the same actor -- copies the tombstone's likes onto the new record as `liked` events `via: "name_inherited"`, `detail: {from: <tombstone id>}`; the tombstone keeps its own likes/history untouched and stays restorable (which then leaves both records carrying the same users' likes -- accepted); no tombstone for the name inherits nothing; every format's own `has_magic`/payload are unaffected (name inheritance is a layout-scope `liked` event only) | `tests/api/write.test.ts` |
| LDB-P10 | design/layout-db/18-command-decisions.md §2 D2: a rename (`PATCH {name}`) never loses the id -- the record's id, rev chain, likes, history and `_dbId`-equivalent projection are unchanged and reachable by the new name AND by id; the old name is freed (LDB-P4 owns that half) | `tests/api/patch.test.ts` |
| LDB-P11 | `upstream` is a fold (20-spark.md S3a): the row's value equals the latest rev-bumping event's `after.upstream`, through any number of transitions (`commitWrite` writes it into `after` and the `layouts` upsert alike, so the two can never disagree). **Restated by 21-formats.md D12**: a restore of an old-shape dump (no `upstream_*` columns at all) yields NULL on every row, and `upstreamOf` now simply reads that NULL back -- the legacy fallback it used to reproduce (LDB-I2a/I12) is deleted. **Restated again by 21-formats.md (§2.2, follow state is layout-level, MF-12):** `upstream` lives on `layouts` only, never on a `layout_formats` row -- but "the latest rev-bumping event" here means the latest one that TOUCHES upstream (MF-12's own flag): every layout-scope event, plus a format-scope event on lineage `spark` (which also carries `after.upstream`, precisely because it forks/keeps upstream too); a format-scope event on any OTHER lineage carries no `upstream` field on its snapshot at all and is skipped by the fold, exactly like before it changed nothing | `tests/events/upstream.test.ts` (fold + old-shape-row property), `tests/rehost.test.ts` (dump/restore round trip) |
| LDB-P13 | 20-spark.md S5 (19-upcast.md round 2 §3/§7's P11, renumbered): older-major write. A `PUT`/`POST` naming a format `F` not the record's LINEAGE's latest is stored at the lineage's latest with `detail.written_as = F`; a `PUT` in `F != that lineage's currently-stored format` is refused `409 format_behind` (same body shape as `held`: `held: true, format, see, rev`) iff `translate(the stored row, F)` is held (R1: the client could never have read the format whole in `F`, so a pure upcast would be a blind overwrite) -- checked BEFORE `validatePayload`, and only when `F` differs from the currently-stored format; after an accepted write, `GET :ref/rev/:n?format=F` returns exactly what was written; under any sequence of writes a lineage's stored major never decreases. `POST` in an older major is R2's chain with no R1 (there is no prior record to have been unreadable). **Restated by 21-formats.md (§2.3 D4):** `?as=` is gone -- every read is `?format=` now, required, no default. Exercised through the stub `t/1 -> t/2 -> t/3` lineage, registered as a real SECOND stored lineage alongside `spark`, over real HTTP writes (`spark/1`/`mana2/1` are each still one-major) | `tests/api/put-format.test.ts` (matrix over registered pairs × {held, not}; property test over random write-sequence × format × payload-risk, asserting the stored major never decreases) |
| LDB-P14 | **Restated by 21-formats.md (§2.2/§4, several formats per layout):** `commitWrite`'s `currentN` (the importer's "expect this base or fail" input, `appendWrite`'s old `expectRev` under its new name) is enforced by the SAME `layout_revs` PK guard every write goes through (LDB-P2/LDB-P20/MF-6) -- a system write commits only if the layout's counter `n` is STILL at the value it read, checked by the batch's own insert failing otherwise, never a separate pre-check. Every system writer (the importer) builds its write from the SAME read its payload was computed from, touching only the layout's own fields and lineage `spark` (§3 F2); a user write interleaved between that read and the system writer's own write survives untouched, on EITHER scope, and the record stays at whatever state the user write left it in (LDB-I14/MF-12's "the importer never writes a forked record" depends on this: without it, a stale system write could silently land on top of a user's fork). `import/cmini.ts`'s `tick()` catches the thrown `RevConflictError` per id -- both in the fetch/apply loop (`applyFetchedId`) and the delete loop (`applyDeleteAction`) -- counts it in `TickStats.raced`, and moves on; it is never an uncaught exception that would abort the whole tick (and every OTHER id still queued behind it), and it is never retried within the same tick (the next tick re-evaluates the raced id from a fresh read; contrast a USER write's cross-scope race, which retries within the same request via `commitWithRetry`, LDB-P20). **Amended (coordinator review, third batch, MEDIUM):** the same "never abort the tick" guarantee extends to likes, now that D13 L1/L2 make a repeat like/redundant unlike a real thrown error instead of a no-op -- `import/apply.ts`'s `importAppendLike` wraps every importer-side `appendLike` call and swallows `already_liked`/`not_liked` (exactly the state the importer wanted anyway; the public API stays strict), and `importLikes` additionally dedupes its `userIds` before ever calling it, since cmini's own `likes` array in the upstream response isn't deduped and a repeated id would otherwise throw on its own second call | `tests/events/upstream.test.ts` (deterministic race case + a `fast-check` property over random interleaving counts), `tests/import/tick.test.ts` (`tick()`'s own per-id catch-and-count, both loops), `tests/import/cases.test.ts` (`[LDB-P14]`: a duplicate id in the upstream `likes` array, and a real unlike racing the importer's own unlike of the same user, forced deterministically via a spy on the cross-module `appendLike` import) |
| LDB-P15 | 20-spark.md S3s (decision 14): every write states its `source: {client, version}` (`Write`/`Info`/`Like` all require it -- tsc enumerates every call site). `client` is PROVEN, never declared: `client:<id>` on the client lane (`Actor.source_client`, the signature's own client id), `discord-app:<app id>` on the user lane (the Discord OAuth2 app the bearer token was issued to), `system:cmini-import`/`system:migration` for system writers -- no header or body field can set it (the spoof matrix). `version` is the validated `X-Client-Version` header (parsed once in `requireActorOnWrites`, ≤ 64 chars of `[A-Za-z0-9._+/:-]` else `400 invalid_client_version`) or `null`; it never influences `client`. **Restated by 21-formats.md (several formats per layout):** `commitWrite` stores `source` on every event it appends AND folds it onto whichever row(s) that event's scope touches -- a layout-scope event moves `layouts.source`, a format-scope event moves that lineage's `layout_formats.source`, a create's two events move both; `appendInfo`/`appendLike` store it on the event only, never folding it onto either table. `RecordRow.source`/`toWire`/`sansPayload` carry the fold (`before`/`after`, `/v1/changes`, webhooks, list, `full=1`); `/history` and `/rev/{n}` return each event's OWN source, computed off that event's row (`sourceOfEvent`) so a pre-0005 event (no `source_client` column) reads `{client: "legacy:" + via, version: null}` with no extra query; a pre-0005 record (`layouts.source_client` still NULL) reads `source: null` on every route (no per-record legacy synthesis, unlike `upstream` -- a `layouts` row has no `via` to fall back on) | `tests/api/source-matrix.test.ts` (lane × verb × version-header matrix; the spoof matrix: no header/query/body field sets `client` on either lane), `tests/events/source.test.ts` (replay property, per scope: a format-scope write moves only that format's source, a layout-scope write moves only the layout's), `tests/auth/discord.test.ts`, `tests/api/history.test.ts` |
| LDB-P16 | MF-1 (21-formats.md §2.2/§4): format independence -- a write to format A never changes format B's payload, rev, `created_at` or `modified_at`; a layout-level write changes no format row at all. Part of the ONE shared `fast-check` write model (random sequences of every write kind, both scopes, over `spark` and a test-only second lineage): every format row outside the write's own scope is snapshotted before and checked byte-equal after, at every step | `tests/events/fold.test.ts` (the shared write model) |
| LDB-P17 | MF-2 (21-formats.md §2.2/§4): rev partition -- every rev-bumping event has exactly one scope (`format` NULL bumps `layout_rev` by 1, otherwise it bumps that format's own `rev` by 1); a write that changes both scopes (a create, an import) appends one event per scope, never one event claiming both. Per layout, `layout_revs.n` is gapless from 1; per `(layout, lineage)`, `rev` is gapless from 1; per layout, `layout_rev` equals its count of layout-scope events | `tests/events/fold.test.ts` (the shared write model's own per-step gaplessness check) |
| LDB-P18 | MF-3 (21-formats.md §2.1/§4): replay equivalence -- folding a layout's events in `seq` order, with payloads from `layout_revs`, reproduces its `layouts` row and every `layout_formats` row exactly (`foldLayout`, the several-formats successor to the old single-format `foldRecord`) | `tests/events/fold.test.ts` (the shared write model), `tests/rehost.test.ts` (dump/restore round trip) |
| LDB-P19 | MF-5 (21-formats.md §2.1/§4): at least one format -- every layout has at least one `layout_formats` row; creation always takes one (a create is a `created` event plus a `format_added` event, one D1 batch) and nothing in this slice removes one (format removal is explicitly out of scope, §2.4) | `tests/events/fold.test.ts` (the shared write model's per-step check: every existing slot has >=1 format row after every op) |
| LDB-P20 | MF-6 (21-formats.md §2.2/§4): concurrency per scope, serialization per layout -- two writers on one format with the same If-Match: exactly one lands, the other gets `409 stale` naming that format's current rev; writers on different scopes of one layout both land (a layout-scope rename and a format-scope replace racing on one layout never block each other); a format write racing a transfer or delete that lands first is refused by the new owner/deletion state, never landing on a tombstone or for a former owner. The guard is still `layout_revs`' PK (now `(layout_id, n)`, LDB-P2) inside the batch -- a losing writer's whole batch rolls back and retries from a fresh read (`commitWithRetry`) | `tests/events/races.test.ts` (same-format race, cross-scope race, forced PK-collision retry path) |
| LDB-P21 | MF-11 (21-formats.md §2.3/§4): scoped If-Match -- a write accepts only its own scope's token (`"layout:<rev>"` for a layout-scope write, `"<lineage>:<rev>"` for a format-scope write) or `*`; a bare unscoped number or another scope's token is `400 bad_request`, checked before any read or mutation; every write response's `ETag` is exactly its own scope's token | `tests/api/ifmatch.test.ts` (generated matrix: every write route × {absent, bare number, own scope, other scope, wrong scope, `*`}) |
| LDB-P22 | 21-formats.md D13 L4/M1 (H24, 2026-09-13: restored -- the L5 `like_adjust` term was removed): `commitWrite`'s own `layouts` upsert sets `like_count` from `(SELECT COUNT(*) FROM likes ...)`, never a caller-supplied value, so it self-heals to the truth on every write regardless of whether that write is itself about a like -- a like/unlike landing strictly between a write's own fresh read and its commit is never reverted by that write's upsert, and `commitWrite`'s own return value (what an ordinary write's caller sees) reads the same live `like_count`/`link` back in the same batch. `foldLayout`'s replay carries its own running `withLikeDelta` tally AND its own `link` fold (from the latest `link_approved`/`link_cleared` event) forward across a layout-scope event's adoption of `e.after`, rather than reverting to that event's own (possibly stale) baked-in `like_count` | `tests/api/mf1-like-race.test.ts` (a real like/unlike HTTP request injected strictly between a PUT's own fresh read and its commit, via the same `byRefWithFormats`-spy technique as LDB-P20's HTTP races) |
| LDB-P23 | 21-formats.md D13 E3: an edit based on its part's current version succeeds even when another part of the same layout is written in between, unless it fails on its own merits -- the server-side retry (`commitWithRetry`, MF-6/LDB-P20) is what guarantees this, including under a real concurrent pair on the SAME layout (a rename racing a format replace, both `If-Match: *`, driven via `Promise.all`) and, at the edge, when every retry attempt still collides on the shared `layout_revs (layout_id, n)` PK: exhausting `MAX_RETRIES` answers the SAME `409 stale` shape (§2.3: scope, rev, a fresh record, `last_write`) any other staleness does, never a raw `RevConflictError` leaking through as an unmapped `500`. **Amended (coordinator review, third batch, LOW):** an exhausted retry on a format ADD (`If-None-Match: *`) can reach this fallback with no existing row for that lineage at all -- `last_write` is `null` in exactly that case (there is no prior rev-bumping event for a scope that was never written), never a second `500` from assuming one always exists | `tests/api/mf2-exhausted-retry.test.ts` (stubs `commitWrite` to always throw `RevConflictError`, a format-scope write, a layout-scope write, and a format ADD with no prior row -- mutation-checked by hand, reverting the `null` guard and confirming the ADD case goes red with a real 500), `tests/events/fold.test.ts` (the shared write model's `concurrent_pair` op; mutation-checked by setting `MAX_RETRIES` to 0, which correctly turned it red) |
| LDB-P24 | design/layout-db/23-geometry.md §10.1 (2026-09-13 cutover): `POST /v1/admin/magic-seed` is a SYSTEM write -- the event's actor is `system:magic-seed`, via `seed:aklgg`, `admin: false`, and the record's `upstream.state` is `following` afterwards whatever it was before (a seed is a one-time migration, 20-spark.md decision 14: only system writes never fork); the candidate goes through the stored format's `setMagic` + `validate()` like a PATCH (`invalid_payload`/`magic_collision` refused, nothing written); admin lane only (anonymous 401, non-admin 403) | `tests/api/admin.test.ts` ("POST /v1/admin/magic-seed") |
| LDB-R1 | Polled routes carry `Cache-Control` + strong `ETag` and answer `304` to a matching `If-None-Match` -- compared weakly per RFC 7232 §3.2, so the `W/"…"` form Cloudflare rewrites every compressed response's ETag to (what a client that echoes what it received sends back) matches too (2026-09-10: the bot's per-minute heartbeat never got a 304 before this); the ETag changes iff the event head, the query, OR (20-spark.md S2) the wire shape changes -- a `WIRE_VERSION` constant is folded into the hashed query so a pre-deploy `If-None-Match`/edge-cached body can't keep serving the old shape at an unchanged head seq. **Amended (LDB-R9):** `/v1/meta` and `/v1/authors` also show author data, which no event moves -- their heads are LDB-R9's (`/v1/authors`' tag does NOT move on an event) | `tests/api/etag.test.ts` |
| LDB-R2 | `/v1/meta` counts and `seq`/`revision` equal the tables. **Amended (migrations/0007, LDB-R9):** `authors_version`/`authors_modified_at` equal `authors_head`'s row (`authors_modified_at` is the `last_seen_at` of the latest author insert or rename, no longer `MAX(last_seen_at)`, which moved on every sign-in) and `author_count` equals the `authors` table | `tests/api/meta.test.ts` |
| LDB-R3 | The conformance fixtures are the API contract; changing one is a documented API change | `tests/api/conformance.test.ts` (+ review) |
| LDB-R4 | Every `sort` × `limit` cursor walk visits every live record exactly once | `tests/api/list.test.ts` |
| LDB-R5 | `/rev/{n}?format=F` reproduces `F`'s own payload at ITS rev `n`, for every stored lineage's own `n` independently (revs are per `(layout, lineage)`, not per layout) | `tests/api/history.test.ts` |
| LDB-R6 | Writes are limited to 1000 per 10-minute window per actor, counted per attempt, `429` + `Retry-After`; reads are never counted | `tests/api/ratelimit.test.ts` |
| LDB-R7 | Client-lane writes are limited to 5000/10min per client, on top of the 1000/10min per-actor limit that applies to every lane; a 429 names which counter tripped (`scope`) | `tests/api/ratelimit.test.ts` |
| LDB-R8 | `GET /v1/layouts?liked_by=<user_id>` equals a filter over `likes`, composable with every other filter/sort, and rides on `?full=1` | `tests/api/list.test.ts` |
| LDB-R9 | The authors validator (migrations/0007's `authors_head`, moved by trigger by exactly 1 on every `authors` insert, delete or `user_id`/`name` change, and by nothing else). `/v1/meta`'s ETag is computed from the event head, `authors_head`, the `last_diff`/`last_drill` records and the format list, and its body carries every one of them (`authors_version` is the new field), so across any sequence of events, import passes, sign-ins, client-lane requests and bookkeeping the ETag moves on a step iff the body does, and two equal tags mean equal bodies. `/v1/authors` keys on `authors_head` alone (never the seq -- an event moves no author): `?by=id` (`{user_id: name}`, lossless) moves iff its body does; the default name-keyed shape, which collapses shared names, moves iff the stored (user_id, name) set does. A conditional request costs ONE D1 query on either route (seq + `authors_head` + both `import_state` keys as scalar subqueries on `/v1/meta`; one primary-key read on `/v1/authors`). The dump's `meta` carries `authors_version` and a restore sets `authors_head` to it exactly (LDB-D1) | `tests/api/authors-validator.test.ts` (`[property]` over generated op sequences; the trigger matrix; the counted-`prepare` cost check), `tests/api/etag.test.ts` |
| LDB-R10 | A conditional GET of `/v1/meta` or `/v1/authors` (either shape, strong or `W/` `If-None-Match`) after an author-only change -- the import adding an id, the import renaming an id upstream no longer lists under its stored name, a sign-in that renames, the import replacing a client-lane placeholder, the client lane's first sight of an id -- is never `304`, and a plain GET never gets the pre-change body from `caches.default` (`conditional()` re-validates every cached hit against the live tag) | `tests/api/authors-validator.test.ts` (change kind × route × tag form matrix, plus the property) |
| LDB-R11 | Bookkeeping never moves either validator: a sign-in that keeps its name (`last_seen_at` only), one whose name equals the import's (`name_source` only), a repeat client-lane request, an import pass that writes nothing, and a statement that sets `name` to itself each leave `authors_head.version` and both routes' ETags unchanged, and the prior tag still gets `304` -- so the spark bot's per-command `/v1/meta` check stays a 304 across sign-ins and bot writes | `tests/api/authors-validator.test.ts` (bookkeeping kind × route matrix, the trigger matrix, the property) |
| LDB-S1a | `db/tests/fixtures/db-responses/` (site-side sync fixture, design/layout-db/11-implementation-phase3.md §1 W1) equals the live `/v1/meta`, `/v1/layouts`, `/v1/layouts?full=1`, per-name `/v1/layouts/{name}`, `/v1/layouts/{name}/likes` and `/v1/authors` routes over the standard upstream-100 seed. **Restated by 21-formats.md D5**: `?as=cmini/1` is deleted -- every recorded response is spark-shaped now (`layouts-full-cmini1.json`'s filename is stale, kept until F3c updates the site's own `DbSource` to match). **Restated again by 21-formats.md (§2.3/§2.4, D4):** `?as=` is gone entirely, replaced by `?format=`, now required on every recorded route that returns a payload (`format=spark/1` throughout); every recorded body carries the new `formats` map and the two-rev shape (`layout_rev` plus each format's own `rev`) instead of a bare `rev` | `tests/api/fixture-export.test.ts` |
| LDB-T1 | Every registry id has a tagged test and every tag has a registry row | `tests/tools/invariants.test.ts` |
| LDB-V1 | design/layout-db/25-api-versioning.md: the API's public minor version is not a second, independently-bumped counter -- `core/version.ts`'s `API_MINOR` is a direct re-export of `core/etag.ts`'s `WIRE_VERSION` (the ETag-busting counter already bumped, with a real comment trail, for every `/v1` wire-shape change); `API_MAJOR` is the literal `1` (no `/v2` registered) | `tests/contract/contract.test.ts` |
| LDB-V2 | Every `/v1` response -- success or error, every route, enumerated over `db/tests/conformance/manifest.ts`'s full `CASES` list (~350+ (route, status) pairs, `LDB-R3`'s own contract) -- carries `X-AKLDB-API: <major>.<minor>` (`core/version.ts`, set once by `src/index.ts`'s global middleware + `onError`, never per-route). The same check (via `tests/api/support.ts`'s shared `assertConformanceCase`) also runs during the rehost replay against a restored database, though that file carries no `[LDB-V2]` title of its own | `tests/api/conformance.test.ts` |
| LDB-V3 | `GET /v1/meta` carries `api: {major, minor}` (matching the live `API_MAJOR`/`API_MINOR`) and `deprecations: []`, and the SAME minor also appears in the `X-AKLDB-API` response header | `tests/api/meta.test.ts` |
| LDB-V4 | The full `/v1` route table -- one row per conformance case: method, route pattern, auth lane (joined from `db/docs/adoption.md`'s endpoint table), status, and a structural (field-name/type, never value) fingerprint of the response body -- is checked in at `tests/contract/route-table.golden.json`; a live computation that disagrees with it fails with a message naming exactly what changed and what to do: bump `WIRE_VERSION` + add a `db/CHANGELOG-API.md` entry + regenerate the golden for an additive change, or design a `/v2` instead of a field/route removal or type change | `tests/contract/contract.test.ts` |
| LDB-V5 | `db/CHANGELOG-API.md` has exactly one `## 1.<n>` entry per minor from 1 up to the live `API_MINOR`, no gap, no duplicate -- a `WIRE_VERSION` bump with no matching changelog entry (or vice versa) fails | `tests/contract/contract.test.ts` |
| LDB-V6 | `db/tests/fixtures/db-responses/meta.json` (the akldb.org site's pinned sync fixture, LDB-S1a) has the same structural shape as the golden's declared `/v1/meta` (`meta/200`) shape -- the site's pinned reads and the API's declared contract cannot silently disagree on this route | `tests/contract/contract.test.ts` |
| LDB-V7 | A registered `core/version.ts` `DEPRECATIONS` entry emits `Deprecation`/`Sunset` HTTP-date headers keyed on an exact `METHOD routePath` match, and gives at least `MIN_DEPRECATION_NOTICE_DAYS` (90) of notice between `since` and `sunset`; a route with no entry gets neither header | `tests/contract/contract.test.ts` |
| LDB-W1 | Every write route is resolve → authorize → check → `appendWrite`; no file under `src/routes/` prepares a D1 statement | `tests/tools/routes-noprepare.test.ts` |
| LDB-MD1 | L5 moderation (design/akldb-site/01-plan.md §4.1): a banned actor's non-safe request is `403 banned` on both auth lanes, on every non-GET route (enumerated the same way LDB-A1 is); reads and `GET /v1/me` are never refused by it. An admin is never `banned` (`auth/roles.ts`'s `roleOf`, a role rule, not a refusal) -- `PUT /v1/admin/bans/:user_id` on a current admin is `409 cannot_ban_admin` instead. A ban (or unban) lands on the very next request, no cache window (`roleOf` is never stored in `auth_cache`) | `tests/auth/moderation.test.ts`, `tests/api/moderation.test.ts` |
| LDB-MD3 | L5 (§4.4, H24 2026-09-13: the `like_adjust` half of this row was removed with the admin like-count override -- `link` is unaffected): `link` is a fold -- the `layouts` row equals the latest `link_approved` / `link_cleared` event's own `after` (MF-3), checked by the shared write model and by the rehost dump/restore round trip. **Restated by the link-approval-on-import follow-up (23-geometry.md):** a following layout's `link_approved`/`link_cleared` event may itself come from the importer (`via: "import:cmini"`, `admin: false`) -- cmini's own `link` field is carried over automatically, validated through `core/links.ts`'s `validateLinkUrl`, and folds exactly like an admin-approved one; a bad upstream value (not https, too long, embedded credentials) is skipped with an `import_error` info event instead of ever reaching `layouts.link` | `tests/events/fold.test.ts`, `tests/rehost.test.ts`, `tests/import/cases.test.ts` |
| LDB-MD4 | L5 (§4.3): an `admin`-sourced author name survives any sequence of sign-ins (the bearer lane), client-lane requests, and cmini import passes -- `auth/discord.ts`'s upsert guards its `name`/`name_source` update on `authors.name_source <> 'admin'` (a second, unguarded statement still moves `last_seen_at`), and `import/authors.ts`'s `planAuthorNames`/`writeAuthorNames` treat `admin` exactly like `user` (never renamed, never inserted over) | `tests/import/authors.test.ts`, `tests/api/authors-validator.test.ts`, `tests/api/moderation.test.ts` |
| LDB-MD5 | L5 (§4.4): only an APPROVED link ever appears on a public wire (`layoutToWire`'s `link` field, every shape); a pending, rejected or superseded url never appears anywhere outside `GET /v1/layouts/:ref/link` (owner/admin) and `GET /v1/admin/link-queue`. **Restated by the link-approval-on-import follow-up:** the importer's own auto-carried link is exactly as "approved" as an admin's -- it never goes through `link_submissions` at all -- but once ANY link event's `via` differs from `import:cmini` (an admin approved/cleared by hand), every later import tick leaves `layouts.link` alone even if cmini's own field keeps changing (`import/apply.ts`'s `importLink`/`latestLinkVia`) | `tests/api/links.test.ts`, `tests/import/cases.test.ts` |
| LDB-MD6 | L5 (§4.4): at most one `pending` link submission per layout -- a fresh owner submission supersedes the layout's existing pending one (status `superseded`, no event: the submitter's own bookkeeping); approving or clearing a link also supersedes any other still-pending submission for that layout, in the same batch as the `layouts.link` write | `tests/api/links.test.ts` |
| LDB-MD7 | L5 (§4.1, §4.3, §4.4; H24 2026-09-13: `appendLikeAdjust` was removed with the admin like-count override): every moderation action is exactly one event, `admin: 1`, `via`/`source` the actor's own lane (extends LDB-A5) -- `appendAdmin` (bans, author-rename) and `appendModeration`/`appendLinkChange` (layout-scoped) never hard-code `via` for these new kinds | `tests/events/moderation-events.test.ts` |
| LDB-MD8 | L5 (§4.5): dump ↔ restore round-trips `bans`, `link_submissions`, `layouts.link`, `likes` byte-exact -- extends LDB-D1/LDB-D9. (H24, 2026-09-13: `layouts.like_adjust` is a dead column, migrations/0015 -- restore always writes 0 to it regardless of what a dump carries, so it is no longer part of what this row claims round-trips.) | `tests/rehost.test.ts` |
| LDB-MD9 | L5 (§4.4): the link validator (`core/links.ts`'s `validateLinkUrl`) is pure -- table-tested accept/refuse cases (https only, no embedded credentials, ≤ 2048 chars, not a URL at all); the importer runs cmini's own `link` field through the SAME validator before ever carrying it over, and a value it refuses is skipped (`import_error` info event) rather than reaching `layouts.link` | `tests/core/links.test.ts`, `tests/import/cases.test.ts` |
| LDB-MD10 | L5 (§4.1): `GET /v1/me` reports `banned` and `admin` fresh on every call -- neither is ever read from `auth_cache` (`roleOf` runs one live query per request, on both lanes) | `tests/api/moderation.test.ts`, `tests/auth/moderation.test.ts` |
