# db/ invariant registry

The `LDB-*` invariants for the layout-db service (`design/layout-db/`,
phase 1: `07-implementation-phase1.md`). This registry lives with the code
(it moves with `db/` when the service splits into its own repo, per
`00-plan.md` §7) rather than in the site's `design/INVARIANTS.md`, which
carries one pointer entry instead.

Every row here needs a test tagged `[LDB-*]` in its `test()`/`it()` title
under `db/tests/`; `tests/tools/invariants.test.ts` (LDB-T1) fails the
build when an id has no tagged test, or a tag names an id not in this
table. Each slice's PR adds its own rows (07 §6); this file starts with
the S1 rows only.

| id | invariant | enforced by |
|---|---|---|
| LDB-A1 | No write is accepted without a resolved actor; every non-GET route answers 401 to an anonymous request (enumerated from the router) | `tests/auth/routes.test.ts` |
| LDB-A2 | The Discord cache serves a success ≤ 5 min and a 401 ≤ 60 s; 5xx/429/network are never cached; the token is never stored. **Amended (20-spark.md S3s):** `resolveBearer` calls `GET /oauth2/@me` (not `/users/@me`); the cache also keys the token's `app_id` (`application.id`) beside `user_id`/`name`; a cached SUCCESS row with `app_id IS NULL` (written before 0005) is treated as a miss and re-verified; a 200 with no `user` key (the grant lacks the `identify` scope) is cached as a FAILURE exactly like a real 401, `token_invalid`, with a message naming the missing scope | `tests/auth/discord.test.ts`, `tests/api/me.test.ts` |
| LDB-A4 | Client-lane signatures (02 §3.2, 10 C1): every vector in `tests/vectors/client-signing.json` is accepted; each single-field mutation (method, path, query, timestamp ±301s, replayed nonce, body, actor, signature, key) is refused with the named 401; every malformed/missing header collapses to `bad_signature`; and every route in the sweep pins every client-lane 401 code | `tests/auth/client.test.ts`, `tests/tools/vectors.test.ts`, `tests/api/conformance.test.ts` |
| LDB-A5 | Every accepted write's event carries `via` (the actor's own lane -- 10 C1 widens this from the literal `"discord"` to `client:<id>` on the client lane); every admin action is an event with `admin = 1`; the admin client routes (register/revoke/list) follow the same admin-only, event-logged pattern, never leaking the pubkey into `detail`; the manual cron triggers (`POST /v1/admin/import/tick`, `POST /v1/admin/diff/tick`, `POST /v1/admin/nightly/tick`, X4 follow-up + follow-up 3) call the exact same `tick()`/`diffTick()`/`runNightly()` their own cron dispatches, refuse `409 import_paused` while paused (import only), and are event-logged the same way. **Amended (20-spark.md S4):** `POST /v1/admin/migrate/tick` is a fifth manual trigger, same shape, calling the SAME `migrateTick()` (`core/migrate.ts`) with no cron behind it at all (decision 11: this one is operator-only, never scheduled) and, unlike import/tick and strip-cmini-magic, NOT gated on `admins.isImportPaused` (LDB-P14's `expectRev` is what keeps the two writers safe to interleave, so there is nothing for a pause check to protect) -- logged `admin.migrate_ticked`, `detail` carrying the full `MigrateReport` | `tests/api/admin.test.ts`, `tests/api/write.test.ts`, `tests/api/clients.test.ts` |
| LDB-A6 | The admins table never has fewer than two active rows after bootstrap (the count check and the delete are one statement, so two concurrent removes at three rows can't both commit) | `tests/api/admin.test.ts` |
| LDB-A7 | Owner changes only via `transfer`; a write body naming `owner` (or any field outside the verb's schema) is refused | `tests/api/bodies.test.ts`, `tests/api/write.test.ts`, `tests/api/transfer.test.ts` |
| LDB-A8 | A client-lane nonce is accepted once: the `nonces` PK insert is the replay check, run only after the signature verifies; rows are pruned after 900s | `tests/auth/client.test.ts` |
| LDB-A9 | A revoked client's requests are refused from the revocation onward -- `clients.status` is read on every request, never cached | `tests/auth/client.test.ts`, `tests/api/clients.test.ts` |
| LDB-C1 | `db.yml`'s shape (test job on PR/push under `db/**`; deploy needs test, main+push only, migrations before deploy; daily job runs rehost + diff; actions pinned) is asserted from the parsed YAML | `tests/tools/ciwiring.test.ts` |
| LDB-C2 | `canonical()` is key-order-invariant and lossless | `tests/core/canonical.test.ts` |
| LDB-C3 | `[env.preview]` redeclares every top-level binding and var with the preview resource names; no top-level binding is missing from it | `tests/tools/wrangler-envs.test.ts` |
| LDB-C4 | The diff cron (hour=4, minute=0 slot of the one `*/5 * * * *` trigger) reads our side from D1 directly and never fetches its own origin; it is one invocation, bounded by ⌈records/500⌉ pages | `tests/import/difftick.test.ts` |
| LDB-C5 | The single `*/5 * * * *` cron trigger dispatches by `event.scheduledTime` (UTC), never `event.cron`: every invocation runs the import tick then the webhook drain; hour=3/minute=0 also prunes + writes the nightly dump; hour=4/minute=0 also runs the diff; a cron string other than `*/5 * * * *` still throws; one job throwing is caught and logged (`runJob`), never blocking the jobs queued after it in the same invocation | `tests/import/tick.test.ts` (`[matrix]`, `[property]`, `[order]`, `[isolation]`) |
| LDB-D1 | The nightly dump is complete (every table, the whole event log), its `latest.json` sha256 matches the object served, and the monthly copy is written on the 1st (and only the 1st); the manual tick (`POST /v1/admin/nightly/tick`, X4 follow-up 3) and the cron write byte-identical dumps for the same state. **Amended (20-spark.md S3a):** the dumped `layouts` rows carry `upstream_source`/`upstream_id`/`upstream_state` | `tests/api/dump.test.ts`, `tests/rehost.test.ts`, `tests/api/admin.test.ts` |
| LDB-D6 | 20-spark.md S5 (19-upcast.md round 2 §6.2, id unchanged): per-major dumps. The nightly writes one `latest.<name>-<N>.json` (slash-free) plus a `.sha256` sidecar for EVERY registered `role: "stored"` major (never an `"output"` one like `mana2/1`), built from the SAME `layouts` page the main dump already reads (no extra D1 query); each carries every LIVE record (tombstones excluded) translated to that major, or `{..., held: true, see: <native format>}` when it can't be shown; `latest.json` itself is byte-unchanged in shape. Deviation: nothing currently SERVES these files over HTTP (`routes/dump.ts`'s key routing isn't part of this slice's file list) -- tests read the written R2 objects directly through the `DUMPS` binding; served at `GET /v1/dump/latest.<name>-<N>.json` (+ `.sha256`) under an anchored key pattern, so no other bucket key is reachable | `tests/api/dump.test.ts` |
| LDB-D5 | The Fly restore drill's report is only `ok: true` when the restored database equals the dump byte-for-byte on every record: `latest.json`'s claimed sha256/bytes match the fetched dump, the local D1 restored from it (through the real `restoreSql()` code path) has the dump's exact `layout_count`/`seq`, and every layout id served over HTTP by the restored, running Worker equals the dump's own record (fields, likes, payload) exactly; a corrupted/truncated transfer, a restore mismatch, or any HTTP mismatch each independently make the whole drill `ok: false`; a step that never ran (an earlier one failed first) is distinguished from one that ran and failed. **Amended (20-spark.md S3a):** the drill's per-record HTTP check includes `upstream`; `restoreSql`/`db/scripts/drill-verify.mjs`'s `expectedFromRecord` treat a dump row with no `upstream_*` keys at all (pre-0005) exactly like present-and-NULL | `tests/drill/fetch-dump.test.ts`, `tests/drill/verify.test.ts`, `tests/drill/report-drill.test.ts`, `tests/drill/run-sh.test.ts` |
| LDB-E1 | Format edits are pure, identity on their own projection, and validity-preserving | `tests/formats/edits.test.ts` |
| LDB-F1 | Every stored payload validates against its format's frozen schema; a write that does not is refused with the failing path | `tests/formats/goldens.test.ts`, `tests/formats/mutations.test.ts` |
| LDB-F2 | `compileMagic()` (spark/1's compile step, was `lower()`) is deterministic across versions | `tests/formats/goldens.test.ts` (`.lowered.json` goldens) |
| LDB-F3 | Intent is never lowered on store | `tests/formats/intent.test.ts` |
| LDB-F4 | Lowering collisions are refused with both sources named | `tests/formats/collisions.test.ts`, `tests/formats/mutations.test.ts` |
| LDB-F5 | `cmini/1 → akl/1 → cmini/1` is identity on the projection (magic included) for every fixture; the live set's own identity check is P5's, whose projection is magic-less since M1 (design/layout-db/17-magic-ownership.md) -- the FORMAT-level round trip itself still includes magic, only the import/diff projection excludes it; `mana2/1 → akl/1 → mana2/1` is identity (modulo row whitespace + unused fingermap padding) for every vendored fixture; `akl/1 → mana2/1 → akl/1` is identity minus mana2's documented losses (magic idiom structure, `TB`, non-`x.mana2` `x`, an akl-native `ortho` board, non-contiguous columns) for every akl/1 fixture | `tests/formats/roundtrip.test.ts`, `tests/formats/mana2.test.ts`, `tests/api/list.test.ts`, S8 |
| LDB-F6 | Merged format majors are immutable | `tests/formats/frozen.test.ts` |
| LDB-F7 | Every format has ≥ 1 fixture and a frozen golden per declared translation; the unregistered cmini adapter (20-spark.md S1) carries the same bar, generated the same way -- `goldens.test.ts` loops over `validated-shapes.ts`'s shared shape list (every registered format plus the adapter), not `listFormats()` directly, so the adapter's own goldens (`validate`/`rows()`/`to["spark/1"]`=`fromCmini`/`to["mana2/1"]`) are covered generically again; the two directions no registered `to`/`from` map reaches -- spark/1 fixtures' `toCmini` (spark's own `to` no longer lists `cmini/1`) and mana2/1 fixtures' `toSpark`/`toCmini` (mana2/1's registry `to`/`from` are `{}`) -- are covered by dedicated blocks instead | `tests/formats/goldens.test.ts`, `tests/formats/validated-shapes.ts`, `tests/formats/mana2.test.ts` (X2: 74 vendored mana2/1 fixtures + `d5.jsonc`'s documented exclusion; its own "goldens (LDB-F7)" block for `.spark-1.json`/`.cmini-1.json`), `tests/formats/adapter-cmini.test.ts` (spark/1 fixtures' `toCmini` golden, `.cmini-1.json`) |
| LDB-F8 | `liftRules(compileMagic(m)) == (m, [])` for every valid idiom set; `compileMagic(lift(rows)) ≡ rows` for every typed row set; leftovers are exactly the rows that fail their tag's invariant | `tests/formats/lift.test.ts` |
| LDB-F9 | A held record keeps name/owner/rev and reads as its own format | `tests/api/held.test.ts` |
| LDB-F10 | `x` survives same-format round trips; only `x.cmini` survives `to["cmini/1"]` | `tests/formats/x.test.ts`, `tests/formats/roundtrip.test.ts` |
| LDB-F11 | Every live upstream detail (snapshot) validates as `cmini/1` and `hasMagic` matches upstream's `has_magic` | `tests/formats/cmini-envelope.test.ts` |
| LDB-F12 | The DB's `akl/1 → mana2/1` grid and thumb strings equal the site's real `bridgecore.ConvertLayout` wasm output for every `cmini/1` fixture (modulo trailing `skip`s and the fingermap digit under a `skip` cell) | `tests/formats/mana2-convert-parity.test.ts` against the frozen `tests/fixtures/mana2-convert/*.json` snapshot (`scripts/check-convert-parity.mjs` records/re-verifies it from the live wasm) |
| LDB-F13 | The set of vendored `mana2/1` layouts held for `akl/1` is enumerated with reasons; a new held vendored file fails until listed | `tests/formats/mana2.test.ts` |
| LDB-F14 | `spark/1`'s `compileMagic()` emits the site's word-start row: a magic key whose `default` is a literal character also gets `{inputs: ' '+key, output: ' '+default}` (web/src/core/rules.ts's `magicRulesFlatCompile`, I-153, is the source of truth for the condition -- `repeat_previous` gets none, `except` does NOT suppress it since the site has no such list, an explicit `magic_keys[].rules[]` entry for `after: ' '` replaces it same as any board char); the row is absorbed back into the idiom by `liftRules` (LDB-F8) and participates in collision detection (LDB-F4) like any other scaffold row | `tests/formats/magic-word-start.test.ts`, `tests/formats/lift.test.ts` (LDB-F8's own property test, which generates and round-trips literal defaults) |
| LDB-F15 | `spark/1`'s `compileMagic()` matches the site's compile on two more scaffold rules (`web/src/core/rules.ts`/`magicScaffold.ts` are the source of truth): (1) a magic key's board-char scaffold (`repeat_previous` or a literal default) excludes EVERY magic key's own char AND every chiral key's own char, one GLOBAL set shared across every key's scaffold, not just that key's own char; (2) a chiral key's scaffold enumerates EVERY layout key with a resolvable hand, the chiral key's OWN char included -- always same-hand, so it takes `same` (never `opposite`), producing a self row. A frozen row only the old (too-narrow) scaffold used to reproduce (a real historical row like auditor's `b*->bb`, 'b' its own magic key) still round-trips: `liftRules` promotes it into an EXPLICIT `magic_keys[].rules[]` override, which relowers tagged `magic` instead of `repeat`/`default:<c>` -- same (inputs, output), intentionally relabeled (LDB-F8's frozen-fixture and property tests tolerate exactly this relabeling, nothing else) | `tests/formats/magic-special-scaffold.test.ts`, `tests/formats/lift.test.ts` (`003-auditor` frozen-fixture case + LDB-F8's property test), `tests/formats/roundtrip.test.ts` (`cmini/1` snapshot half) |
| LDB-F16 | 20-spark.md S2: one stored format. Every accepted write resolves `body.format` through `resolveFormat` and stores the resolved module's OWN id (`module.id`), never the caller's literal -- an `akl/1` write stores `spark/1` byte-identical. A format whose `role` is `"output"` (`mana2/1`) is `400 format_not_writable`; `cmini/1` and anything unregistered are `400 unknown_format` listing registered ids only. Every write that carries an EXISTING record's payload forward (delete, restore, transfer, PATCH's normalization) stores `storedAsSpark(record)`, never the record's own literal format/payload, with `has_magic` recomputed from the converted payload -- otherwise a delete/restore/transfer/PATCH of an unmigrated legacy record would re-store its old format. **Extended by 20-spark.md S3b to the importer/strip system writers**: `applyNew`'s fresh imports and `applyMapped`'s case-4 carry-forward (unified into one branch -- the akl/1-only and cmini/1-only branches are gone) write `spark/1`, converting an existing legacy-stored record's payload through `storedAsSpark` first; `applyDelete`'s rev-bumping `upstream_deleted` tombstone does the same; `stripCminiMagic` writes `storedAsSpark` of the stripped payload, never the record's own `cmini/1` literal | `tests/api/format-write-matrix.test.ts` (registry matrix: verb × format), `tests/api/write.test.ts`, `tests/api/patch.test.ts`, `tests/api/restore.test.ts`, `tests/import/cases.test.ts` (import cases 1/4/8), `tests/import/strip.test.ts` |
| LDB-F17 | spark/1 stays lowerable: `to["mana2/1"]` never returns `held` for a valid spark/1 payload | `tests/formats/registry-aliases.test.ts` (every spark/1 fixture + random single-field mutations that still validate) |
| LDB-F18 | 20-spark.md S5 (19-upcast.md round 2 §8's F16, renumbered): chain contract. Every registered `<L>/<N>`, `N > 1`, has `<L>/<N-1>` registered and exports `up`/`down`/`edits`; `to`/`from` never name the module's OWN lineage (any major); over a fixture at `N`, `down(p)` is held or `up(down(p)) === p` (down is honest); over a fixture at `N-1`, `up(q)` never holds, validates at `N`, and `down(up(q)) === q` (up is injective). With only `spark/1`/`mana2/1` registered (each a one-major lineage), this is exercised entirely through a test-only stub lineage (`tests/formats/stub-lineage.ts`'s `t/1 -> t/2 -> t/3`), including deliberately-broken variants that prove `chainViolations` catches each missing piece (missing `up`/`down`/`edits`, a lineage gap, a self-lineage `to`/`from` edge) | `tests/formats/chain.test.ts` |
| LDB-F19 | 20-spark.md S5 (19-upcast.md round 2 §8's F17, renumbered): path composition. `translate()`/`path()`/`walk()` walk legacy-normalize -> chain -> pinned cross edge -> chain; the result is held iff any step is held; a `held` body's `see` names the record's own NATIVE format; `GET /v1/formats`' `can_translate_to` equals the set of formats `path()`/`hasEdge()` structurally reaches (same lineage always; a different lineage with a registered cross edge at ANY major, not just the format's own). Exercised through the stub lineage: `t/1 -> t/3` (up, 2 steps), `t/3 -> t/1` (down, 2 steps, held iff `b`/`c`), and a cross edge pinned at `t/2` -- a NON-LATEST major of `t` (whose latest is `t/3`) -- reaching a second stub lineage `u/1` | `tests/formats/path.test.ts` |
| LDB-F20 | Aliases: every alias is in one table (`db/formats/registry.ts`'s `ALIASES`), and so is every package alias subpath (`db/formats/package.json`'s `./akl/1`/`./cmini/1` exports) and the `?format=` list filter (`routes/layouts.ts`'s `resolveFormatFilter`). `akl/1` resolves (`resolveFormat`) to the SAME module `spark/1` does, byte-identical, and writes store `spark/1`. The wire `format` field is the record's NATIVE format everywhere -- the ONE exception (20-spark.md §1.12, refined §8 R-H3/R-L3) is a response to a request that named `akl/1`: `?as=akl/1` on detail/`/rev/{n}`/`full=1`, the write response (POST/PUT), and a `409 stale` body's embedded `record`, all carry `format: "akl/1"` even though the record is stored (and every other read shows) `spark/1`. `?as=cmini/1` resolves (`translate`) to the adapter's `toCmini` projection but is NEVER relabelled (it's a projection, not the same format); `cmini/1` is marked `write: "refuse"` (`400 unknown_format`) | `tests/formats/registry-aliases.test.ts`, `tests/tools/package.test.ts`, `tests/api/format-read-labels.test.ts` (the label rule: detail/full=1/rev, `?format=`), `tests/api/format-write-matrix.test.ts` (the deployed-bot path: write response + 409 stale), `tests/api/write.test.ts`, `tests/api/held.test.ts` |
| LDB-F21 | Legacy stored rows read as spark: `translate()` normalizes a record whose `format` is `akl/1` or `cmini/1` through `storedAsSpark` before resolving `as` -- every `as` (registered id or alias) then sees a `spark/1`-shaped record, so a legacy row reads exactly as its `storedAsSpark` twin on every route (`?as=` each registered id and alias, `/rev/{n}`, `full=1`), with no raw-identity shortcut even when `as` names the row's own legacy format back. `storedAsSpark` is the ONE converter of a stored legacy payload: every read of a legacy `layouts`/`layout_revs` row, and every write that carries a legacy record's payload forward (delete/restore/transfer/PATCH) | `tests/formats/registry-aliases.test.ts`, `tests/api/format-write-matrix.test.ts`, `tests/api/write.test.ts`, `tests/api/restore.test.ts`, `tests/api/patch.test.ts` |
| LDB-G1 | Restorable from a public dump + the public repo | `tests/rehost.test.ts` (daily job: against the real deployed dump) |
| LDB-G2 | No admin id is a constant in code (the migration seed is data) | `tests/tools/noconst.test.ts` |
| LDB-G3 | `.github/CODEOWNERS` is generated from every format's `OWNERS` file (one line per format major, no format missing, no stale handle) | `tests/tools/codeowners.test.ts` |
| LDB-G4 | Every binding/var the Worker reads is in the runbook table | `tests/tools/runbook.test.ts` |
| LDB-G5 | Nothing imports across the `db/` boundary in either direction | `tests/tools/boundary.test.ts` |
| LDB-G6 | The split repos (`git filter-repo`'d clones of `db/` and `bot/`) test green with no edits: `repoLayout()` resolves the right prefix and workflow file inside either shape | `tests/tools/ciwiring.test.ts` ([LDB-G6] case), `.github/workflows/db.yml`'s `split-dry-run` job (`scripts/split/split-db.sh --dry-run`) |
| LDB-G7 | `@akl/layout-formats` packs every format's built entry and schema.json and imports cleanly from a clean install; its `exports` map is derived from the registry, so a fourth format cannot be forgotten | `tests/tools/package.test.ts` |
| LDB-G8 | `db/INTEGRATION.md`'s error-code appendix is generated from `src/core/errors.ts`'s own `ApiError` factories (`db/scripts/gen-error-table.mjs`); the integration guide's error table cannot drift | `tests/tools/error-table.test.ts` |
| LDB-G9 | The docs hub is generated fresh on every build (never checked in -- design/layout-db/*.md is edited too often for a committed copy to stay current) and cannot drift from its own sources: every `design/layout-db/*.md` and `db/docs/*.md` is in the hub, and nothing else is; every page's navigation lists every doc exactly once, in reader order; every internal link and every "Markdown for agents" link resolves; `all.md` equals every doc's markdown copy concatenated in nav order; each page's `.md` copy equals the builder's own render of its source; two builds are byte-identical. `npm run build`'s assemble step produces `dist/layoutdb/index.html`. Skips once `db/` splits out (design/layout-db/ does not exist there) | `tests/tools/docs-site.test.ts`, `web/tests/tools/assembledist.test.mjs` |
| LDB-H1 | Webhook delivery is at-least-once and in order per hook: every matching event past a hook's cursor is POSTed with a valid signature before the cursor passes it; the cursor is advanced only by compare-and-set | `tests/api/webhooks.test.ts` |
| LDB-H2 | The stream is the feed: the frames of any stream, and of any chain of streams reconnected by `Last-Event-ID`, are exactly `/v1/changes`' items past the original `since`, in order, no gap, no duplicate; every stream closes at the bound with `next` | `tests/api/stream.test.ts` |
| LDB-H3 | The changelog page shows exactly the feed's events for its parameters, with every interpolated value HTML-escaped | `tests/api/changelog.test.ts` |
| LDB-H4 | A webhook secret never leaves the `webhooks` table: no response body, no event, no dump carries it | `tests/api/webhooks.test.ts` (scans), `tests/api/dump.test.ts` (`webhooks: []`) |
| LDB-H5 | A drain with nothing to deliver writes zero D1 rows; a delivered batch writes exactly one `webhooks` row per hook | `tests/api/webhooks.test.ts` (statement counter) |
| LDB-I1 | The import is idempotent: the same upstream state twice appends zero events | `tests/import/tick.test.ts` |
| LDB-I2 | The import never overwrites a record that does not follow upstream | `tests/import/cases.test.ts` |
| LDB-I2a | "Follows upstream" ⇔ the record's latest rev-bumping event, MAGIC-ONLY events excluded (LDB-I12), has `via = import:cmini`. **Narrowed by 20-spark.md S3a**: this is now the LEGACY rule alone, realized by `core/follows.ts`'s `legacyFollows` (renamed from `followsUpstream`; also skips `migrated` events, S4's own writes) -- consulted only by `core/upstream.ts`'s `upstreamOf` (its fallback for a record whose `upstream` field is still null) and by the S4 migration. Every other caller reads `upstreamOf(...).state === "following"` instead (LDB-I14) | `tests/events/follows.test.ts`, `tests/api/restore.test.ts` |
| LDB-I3 | Tombstoning more than `max(5, 5%)` of live records in one tick stalls the import instead | `tests/import/plan.test.ts` |
| LDB-I4 | Every import event carries `via = import:cmini` and `actor = system:cmini-import` (likes: the liking user) | `tests/import/cases.test.ts` |
| LDB-I5 | Imported names are stored verbatim (case kept, `check_name` not applied) and are unique case-insensitively | `tests/import/cases.test.ts` |
| LDB-I6 | A list shorter than half the live record count stalls the whole tick | `tests/import/plan.test.ts` |
| LDB-I7 | A tick whose `/meta` token is unchanged makes no further request and writes nothing | `tests/import/tick.test.ts` |
| LDB-I8 | Every upstream request carries the UA; 404 is never retried; other failures are retried 3x | `tests/import/upstream.test.ts` |
| LDB-I9 | Upstream JSON is parsed only through `core/safejson.ts`: Go's `\u003c`/`\u003e`/`\u0026` escapes are rewritten to literals before `JSON.parse` (a reproduced V8 bug decodes escaped object keys non-deterministically on the ~5 MB `?full=1` body, in Node and in workerd), an escaped backslash is never touched, and the parse is checked against a second parse | `tests/core/safejson.test.ts`; `tests/import/upstream.test.ts` |
| LDB-I10 | M1 (design/layout-db/17-magic-ownership.md): an imported payload never carries `magic` -- dropped from upstream's detail before validation (`import/apply.ts`'s `payloadFromRaw`), for `applyNew` and `applyMapped` alike; `has_magic` still derives from the format's `hasMagic(payload)`, which for a freshly imported record is now always false. `POST /v1/admin/import/strip-cmini-magic` (same shape as `.../import/tick`) extends this to records imported before this landed: a one-time, batched, idempotent pass that writes away any live, upstream-following record's legacy cmini magic as an `imported` rev bump (`detail.reason: "magic_stripped"`) | `tests/import/cases.test.ts`, `tests/import/strip.test.ts`, `tests/api/admin.test.ts` |
| LDB-I11 | M1: an import write preserves the record's own `magic` -- `applyMapped`'s case-4 write carries the EXISTING record's `magic` into the new payload, since upstream's own is never present to begin with (LDB-I10). **Amended by 20-spark.md S3b**: comparison and carry-forward both moved to spark. `import/apply.ts`'s `contentDiffers` compares `{name, owner, created_at, modified_at, payload}` with `magic` dropped on both sides -- upstream's detail run through `fromCmini`, the record's own (possibly legacy-stored) payload through `storedAsSpark` first (LDB-F21) -- and the D12 diff's `import/diff.ts` `compareRecords` drops `magic` the same way over its own spark projection; neither upstream adding/changing/removing magic nor a followed-or-not record's own magic is ever mistaken for a content difference. The akl/1-only and cmini/1-only case-4 branches are UNIFIED into one: the existing payload is normalized through `storedAsSpark` first (a genuinely `cmini/1`-stored record's flat magic rows are therefore LIFTED to spark's own idiom the first time case 4 touches them, via `fromCmini`; byte-for-byte from then on, since the record is `spark/1` thereafter) | `tests/import/cases.test.ts`, `tests/import/diff-unit.test.ts` |
| LDB-I12 | **Narrowed by 20-spark.md S2 (decision 6): magic edits fork like any other write now.** `isMagicOnlyReplace` and the `detail.magic_only` marker are DELETED -- no new write (PUT or PATCH) ever writes `magic_only` again, and a magic-only write bumps `modified_at` and forks unconditionally, same as any other edit. This row is now the definition of `legacyFollows` (S3a's rename of the pre-S2 `followsUpstream`), used only by historical-event reads: `core/follows.ts`'s `legacyFollows` still walks back past every rev-bumping event carrying `detail.magic_only: true` (written before this slice) OR `kind: "migrated"` (S3a addition -- S4's own writes must not become "the latest write" for the legacy rule either) to the first one that is neither before reading its `via` (LDB-I2a amended) -- the 67 M2-seeded records depend on this to come out following. Nothing else reads the marker | `tests/events/follows.test.ts` (historical `detail.magic_only` and `migrated` events, constructed directly, still skipped), `tests/api/patch.test.ts`, `tests/api/write.test.ts` (no NEW write ever emits the marker; a magic-only write forks) |
| LDB-I13 | 20-spark.md S3b: the cmini adapter is exact for import purposes -- for a following record, `fromCmini(upstream detail)` equals the record's own spark payload minus `magic` (replaces the pre-spark cmini/1-level F5/F11 wording, which compared cmini/1 to cmini/1 directly); every LIVE upstream detail's `fromCmini` conversion validates as spark (`spark.validate`), not just as `cmini/1` -- a detail that's schema-valid per cmini/1 but fails spark's own (stricter: magic semantics, collisions, the `x` size cap) validate is reported as `invalidUpstream`, never thrown, by both `parseUpstreamRaw` (`import/diff.ts`, the D12 diff) and the live daily job over real upstream data | `tests/formats/cmini-envelope.test.ts` (every upstream-100 detail's `fromCmini` validates as spark), `tests/import/diff-unit.test.ts` (`parseUpstreamRaw` reports a cmini-valid/spark-invalid detail), `tests/upstream-diff.test.ts` (daily, live) |
| LDB-I14 | Upstream write rule, one function (`core/upstream.ts`'s `nextUpstream(prior, kind, via)`): `migrated` never changes it (initial value = the legacy rule, computed once by S4); `null` stays `null` (a write with no known upstream link never acquires one); otherwise an import-system write (`via: "import:cmini"` -- create, update, revival, the rev-bumping `upstream_deleted` tombstone, strip) sets/keeps `following`, and every user rev-bumping write (PUT, PATCH incl. `{magic}` per decision 6, rename, fingermap, transfer, delete, restore) forks (`state: "forked"`, same `source`/`id`); the importer never writes a `forked` record (S3b's own call sites are gated on `following` first, and every one guards its write with `expectRev`, LDB-P14). `prior` is `upstreamOf(db, rec)`: the record's own `upstream` field when non-null, else the legacy fallback (`legacyFollows` + `import_map`). **Corrected 2026-09-10** (S4 review, a migration non-termination bug this slice found): an `import_map` row IS the link -- `state` is `following` when `legacyFollows` says so, else `forked` (e.g. import case 2's same-owner name clash, or any imported record a user has since edited); `null` means only "no `import_map` row at all", never "mapped but not following" (superseding R-L6's "the legacy model can't tell those apart" reasoning -- it doesn't need to: the row already answers which one it is) | `tests/core/upstream.test.ts` (pure `nextUpstream` matrix: kind × prior {null, following, forked} × via), `tests/events/upstream.test.ts` (`upstreamOf`'s field-vs-fallback precedence, the `import_map` {yes, no} dimension, mapped-but-not-following resolving to `forked`) |
| LDB-L1 | Likes move `like_count`, `likes` and `meta.revision`/`seq` only -- never `rev`, `modified_at` or `layouts_modified_at`; concurrent likes are counted exactly | `tests/api/likes.test.ts` |
| LDB-M1 | `/v1/meta.last_diff` and `.last_drill` are `{at, ok}` written on every run including failures; `/v1/meta`'s ETag folds both `at`s in so a poller can never get stuck on a stale 304 | `tests/import/difftick.test.ts`, `tests/api/admin.test.ts` |
| LDB-N1 | `check_name` is the bot's rule set with the bot's strings (`NAME_SET` minus the space), plus the 64-char cap and the ULID-shape refusal, applied to `POST`, rename, and (20-spark.md S2) restore's optional `{name}` | `tests/api/names.test.ts`, `tests/api/patch.test.ts`, `tests/api/restore.test.ts` |
| LDB-P1 | Every write appends exactly one rev-bumping event and one `layout_revs` row; the record equals the fold of its events; `seq` is gapless | `tests/events/fold.test.ts`, `tests/events/races.test.ts`, `tests/tools/onlywriter.test.ts` |
| LDB-P2 | An `If-Match` mismatch writes nothing and returns the current record; two writes at one `rev` → exactly one commits, the other gets `stale` with the winner's record; the guard is `layout_revs`' PK inside the batch. An absent `If-Match` on an existing record (`PUT`/`PATCH`/`DELETE`/`transfer`) is refused with `400 if_match_required`, never treated as a blind overwrite -- checked before any read or mutation (`*` still means "overwrite on purpose", but the client must say so explicitly) | `tests/api/ifmatch.test.ts`, `tests/events/fold.test.ts`, `tests/api/write.test.ts`, `tests/api/conformance.test.ts` |
| LDB-P3 | A follower's state built from webhook deliveries alone (with drops, reordering and duplicates) equals its state built from the feed alone | `tests/events/feed.test.ts` |
| LDB-P4 | A name is released only by delete or rename | `tests/events/names.test.ts`, `tests/events/races.test.ts`, `tests/api/refs.test.ts`, `tests/api/names.test.ts`, `tests/api/patch.test.ts` |
| LDB-P5 | Every following record read equals upstream on the projection (likes sorted, magic excluded since M1 -- design/layout-db/17-magic-ownership.md §3; LDB-I10/I11 own why). **Amended by 20-spark.md S3b**: the D12 diff compares exactly the records whose `upstream.state === "following"` (read straight off each side's own `upstream` field, never resolved from an event's `via` any more -- the old `OursSource.followsUpstream`/`/history` lookups are deleted); a name-matched local record that's `forked` or has no upstream link at all is reported as `divergent` (informational, never a failure -- a forked record is by definition allowed to differ). Comparison happens in spark now: `d1Ours` yields `storedAsSpark` payloads, `httpOurs.full` reads `?full=1&as=spark/1`, and `parseUpstreamRaw` runs upstream's detail through `fromCmini` (LDB-I13 owns its own validate half) | `tests/upstream-diff.test.ts` (daily, live), `tests/import/diff-unit.test.ts` (unit half, over `upstream-100`) |
| LDB-P6 | `/v1/changes` serves from `since=0`, including after a restore | `tests/events/feed.test.ts`, `tests/rehost.test.ts` |
| LDB-P7 | Every error response carries `error` and `message`; every (route, status) pair has a conformance case | `tests/api/conformance.test.ts` |
| LDB-P8 | A tombstone is unreadable by name from the moment of deletion (phase 1) and restorable by id AT ANY TIME by its owner or an admin (20-spark.md §1 decision 8: the 30-day owner window is gone), keeping history; format/payload carry forward through `storedAsSpark` (LDB-F16/F21), not verbatim. The restore body is optional (decision 9, refined §8 R-L1): absent, `{}`, or `{name}` (any other key is `400 bad_request`); without `name`, restoring under a reclaimed name is `409 name_taken` with `holder`; a `name` goes through `check_name` (LDB-N1 amended) and, when it differs from the tombstone's own, the `restored` event carries `detail: {renamed_from}` -- both records keep their own likes | `tests/api/refs.test.ts`, `tests/api/restore.test.ts` |
| LDB-P9 | design/layout-db/18-command-decisions.md §2 D1 (saltorbit: "tombstoned name carries likes for whoever takes it. it's a quirk people like"): `POST /v1/layouts` whose name is currently held by a tombstone -- ANY owner, not just the same actor -- copies the tombstone's likes onto the new record as `liked` events `via: "name_inherited"`, `detail: {from: <tombstone id>}`; the tombstone keeps its own likes/history untouched and stays restorable (which then leaves both records carrying the same users' likes -- accepted); no tombstone for the name inherits nothing; `has_magic`/payload are unaffected | `tests/api/write.test.ts` |
| LDB-P10 | design/layout-db/18-command-decisions.md §2 D2: a rename (`PATCH {name}`) never loses the id -- the record's id, rev chain, likes, history and `_dbId`-equivalent projection are unchanged and reachable by the new name AND by id; the old name is freed (LDB-P4 owns that half) | `tests/api/patch.test.ts` |
| LDB-P11 | `upstream` is a fold (20-spark.md S3a): the row's value equals the latest rev-bumping event's `after.upstream`, through any number of transitions (`appendWrite` writes it into `after` and the `layouts` upsert alike, so the two can never disagree); a restore of an old-shape dump (no `upstream_*` columns at all) yields NULL on every row, and `upstreamOf` then reproduces exactly the legacy rule (LDB-I2a/I12) for it | `tests/events/upstream.test.ts` (fold + old-shape-row property), `tests/drill/verify.test.ts`, `tests/rehost.test.ts` (dump/restore round trip) |
| LDB-P12 | Record migration (20-spark.md S4, `core/migrate.ts`'s `migrateTick(db, now, {dryRun, after, limit})`): selection is `format` in {every `LEGACY_STORED` id, every registered major of the stored lineage BELOW its current latest} (live and deleted) OR (`upstream_state IS NULL` AND an `import_map` row exists) -- the second arm backfills a record a write already stored at latest between the 0005 deploy and this run. **Generalized by 20-spark.md S5** from a hardcoded `format != 'spark/1'`: the target is `storedLatestId()` (the ONE registered `role: "stored"` lineage's CURRENT latest, read from the live registry every call, never cached), so the same selection/conversion keeps working the moment a `spark/2` (or, in tests, a stand-in extending the real `spark` lineage) registers -- proved by a dedicated test that registers such a stand-in, converges a `spark/1` record to it, then unregisters it and confirms the selection reverts to plain `spark/1`. A legacy-stored record (`cmini/1`/`akl/1`) normalizes through `storedAsSpark` FIRST (never a chain step), then chains up the rest of the way if latest is past `spark/1`; a record already at a registered lower major of the stored lineage chains up directly. Per record: the converted payload validates against the LATEST module (not hardcoded spark/1); a failure (or a computed `hasMagic()` disagreeing with the stored `has_magic`) writes nothing and lands in `invalid` (the record keeps reading through LDB-F21); otherwise `appendWrite({kind: "migrated", expectRev: record.rev, ...})` with `modified_at`/`created_at`/`deleted`/likes unchanged and `upstream` = `upstreamOf` (the legacy rule when the field is null, decision 7: the 67 M2-seeded records compute as `following` because that rule skips magic-only events, `legacy_magic_only_following`'s own count) -- a `RevConflictError` is caught and counted `raced`, never thrown, and the record is re-selected by a later tick. Dry run: identical selection/conversion, zero D1 writes, the same report shape. `≤ 100` records per call (`next_after` pages); a quiet tick (nothing left to convert or backfill) writes nothing. **Termination depends on LDB-I14's 2026-09-10 correction**: a mapped-but-not-following record resolves to `forked` (non-null `upstream_state`), not `null` -- had it stayed `null`, the arm-2 backfill selection (`upstream_state IS NULL AND` mapped) would re-select and re-`migrated` that record forever, and the operator script's real-run loop (`until converted == 0 && raced == 0`) would never converge | `tests/events/migrate.test.ts` (fake clock, a seeded mixed store: a following `cmini/1` record, a `cmini/1` tombstone, an `akl/1` record with the 67's own magic-only-history shape, a `spark/1` record with null upstream and an import_map row, one adapter-invalid legacy payload; dry run via a `.batch()`-counting proxy; a quiet second tick; a `RevConflictError` deterministically forced via a `readById` spy; a mapped-but-not-following record and an imported-then-user-edited record both converge to `forked` in one tick, a second tick selecting nothing), `scripts/tests/test_migrate_records_to_spark.py` (the real-run loop terminates) |
| LDB-P13 | 20-spark.md S5 (19-upcast.md round 2 §3/§7's P11, renumbered): older-major write. A `PUT`/`POST` naming a format `F` not the record's lineage's latest is stored at the lineage's latest with `detail.written_as = F`; a `PUT` in `F != record.format` is refused `409 format_behind` (same body shape as `held`: `held: true, format, see, rev`) iff `translate(record, F)` is held (R1: the client could never have read the record whole in `F`, so a pure upcast would be a blind overwrite) -- checked BEFORE `validatePayload`, and only when `F` differs from the record's current format; after an accepted write, `GET ?as=F` returns exactly what was written; under any sequence of writes the record's stored major never decreases. `POST` in an older major is R2's chain with no R1 (there is no prior record to have been unreadable). Exercised through the stub `t/1 -> t/2 -> t/3` lineage over real HTTP writes (`spark/1`/`mana2/1` are each still one-major) | `tests/api/put-format.test.ts` (matrix over registered pairs × {held, not}; property test over random write-sequence × format × payload-risk, asserting the stored major never decreases) |
| LDB-P14 | `appendWrite`'s optional `expectRev` (20-spark.md S3a, §8 R-H4): when set, the write commits only if the record is STILL at that rev at the moment it runs -- checked against a fresh re-read, before any write, and refused as `RevConflictError` otherwise. Every system writer (the importer, strip, the S4 migration) passes it, built from the SAME read its payload was computed from; a user write interleaved between that read and the system writer's own write survives untouched, and the record stays at whatever state the user write left it in (LDB-I14's "the importer never writes a forked record" depends on this: without it, a stale system write could silently land on top of a user's fork). **Extended by 20-spark.md S3b (the importer's own case)**: `import/cmini.ts`'s `tick()` catches a thrown `RevConflictError` per id -- both in the fetch/apply loop (`applyFetchedId`) and the delete loop (`applyDeleteAction`) -- counts it in `TickStats.raced`, and moves on; it is never an uncaught exception that would abort the whole tick (and every OTHER id still queued behind it). The next tick re-evaluates the raced id from a fresh read | `tests/events/upstream.test.ts` (deterministic race case + a `fast-check` property over random interleaving counts), `tests/import/tick.test.ts` (`tick()`'s own per-id catch-and-count, both loops) |
| LDB-P15 | 20-spark.md S3s (decision 14): every write states its `source: {client, version}` (`Write`/`Info`/`Like` all require it -- tsc enumerates every call site). `client` is PROVEN, never declared: `client:<id>` on the client lane (`Actor.source_client`, the signature's own client id), `discord-app:<app id>` on the user lane (the Discord OAuth2 app the bearer token was issued to), `system:cmini-import`/`system:migration` for system writers -- no header or body field can set it (the spoof matrix). `version` is the validated `X-Client-Version` header (parsed once in `requireActorOnWrites`, ≤ 64 chars of `[A-Za-z0-9._+/:-]` else `400 invalid_client_version`) or `null`; it never influences `client`. `appendWrite` stores `source` on the event AND folds it onto the `layouts` row (rev-bumping only); `appendInfo`/`appendLike` store it on the event only, never folding it onto `layouts` (mirrors how they never move `upstream`/`rev` either). `RecordRow.source`/`toWire`/`sansPayload` carry the fold (`before`/`after`, `/v1/changes`, webhooks, list, `full=1`); `/history` and `/rev/{n}` return each event's OWN source, computed off that event's row (`sourceOfEvent`) so a pre-0005 event (no `source_client` column) reads `{client: "legacy:" + via, version: null}` with no extra query; a pre-0005 record (`layouts.source_client` still NULL) reads `source: null` on every route (no per-record legacy synthesis, unlike `upstream` -- a `layouts` row has no `via` to fall back on) | `tests/api/source-matrix.test.ts` (lane × verb × version-header matrix; the spoof matrix: no header/query/body field sets `client` on either lane), `tests/events/source.test.ts` (replay property shared with P11: the record's `source` equals its latest rev-bumping event's `after.source`), `tests/auth/discord.test.ts`, `tests/api/history.test.ts`, `tests/drill/verify.test.ts` |
| LDB-R1 | Polled routes carry `Cache-Control` + strong `ETag` and answer `304` to a matching `If-None-Match` -- compared weakly per RFC 7232 §3.2, so the `W/"…"` form Cloudflare rewrites every compressed response's ETag to (what a client that echoes what it received sends back) matches too (2026-09-10: the bot's per-minute heartbeat never got a 304 before this); the ETag changes iff the event head, the query, OR (20-spark.md S2) the wire shape changes -- a `WIRE_VERSION` constant is folded into the hashed query so a pre-deploy `If-None-Match`/edge-cached body can't keep serving the old shape at an unchanged head seq | `tests/api/etag.test.ts` |
| LDB-R2 | `/v1/meta` counts and `seq`/`revision` equal the tables | `tests/api/meta.test.ts` |
| LDB-R3 | The conformance fixtures are the API contract; changing one is a documented API change | `tests/api/conformance.test.ts` (+ review) |
| LDB-R4 | Every `sort` × `limit` cursor walk visits every live record exactly once | `tests/api/list.test.ts` |
| LDB-R5 | `/rev/{n}` reproduces the payload stored at rev `n` for every n | `tests/api/history.test.ts` |
| LDB-R6 | Writes are limited to 60 per 10-minute window per actor, counted per attempt, `429` + `Retry-After`; reads are never counted | `tests/api/ratelimit.test.ts` |
| LDB-R7 | Client-lane writes are limited to 300/10min per client, on top of the 60/10min per-actor limit that applies to every lane; a 429 names which counter tripped (`scope`) | `tests/api/ratelimit.test.ts` |
| LDB-R8 | `GET /v1/layouts?liked_by=<user_id>` equals a filter over `likes`, composable with every other filter/sort, and rides on `?full=1` | `tests/api/list.test.ts` |
| LDB-S1a | `db/tests/fixtures/db-responses/` (site-side sync fixture, design/layout-db/11-implementation-phase3.md §1 W1) equals the live `/v1/meta`, `/v1/layouts`, `/v1/layouts?full=1&as=cmini/1`, per-name `/v1/layouts/{name}?as=cmini/1`, `/v1/layouts/{name}/likes` and `/v1/authors` routes over the standard upstream-100 seed | `tests/api/fixture-export.test.ts` |
| LDB-T1 | Every registry id has a tagged test and every tag has a registry row | `tests/tools/invariants.test.ts` |
| LDB-W1 | Every write route is resolve → authorize → check → `appendWrite`; no file under `src/routes/` prepares a D1 statement | `tests/tools/routes-noprepare.test.ts` |
