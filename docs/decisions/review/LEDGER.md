# layoutdb v2 — implementation ledger (pick up here)

**For whoever picks this up.** Read in order: `REQUIREMENTS.md` (binding), `PROPOSAL.md` (the target shape, with saltorbit's answers in §6 and the safety nets in §3c), then this file. Integration branch: **`ldb-arch-review`** (from `ldb-formats`; everything layoutdb lands here, never on `main`). Site preview of the docs: `https://db.cmini-web.pages.dev/artifacts/`. Started 2026-09-12.

## Standing rules (saltorbit)

- Correctness > simplicity > latency > cost. Hard requirements H1–H23 in `REQUIREMENTS.md`.
- layoutdb is disposable until the community uses it: wipe + rebuild over migrations; keep pushing to prod `akl-db` on every push (`db.yml`), no release gate yet.
- akl.gg keeps its own Discord auth; the layoutdb website is a separate client; the DB validates tokens, mints none.
- Prod akl.gg (Pages prod env, `cb-magic`, akl.gg zone, `main`'s workflows) is fenced. Nothing merges to `main` without saltorbit.
- Every addition states its invariants and tests them (`db/INVARIANTS.md`, `bot/INVARIANTS.md`, catalog rows). User-facing copy (bot replies, site strings) is a `// COPY: sign-off pending` stand-in until saltorbit signs it.
- Cheaper agents implement (Sonnet by default); the lead reviews every diff. **Agents run in parallel, each spawned with the Agent tool's `isolation: "worktree"`** (cwd set to its own `.claude/worktrees/<name>` at spawn, branch `worktree-<name>`, so no agent ever `cd`s and the session's worktree pin never moves — the 2026-09-09 livelock was cd/EnterWorktree-based). Because that worktree branches from `main`, an agent's first command is `git reset --hard ldb-arch-review`, then `npm ci`. Lead's cross-worktree git goes through scratchpad scripts (`lead-commit.sh`, `land-slice.sh`).
- Sync the live bot / db site only at natural pause points.
- **saltorbit, 2026-09-12: "implement this new way fully. don't merge to main or disrupt existing shipping aklgg, but otherwise you should push updates to the db etc."** Prod layoutdb deploys, spark deploys and the db-alias site are ours to push as slices land.

## Decisions taken 2026-09-12 (beyond PROPOSAL.md §6)

| decision | detail |
|---|---|
| feed = long-poll | `GET /v1/changes?since=&wait=25s`; Worker checks D1 1/s, returns on first event; `wait=` only for registered Ed25519 clients with cap `feed:wait`, ≤ 25 s, counted against the client rate limit; anonymous → immediate answer. Only spark holds it; akl.gg reads static objects. SSE + webhooks deleted. |
| host = Fly | shared-cpu-2x 2 GB; revisit with the per-verb latency histogram. |
| rebuilds | none weekly. Incremental per edit; daily fold/GC in spark; full harvest on GitHub only when `H` changes. |
| safety nets | S1 replica-vs-dump (daily + boot), S2 coverage walk (daily + boot), S3 publish smoke test (part of every publish), S4 daily spot recompute (edited-24h + 30 random, magic always sampled; wasm vs CLI), S6 monthly reference harvest diffed into a scratch line. S5 deferred. Every check writes last result + time to spark `/health`; not run in 48 h = failure; watchdog DMs. |
| likes | union with cmini's by user id; every like carries `via`; importer never emits `unliked`. |
| tombstones | never match in any resolver, fuzzy `view` included. |
| engines | native mana2 CLI publishes; wasm answers; nightly parity sample. |

**2026-09-13 ~09:15Z — geometry/format round LANDED and CUT OVER (cmini-web-19).** `ldb-geometry` fast-forwarded into `ldb-arch-review` (b64c1c7c2): spark/1 = `board` one word (ansi|iso|ortho|colstag), `keys` an ordered list with duplicates, no `TB`/`free`/`notes`, kind-tagged magic sentinels, WIRE_VERSION 6 (LDB-F27..F33, P24); site codec/drawers/publish chips (I-383..387); bot add/board/fingers verbs, text looks, SNAPSHOT_VERSION 4 (LDB-B230..B235); design/layout-db/23-geometry.md + 24-spark-wire-review.md. Prod: wiped + re-imported (4,177; seq 45251; dump forced via the new `POST /v1/admin/dump`), magic re-seeded from akl.gg's 82 rule sets via `POST /v1/admin/magic-seed` (system lane, all `following`). Two lessons: Cloudflare Smart Placement pinned the prod hostname to the version it was enabled with (two later deploys served old code until `[placement]` was removed — never re-enable on akl-db without a version-preview check); stop the old bot BEFORE the wipe. Open follow-ups: L8 (batched writes), L9 (bridge thumbs by label), I1 (identity by id in clients), spec page rewrite per 24's skeleton, #333.

**2026-09-13 — no-board round (saltorbit: "remove the board type from spark/1 ... and from akl.gg on publish").** `design/layout-db/26-no-board.md`: spark/1 = `{keys, magic?}`, no `board`; the iso width rule, `coords`/`STAGGER_BY_KIND`/`cminiBoardWord`, `PATCH {board}` and `setBoard` deleted; `fromCmini` drops cmini's word; `toSpark` drops the mana2 board (908 no longer held); `fromSpark` always emits the ANSI row stagger (`DEFAULT_ROW_STAGGER`); WIRE_VERSION 12 (CHANGELOG 1.12); migration 0016 strips stored rows in place (no wipe); LDB-F40 added, LDB-F31 retired, F5/F23/F24/F27/F30 restated; `frozen.test.ts` back to D11's explicit (empty) FROZEN list. Site: publish sheet board picker + `toAkl1`'s `board` gone. Bot: `board`/`board!`, `add`'s board words, `fingers!`'s ansi rule, the iso out-dent gone; engine/overlay use the fixed `stagger` word.

**2026-09-13 — magic-emit round (xsznix: "`after: a, output: ab` mixes context and output"; saltorbit: "(ngram-in, ngram-out)").** `design/layout-db/27-magic-emit.md`: `magic_keys[].rules[]` is `{after, emit}` (row `after+key -> after+emit`; `after` an n-gram; the old `{after, output}` refused by the schema); `liftRules` leaves a context-rewriting row raw; reseed converts on the way in; migration 0017 rewrites stored rows in place; WIRE_VERSION 13 (CHANGELOG 1.13); LDB-F41. Site converts at the codec (workbench still authors `{after, output}` -- the editor change is a later step); bot follows via `@akl/core/rules` + re-vendored parity vectors.

## Waves and slices

Status: `todo` · `running (agent, branch)` · `review` · `landed <sha>` · `blocked (why)`.

### Wave 0 — docs (lead)
| id | slice | status |
|---|---|---|
| D0 | this ledger + REQUIREMENTS + PROPOSAL + data-flow page on the db preview | landed 89312ddb6 |
| D1 | layoutdb-focused architecture doc for outside reviewers (common infra only; spark/1 format flagged as still open; format rules table) | landed: `design/artifacts/layoutdb-arch.html`, /artifacts/layoutdb-arch/ |
| D2 | ecosystem page rewritten to the new target shape; artifacts index accurate; every SVG measured in Chrome for clipping | landed |
| D3 | retire `design/layout-db/00–22`, `design/pipeline-313/*` to `historical/` with one pointer; `REQUIREMENTS.md` becomes the standing file; CLAUDE.md pointer | todo (after D1) |
| S1a | spec/code discrepancies -- moved verbatim from `design/layout-db/22-spark-spec.md`'s own "Open discrepancies" section during the adoption-docs history-purge pass | For the lead to decide; not resolved by this rewrite. 1. **`keys[].char` single-code-point-ness is not checked by `validate()`.** `schema.json:23` leaves `char` an unconstrained string, and `index.ts`'s `validate()` never calls `isSingleChar` over a plain `keys` entry; `isSingleChar` (`magic.ts:37-39`) is only ever invoked from `validateMagicSemantics`, which walks `magic_keys`/`chiral_keys`/ `adaptive_swaps`, never `payload.keys` itself. A payload with `{char: "ab", row: 0, col: 0, finger: "LP"}` (or `char: ""`) validates today as long as nothing in `magic` references it. §2's normative rule ("a `char` is one code point") describes the design intent and the enforcement that exists for magic-referenced characters; it is not enforced for an ordinary key. No fixture or test proves either the presence or the absence of this check. 2. **No request-body size cap exists.** `design/layout-db/24-spark-wire-review.md` round 2 §H1 asked for a stated cap (64 KiB of canonical bytes, "an order of magnitude over every fixture"), with its own error code and fixture, before the first outside adopter. Nothing in `db/src` bounds a `POST`/`PUT` body, `keys.length`, or `rules.length` (checked: no `MAX_BODY`/`bodyLimit`/byte-length check anywhere under `db/src`). 3. **`from=<lineage>` is not reserved in code.** Round 2 §B asked the server to accept `from=spark` on `?format=mana2/1` as a documented no-op and refuse any other value with `400 bad_request`, so a client could start sending the parameter now. `db/src/routes/layouts.ts` reads no `from` query parameter anywhere; `db/docs/adoption.md:932` still only says the parameter is "not designed yet." 4. **Error paths into `keys` are not `/keys/<i>`.** Round 2 §D said "every error path into `keys` indexes the request body as sent" (i.e. `/keys/<i>`), and §C's resolved rule for finding 5 explicitly names `/keys/<i>` as the path for `magic_needs_unique_key`. As implemented, both `validateMagicKeysUnique`/`validateChiralHandAmbiguity` (`index.ts:218-260`, `:163-210`) and `setFingermap`'s duplicate-char refusal (`edits.ts:28-41`) report the bare path `/keys`, naming the offending character in the message instead. `mutations.test.ts:402` pins the bare `/keys` path, so this is the tested, intentional current behaviour, not an oversight in one spot; it just does not match the round-2 text. 5. **`reserved_rule_type` has no dedicated test, fixture, or invariant id.** The refusal itself is implemented (`magic.ts:777-797`, round 2 §H3's ask), but `grep -a -rln "reserved_rule_type" db/tests` finds nothing, and `db/INVARIANTS.md` has no row for it. A client-supplied `rules[].type` of `"repeat"`, `"magic"`, `"chiral"`, `"adaptive"`, or a `default:<c>` shape is refused today, but nothing in CI would catch a regression that silently stopped refusing it. 6. **`db/INVARIANTS.md`'s LDB-F22 row text is stale.** It still reads "`notes`/`updated` are strings" (line 113); round 2 item A dropped both fields entirely, and the schema now refuses them (`additionalProperties: false`). The rest of LDB-F22's claim (named keys need not be on the layout; a null chiral value reads as absent) still holds and is still the citation this page uses for §3's "zero is fine" rule; only that one clause is out of date. 7. **The served schema's own strictness is in tension with "readers ignore unknown fields."** §9 tells a client to validate against `GET /v1/formats/spark/1/schema.json` (`db/src/routes/formats.ts:44-52` serves the raw ajv schema, `additionalProperties: false` everywhere). A client that does exactly that will reject a payload carrying any field added after the client last fetched the schema, the same problem `design/layout-db/24-spark-wire-review.md` finding 7 raised for a vendored copy. Nothing resolves this for a client that follows §9's own instruction to fetch live rather than vendor; it is moot only because `spark/1` has never actually gained a field since any outside client existed (there is no outside client yet, §0). |

### Wave 1 — layoutdb (`db/`), serial
| id | slice | brief | status |
|---|---|---|---|
| L4 | **delete + long-poll** (running: impl-L4, worktree `.claude/worktrees/ldb-L4`, branch `ldb-L4`) | remove webhooks + lease (routes, core/webhooks.ts, drain job, migrations stay as empty tables or a drop migration — disposable, so a drop), SSE `routes/stream.ts`, Fly drill (`drill/`, `/v1/admin/drill`, `last_drill`), HTML `/admin/changelog`, per-major dump files (LDB-D6), multi-major chain machinery in `formats/registry.ts` + `write.ts` (assert one major per lineage), `[env.preview]` in wrangler.toml; shrink `import/diff.ts` to count + sampled compare. Add `wait=` long-poll to `/v1/changes` gated by client cap `feed:wait` (clients.caps), ≤ 25 s, 1 D1 read/s, ratelimited. Update INVARIANTS (remove rows, add LDB for long-poll gating), INTEGRATION.md, README. Suite green. | landed c6c003142 (db 15,756 tests green on the integration branch); chain machinery FROZEN not deleted (follow-up L6), /admin/changelog kept (bot history links to it). Deployed 2026-09-12 (v1fa2965c) with the live bot still on SSE — its keep-warm 404-loops harmlessly until B3; correctness path (`/v1/meta` per command) unaffected. |
| L1 | **import fixes** | B2 rename-collision → shadow name + `import_conflict` info event, never a wedge; B5 per-id try/catch, token stored only when every id succeeded or was recorded; B4 tick lock via CAS `import_state` row (10 min expiry; manual tick 409 while held); B3 revive tombstone on identical re-add (`deleted` in the differ); B1 likes union: importer adds missing likes, never `unliked`; likes get `via` column. Tests for each (property where it fits), INVARIANTS rows. | landed 515088dc6 (LDB-L5/I19 sticky shadow/I20/I21/C7; migrations 0011 + 0013; db 15,810 green) |
| L2 | **backups** | dump runs on any tick when `last_dump_at` > 24 h (store in import_state), staleness in `/v1/meta.health`; dump + restore `clients`; README: Time Travel procedure; `db.yml` daily job `upload-artifact` the dump 30 d; stream `buildDump` or set `[limits]`; document the fresh-D1 restore rehearsal steps (saltorbit/lead runs it once and records minutes). | landed 850388595 (LDB-D8/D9/M2/C6; `[limits] cpu_ms`; CI artifact; runbook written, fresh-D1 rehearsal NOT yet run) |
| L3 | **Idempotency-Key** | optional header on every mutating route; table `idempotency(client_scope, key, response, at)` 24 h; same key → replay stored response; conformance fixtures; INVARIANTS. | todo |
| L6 | **format chain freeze → delete** | delete the multi-major chain engine (`up/down/chainFn/chainViolations/path`, `chainToLatest`, `format_behind`, `written_as`, stub-lineage T2/T3 fixtures; LDB-F18/F19/P13) once a decision on how spark/2 will land is made — L4 froze it because put-format/held/list/docs-site tests exercise it. | todo (after W3) |
| I1 | **identity by id, not name** (saltorbit 2026-09-13: "move away from names as unique identifiers … persists through rename") | DB already mints a ULID and keeps it across renames (LDB-P4/P10). Clients: akl.gg catalog key `name.toLowerCase()` (`web/src/state/db.ts:519`) → `_dbId`; bot cache/links by id; share + compare links carry the id; adoption.md states "id = identity, name = mutable label". After the geometry slices land. | **landed** efc499b54 (I-391/I-392, renumbered from the agent's I-388/389 after L9; B-13518..B-13524): `state/catalogId.ts` `catalogIdOf`/`dbIdOf` replace every `name.toLowerCase()` derivation (db.ts promote/delete/restore, ownedReconcile, PublishSheet); pointer mode keys on `record.id`; legacy name-shaped hash refs resolve after the catalog loads (`core/legacyLayoutRef.ts`); one-time storage migration (`pinnedIds`, `compareBase/New`, `cminiDrafts[].base`, `benchScratch`) under flag `legacyLayoutRefMigrated`; gates.sh --fast ALL PASS |
| L9 | **bridgecore sides thumbs by label** | `tools/mana2bridge/bridgecore` (`PhysicalThumbSide`, col < 4.5) still decides a thumb key's hand by column for both engines; the format says the label IS the hand (23 §4.2). Imported data agrees (relabelled by the same rule); a colstag 6+6 layout with an `LT` at col 5 is analysed as a right thumb until the Go bridge reads the label. Wasm + CLI + `build_web.py`/`layout_meta.py` `TB` removal in the same slice. | **landed** 1bf682619 (I-388..390): `LT`/`RT` decide by label in bridgecore + build_web.py; `TB` (cmini-source only) keeps the column fallback. 97 LT/RT keys on 40 catalog layouts (e.g. `thumb`, `graphite-thumb`, `whirl-30`) flip → **base rebuild needed** once B28b lands. Pre-existing red: `scripts/tests/test_migrate_magic_rules.py` 11 failures on the tip (untouched by L9) |
| L8 | **one write = one `db.batch`** (cmini-web-65, LATENCY §14b: a write ≈ 300 ms inside the Worker, several sequential D1 statements — idempotency reserve, event append, folds, likes) | collapse `core/write.ts`'s path into one `db.batch([...])` (or as few round trips as the If-Match check allows); bot side (skip the post-write re-read) is cmini-web-65's. After the geometry cutover; not part of the format slice. | **landed** e0d4ff31a (LDB-L8, L8a..e; `tests/api/l8-roundtrips.test.ts` counts D1 round trips per verb): create 3→2, rename/restore 3→2, transfer 4→3, like/unlike 4→2, tombstone-like inheritance 4+3×N→3; putFormat/patch/delete/seedMagic already 2. Name clash is now enforced by the `layouts_name_live` partial UNIQUE index alone (the pre-read was redundant). Idempotent replay stays 2 (known gap L8e: D1 `meta.last_row_id` is per-connection, so an upsert-RETURNING merge would misreport a real conflict). Bot side (skip the post-write re-read) = B26 agent |
| L5 | **moderation** | `bans` (checked in requireActor on writes, 403 banned), admin overrides as events `via: admin` (rename, transfer, set author name, set likes, restore), `link` field + `link_submissions` queue (submit by owner → pending; admin approve/reject → event; approved writes `layouts.link`). Wave 3. Contract: `design/akldb-site/01-plan.md` §4. | landed c900dc0a3; **like-count override REMOVED 82dc276c4 (H24, saltorbit 2026-09-13)** — migration 0015 (dead `like_adjust` column, wipe makes it moot), WIRE_VERSION 5, SNAPSHOT_VERSION 3; bans / author name / link stay |

### Wave 2 — spark (`bot/` absorbs `stats/` + `workers/data-writer/`), serial
| id | slice | brief | status |
|---|---|---|---|
| B1 | **cell store + row tables + rank** | content-hash-keyed cell store persisted on `/data`; eager compute on rev bump (wasm, 39 cells); per-(corpus, space) row tables; `rank` sorts at once, footer stand-in for pending rows; delete `provenance.ts` + publish tracker + adoption resets; LDB-B rows. | landed 23435c60b (LDB-B90..B95; provenance deleted ~1.7k lines; seed script skipped; tier-2 sweeper folded into the engine budget) |
| B2 | **publisher module** | native mana2 CLI in the image (from `stats/Dockerfile`), computes 108 cells per changed layout into the same store (CLI wins), emits overlay (catalog, rules, authors, per-corpus stats) → R2 via S3 API token (verify conditional PUT; else keep data-writer), pointer CAS with `db_seq`, daily fold + GC, S3 smoke test as part of publish. Delete `scripts/stats_service/`, `stats/`, `workers/data-writer/` when green. | landed 6131ed5f3 (LDB-B102..B109; hand-rolled SigV4 S3 client; fake-CLI tested; real R2 + real CLI + Docker stage unverified until deploy; overlay catalog fields gap → B5) |
| B3 | **boot + feed + write hardening** | wasm + defs baked into image; boot from volume snapshot + cell store, catch up, warm default corpus, `ensureFresh` before login; long-poll client; write reconciliation after timeout/5xx + Idempotency-Key header; bounded table residency; LDB-B5 live parity off the deploy gate (nightly). | landed 8ed066934 (LDB-B110..B115; long-poll w/ degrade, snapshot boot, baked wasm, Idempotency-Key + reconcile, tombstone guard, live suite → nightly `live` job; found+fixed a boot crash in B2's S3 wiring) |
| B4 | **safety nets** | S1, S2 (daily + boot), S4 (daily, magic always sampled), `/health` with last-run + age per check, 48 h rule, watchdog DM. | landed 7260dd2cc (LDB-B96..B101; S1 excludes layouts the replica is legitimately ahead of vs the nightly dump; publisher interface stubbed until B2 wires it) |

| B6 | **boot never waits on the sweep** | INCIDENT 2026-09-12 17:55–18:02Z: the wave-2 image never reached `client.login()` (first boot: empty cell store → `warmBoot` enqueued 4,175 layouts; sweep held the engine lock; boot's `warmCorpus` waited behind it; resident-table budget 8 vs 39 tables/layout thrashed loads; no per-phase boot logs; `/health` started after boot so Fly's check never passed). Rolled back to v32 with `flyctl machine update --image`. Fix: health server first (`main()`, before `bootRuntime`) serving `{phase:'booting', boot:{...}}`; `bootRuntime`'s new `onPhase` hook logs+feeds each phase (replica/engine/publisher/fresh/warm/fonts); `cache/eager.ts`'s pump gets `start()`/`pause()`/`resume()` -- `enqueue`/`warmBoot` only ever queue, `main()`'s `clientReady` handler is the one call site that lets it run (`cells.eagerStart()`/`runtime.openBackgroundWork()`), which also flushes any publisher `notify()`s buffered before that point (same gate, item 5 -- `publisher/index.ts` itself untouched, gating lives in `main.ts`); `engine/host.ts`'s new `createPriorityGate` (`withLock` high / `withLockIdle` low, `EAGER_IDLE_GAP_MS`) sits in front of the driver's own mutex so a command/boot step always preempts the sweep; `cache/eager.ts`'s sweep reordered table-major (one (corpus,space) pair for every queued id, then the next pair) instead of layout-major; `engine.warmCorpus` at boot raced against `BOOT_WARM_TIMEOUT_MS` (30s default). LDB-B130..B135 registered (V0 took B116..B122, B5 took B123..B126, confirmed no collision). Rebased onto V0 (262d7d201) + B5 (eeae69346); kept both intents in `main.ts`/`health/index.ts` (V0's `latency`/`bootTimings`/`fullSnapshot` alongside this slice's `openBackgroundWork`/`onPhase`/`ownServer`; B5's `publisherConfigFrom`/`overlayRowsPy` alongside `eagerIdleGapMs`/`bootWarmTimeoutMs`); fixed the post-rebase `/health` handoff to serve V0's `fullSnapshot()` (was still pointing at checks-only `snapshot()` from before V0 landed). Bot suite green post-rebase (90 files/1,125 tests/73 skipped); `tsc --noEmit` + `eslint` clean. Commit `7974ff229`, branch `worktree-agent-af9cb05e4b1e0be1d`. | landed 5bbf8f62d (LDB-B130..B135; health first, per-phase boot logs, two-lane lock, table-major sweep, bounded warm-up, background work gated on clientReady) |
| B8 | **publisher bounded ticks + CLI throughput** (impl-B8) | Observed 2026-09-12 19:53Z: first backlog pass = 4,177 layouts computed BEFORE the first publish; one layout's native compute took ~55 s (old service: 3.5 s with `--jobs 2`). Fix: ≤ 25 layouts per tick then publish and continue; recent edits first; `--jobs 2`; per-layout timing + backlog in `/health`; the wasm sweep yields to a CLI backlog pass. | review (branch `worktree-agent-ac101a6c435d96188`, `8858f53fe`): `publishPlan` chunks a plan into ticks of `PUBLISHER_TICK_MAX_LAYOUTS` (default 25), most-recently-`notify()`-ed first then the backlog, publishing + continuing immediately (no debounce) until drained; a restart between ticks loses at most the one in flight. `PUBLISHER_CLI_JOBS` (default 2) threads into `compute.ts`'s `--jobs` (was silently 1 -- nothing threaded a value in at all). `PUBLISHER_BACKLOG_PAUSES_SWEEP` (default true) pauses the eager wasm sweep for a multi-tick pass's duration (found + fixed a latent bug along the way: `eager.ts`'s `resume()` called `this.start()`, broken once passed around unbound -- exactly how this wiring does it). `/health.publisher` gains `backlog`/`computedCount`/`computeMsMean`/`lastTickAt`/`lastPointerDbSeq`. LDB-B141..B147 registered with tests (bot suite 1,150 green, 1 pre-existing unrelated flake). NOT done: `scripts/fast_sync_patch.py`'s group-workdir reuse -- reasoning in the commit message / agent report (the workdir is already shared by construction; the rmtree+recreate per group is cheap next to the dominant per-subprocess corpus-parse cost, and isn't unit-testable here without a real corpus + built Go binaries). NOT deployed. |
| B9 | **follow-ups** | (a) `bot/tests/health/index.test.ts` LDB-B119 memory-RSS assertion flakes (seen twice, passes on rerun) — pin it; (b) S1 reports "1 divergent of 4176" without naming the id — log the ids; (c) the Worker's long-poll checks the head once per second → wake p50 ≈ 1 s; consider 250 ms; (d) the deleted scratch layouts `lead-latency-scratch(-renamed)` from the harness runs are tombstones on prod (fine; disposable). | todo |
| B5 | **overlay catalog parity** | B2's overlay `layouts.json`/`authors.json` rows carry only replica-derivable fields; the SPA's filters also need the Python-derived catalog fields (`upperrow`, `thumb`, `complete`, `vowel_hand`, meme-ness, …) that `scripts/build_web.py` computes. Do NOT re-derive in TS: add a small Python entrypoint (`scripts/overlay_rows.py`, reusing `build_web.py`'s functions exactly as the deleted `stats_service/overlay.py` did) that takes layoutdb records on stdin and emits catalog rows; the publisher shells to it per publish (python3 in the image). Golden test: rows byte-equal to a recorded Python output for the fixture layouts; property: every field the SPA reads (grep `web/src` for the catalog row type) is present. | landed eeae69346 (LDB-B123..B126; `scripts/overlay_rows.py` calls build_web.py's `layout_meta_and_keys`; web I-330 test fixed; bot 1,114 green, web 6,688 green) |
| B7 | **spark publisher: never download the whole base; bounded, background start** | INCIDENT 2 (18:45–18:58Z): `publisher.start()` hung ~10 min on `loadPublishedIndex()`'s `currentFullFiles()` pulling the WHOLE published base (~553 files, ~2GB for a real catalog) to answer two small questions, and `bootRuntime` awaited that before `ensureFresh()`. Fix: `loadPublishedIndex()`/`publishPlan()` now read only `{pointer, manifests, layouts.json, rules.json}` (the base) and the CURRENT overlay only (bounded by construction -- never a base's `mana2/*`); the daily fold streams one table at a time, server-side `CopyObject`ing any base table the overlay never touched and no pending tombstone could have dirtied; every S3 request carries `AbortSignal.timeout` (`S3_REQUEST_TIMEOUT_MS`); `bootRuntime` no longer awaits `publisher.start()` -- it runs from `openBackgroundWork()`, bounded by `PUBLISHER_START_TIMEOUT_MS`, past which the publisher marks itself `degraded()` and retries every 5 min. | landed 8eac203bd (LDB-B136..B140; index load = manifests + layouts.json + rules.json only, 6.5% of bytes; fold streams + CopyObject; publisher start background + degraded state; S3 timeouts). Third spark deploy follows. |

### Wave 3 — akl.gg
| id | slice | brief | status |
|---|---|---|---|
| A1 | pointer-only build as the only mode (bundled kept as `build:bundled`, test-only); owned-but-uncatalogued rows render from the record; pointer poll 60 s (+ fast-confirm loop until own publish covered); proxy timeout/body cap/`Secure`; delete `dispatchAfterWrite`. Branch `worktree-agent-a7c09faaaa6ce5163`, 6 commits (through `bb22333c6` + this ledger update), I-371..I-376. | review |
| B10 | **health-alert fixes** (2026-09-12 evening, from saltorbit's DMs) | long-poll timeout no longer raises `db_timeout` (`quietTimeout`, 45 s bound, `feed_slow` note); S4 runs once at boot on the low-priority lane and "never run" is `pending`, not failing; S2 publication coverage answers from the restart-loaded index (`isPublished(id)`; also fixed `rulesSig` default `''` vs `'-'`); S1 treats replica-only ids the dump never heard of as `newerThanDump`, logs divergent ids; watchdog DMs on transitions + one 24 h digest. LDB-B148..B153. | landed b8cb1f2e6; **deployed** spark v42 2026-09-13 01:56Z (saltorbit granted the deploy) |
| B11 | **loop-lag instrumentation** — `perf_hooks.monitorEventLoopDelay` histogram on `/health` (`loop_lag_ms` since-boot + rolling 10 min, `loop_stalls_over_1000ms`, `recent_stalls` with the publisher phase), per-tick `publisher.last_run_at`/`publisher_last_tick_at` + phase timings, `history.json` null `db_seq` fix. Measures the stalls behind the feed timeouts (LATENCY.md §9). | landed (LDB-B154..B159; bot 1,179 green); **deployed** spark v42 2026-09-13 01:56Z with B10 |
| B12 | **publisher off the main thread** (proposed, after B11 numbers): overlay row derivation / table merge / `JSON.stringify` in a `worker_threads` worker or chunked with yields, so a command never waits behind a publish. | landed (LDB-B160..B166 + B181; bot 1,215 green): `publisher/worker.ts` + `workerClient.ts` + `wire.ts` (transferList, chunked encode); applyDelta/buildManifest/mergeOneFile/smoke-derive off-thread; CLI child nice 10 (B15 done here) |
| B13 | **latency harness on Fly** — `dist/e2e-headless-runtime.js` as a build entry, `e2e-headless.mjs` prefers the prebuilt bundle, `bot/scripts/*.mjs` shipped in the image, README "Measuring on Fly". Unblocks V1 round 2 per-verb numbers without waiting for Discord traffic. | landed 2c8931754 (LDB-B170..B173; bot 1,196 green); deploying |
| B14 | **safety-net follow-ups from the v42 boot + v44 worker-crash handling + watchdog restart memory** (landed 45f226ef8, LDB-B200..B203; bot 1,241 green): (a) S2's boot run happens before the publisher index has loaded → "4177/4177 missing publication" at boot; run S2 after `publisher` start resolves (or re-run once); (b) S4 at boot: "42 sampled: 5 wasm/store mismatches, 0 wasm/CLI mismatches, 36 no baseline" — the store's cells may be CLI-sourced (108-cell native build) while S4 recomputes with wasm; saltorbit accepted two engine builds, so S4 must compare like with like (tag each stored cell with its source engine, compare wasm↔wasm-sourced and CLI↔CLI-sourced) and log WHICH fields differ; until then a wasm/store mismatch is not evidence of corruption. | todo |
| B15 | **CLI child at lower OS priority** — `os.setPriority(child.pid, 15)` (or `nice`) on the publisher's `fast_sync_patch.py` / mana2 CLI spawn so bot commands always win the single vCPU during a backlog pass (LATENCY.md §10: compare p95 3.1 s, sfbs-magic p95 1.0 s from contention). One-liner + a test that the spawn options/priority call is made. | done inside B12 (`trySetPriority`, default nice 10, LDB-B181) |
| B16 | **grouped compute in the publisher** (saltorbit 2026-09-13: "if I ever have to do another full rebuild, I don't want it to take 12 hours"): the compute phase runs ONE grouped `fast_sync_patch.py` invocation per tick for every layout in the tick (the CLI parses each corpus once per group, as the CI full harvest does) instead of one CLI call per layout (≈ 3 s each, ~90 % corpus re-parse). Tick size scales with the backlog: a handful of layouts for a live edit (freshness), hundreds during a pass, bounded by a `PUBLISHER_TICK_MAX_LAYOUTS` ceiling tuned to measured peak RSS (grouped mode holds a parsed corpus + all results; machine has 2 GB, bot ≈ 550 MB). Child at low priority (B15). Target: full 4,178-layout pass ≈ 1.5–2 h on 1 vCPU, ≈ 1 h on performance-2x, commands unaffected. An off-machine "publish base from a CI harvest" tool stays optional (only if 15 min rebuilds are ever wanted). Depends on B12 (merge off-thread) landing first. | **B16a landed** (LDB-B190..B196; `fast_sync_patch.py --layouts/--rules-dir`, schema 3, `publisher/batchCompute.ts`; Python 430 + bot 1,207 green; real-CLI oracle NOT yet run — do it on the Fly machine before enabling). **B16b landed** d69d9fee0 (LDB-B210/B211; `PUBLISHER_BATCH_COMPUTE` default OFF, `PUBLISHER_TICK_BULK_LAYOUTS` 200). Gate to turn it on: B17 dry-run base ≡ per-layout published cells. |
| B17 | **throwaway rebuild machine** (saltorbit 2026-09-13: "I would love to have that as a well-formed thing"): second bundle entry `dist/rebuild.js` (no Discord login) — fetch dump → B16 grouped compute over every live layout `--jobs N` → write a fresh base with the fold's base writer → CAS pointer to `{base, overlay: null}` → smoke → exit; `--dry-run` writes the base without the swap. Wrapper `scripts/rebuild-on-fly.sh`: `flyctl machine run <current release image> -a spark-bot --vm-size performance-8x --vm-memory 16384 --rm --command "node dist/rebuild.js"`, streams the log, exits with its status (≈ 12–15 min, < $0.10). Same entry runs locally/CI. Invariants: rebuilt base ≡ publisher output cell-for-cell; atomic pointer swap; live publisher re-diffs to nothing; machine destroyed on exit either way. | **landed** d69d9fee0 (LDB-B212..B215): `dist/rebuild.js` + `scripts/rebuild-on-fly.sh` (`ImageRef`/`--detach` verified against the real flyctl 2026-09-13); **dry run #2 OK 04:16Z** (938 s total; base `20260913T040059Z-55dbe6da35f9`; oracle: equivalent, fsp/fspw differ only at the 15th digit — LATENCY §12). **REAL RUN OK 04:23Z**: base `20260913T042115Z-d83b197cd674` live, overlay null, 1,009 s end to end. B17 is now the way every full pass happens. |
| B18 | **overlay growth between folds**: after ~30 ticks the overlay's `mana2/*` tables hold 700+ layouts each and a single PUT can exceed 30 s on a slow R2 night (3 ticks lost 03:27–03:51Z). Short term: `S3_REQUEST_TIMEOUT_MS=120000` in fly.toml; a base swap (B17) empties the overlay. Proper fix: fold more often than daily when the overlay passes a size threshold, or write per-tick overlay deltas that the fold merges (bounded PUT size). Measure table sizes first (`_manifest.json` + HEAD). | todo |
| B19 | **follow-ups from the first rebuild**: (a) S4/oracle comparisons of CLI cells need a relative tolerance (~1e-9) for `fsp`/`fspw` — grouped vs single runs differ in summation order; (b) the 04:00 UTC daily S1 run stalls the main thread ~2.2 s (dump diff) — move onto the publisher worker or yield; (c) the `[LDB-B200]` notify-dedup test flakes under full-suite load; (d) S4 05:29Z flagged a REAL like-for-like mismatch on `01M29V9Z7FYPSBXVC914801XNK` (`big`,`skip` differ between the stored wasm cell and a fresh wasm recompute) — log both values and the cell key's inputs (rev/rules at store time vs now) before calling it corruption. | todo |
| B20 | **publisher must notice an external base swap**: after B17 swapped the pointer (04:23Z) the live publisher kept its boot-time published index and went on grinding its 3,327-layout backlog (redundant recomputes; a harmless overlay on top of the new base). Fix: on every tick, compare the pointer's `base` with the base the index was loaded from; if it changed, reload the index (`loadPublishedIndex`) and re-plan before computing. Until then: restart the bot after a rebuild (`flyctl machine restart`). Also have `rebuild.js` print that reminder in its summary. | todo |
| B21 | **seed the bot's cell store from the published base at boot** — after a B17 rebuild the live bot still had 3,236/4,179 layouts "missing a cell" and its eager wasm sweep ground through them for hours on the machine's CPU (the steady-state load that exhausts shared-CPU credits). The base tables already hold every layout's cells (CLI-sourced, `source: cli`); ingest them into the cell store at boot (78 tables, ~tens of MB, keyed by `cellKeyFor`) so the sweep has nothing left to compute except genuinely new edits. This is what makes shared-cpu-2x viable again. | todo — high value |
| B22 | **latency record: split handler vs Discord post, keep >1 s details, pace the harness** — `recordCommand` gets `handlerMs` (reply ready) and `postMs` (discord.js send incl. rate-limit queueing); `/health.latency` shows both; violations list keeps >1 s entries (today only >5 s); `e2e.mjs --pace <ms>` so a measurement round never trips the per-channel 5-messages/5-s bucket (bot-hammer-fixes memory, 2026-09-12 — relearned tonight). First item before quoting R2 numbers for writes. | **landed** c685b0510 (LDB-B220..B223: handler/post split, >1 s details kept, `e2e.mjs --pace`); deploying v51 + paced round |
| B23 | **remove the admin like-count override** (saltorbit 2026-09-13 06:10Z: "Mods should not be able to override the like count") — retire `admin.likes_set`/`like_adjust` on both halves of ldb-b197 (db fold + bot cache/apply/feed/snapshot/render); `like_count` = count of `likes` rows. This also removes the S1 `like_count` divergence seen on every boot. Owner: cmini-web-6b via cmini-web-19 (relayed). | handed over |
| B24 | **write PUT latency from Fly** (LATENCY §14): 640–760 ms per PUT, 300–500 ms per changed-record GET vs 65 ms `/v1/meta` — D1 round trips. Step 1 Smart Placement on the Worker (63933c088, deployed 05:45Z) — re-measure; step 2 one `db.batch()` per write in `core/events.ts` appendWrite (count the statements first); step 3 drop the post-write full re-read on the bot (the PUT response carries the record). Target: write handler ≤ 600 ms. | **moved to `lax` 06:06Z** (machine 86de22fee000e8, volume vol_vp26w21jgw97ddw4 from a fresh snapshot; iad machine 7841609c5d3628 + vol_v8ek7w2p3ky17xlv kept STOPPED as rollback, destroy after 24 h). Result: write PUT 640–780 → 530–700 ms only; reads unchanged. The laptop "250 ms write" was a 400-rejected PUT — bad oracle. Remaining per-write cost is Worker-side: Idempotency-Key reserve-before-run (an extra D1 transaction the bot pays and my probe did not) + the post-change full-record GET (270–430 ms). Next: one D1 batch for reserve+write (db, after the format cutover), and drop the bot's post-write re-read. **Previously:** root cause = geography (D1 WNAM, bot IAD; laptop via DEN sees 25 ms reads / 250 ms writes): Smart Placement on, no effect yet; **recommended: move spark to Fly `den`** (volume snapshot → new volume → clone → destroy IAD) in a quiet hour with saltorbit aware. LATENCY §14a. |
| B25 | **RSS growth outside the main heap**: 705 → 849 → 1,045 → 1,622 MB over 4 h on v45–v51 with heap flat at ~420 MB; fresh boot in lax already 1,326 MB. Instrument: main `process.memoryUsage()` (rss/heapUsed/external/arrayBuffers), each worker thread's own memoryUsage via a stats message, wasm linear memory, cell-store bytes in RAM; log every 5 min; find what grows (publisher worker buffers? transferred ArrayBuffers retained? row tables?). | **landed** 059359320 (LDB-B240..B244): `/health.memory_breakdown` (main / publisher worker / engine worker incl. wasm bytes / cell store / row tables / index), 5-min memory log line, watchdog DM carries the breakdown. Growth hunt: publisher worker and canvas renders proven flat over 50 jobs; wasm linear memory grows by design (only LDB-B95's budget reboot reclaims it). Ships with the step-5 spark deploy; read the log after 2 h to attribute the 300 MB/h. → **attributed 10:14Z: engine wasm linear memory (B29).** |
| B26 | **batch compute result parse off the main thread** — a 200-layout schema-3 result is parsed with `JSON.parse` on the main thread in `batchCompute.ts` (5–16 s stalls seen 08:17Z). Parse it on the publisher worker (B12 pattern) or stream per id; and lower `PUBLISHER_TICK_BULK_LAYOUTS` default to 50 until then. | **landed** e4366249e (LDB-B275..B277): `publisher/batchParse.ts` + a `batch-parse` worker job; the main thread never JSON-parses the schema-3 result (transferable per-id buffers); measured main-thread gap 16–24 ms at 200 layouts (was 200–900 ms in the sandbox, 5–16 s live); `PUBLISHER_TICK_BULK_LAYOUTS` stays 200. B24 step 3: NO post-write re-read exists in the write verbs (already folded by B3); the 270–430 ms figure is `cache/feed.ts`'s canonical per-event detail GET inside `ensureFresh()` (R1-mandated) — left alone |
| B27 | **harden the two load-sensitive tests**: `[LDB-B240]` worker heapUsed-within-50 % (tests/publisher/workerMemory.test.ts) and `[LDB-B200]` notify-dedup both fail only under full-suite load and pass alone — force `global.gc()` before each measurement / use fake timers for the debounce; until then they are known flakes, not gate reds. | **landed** e4366249e (LDB-B278/B279): worker `stats()` forces gc before measuring; B200 polls the real `computedCount()` bounded 5 s instead of a fixed 50 ms sleep; 7/7 full-suite runs green; both proven to still catch their regressions |
| B28 | **a base is the site's whole `web/data` tree, not the publisher's slice** — B17's `rebuild.js` writes layouts/rules/authors/mana2 tables/manifest only, so after today's rebuilds the db-alias site booted empty (404 on `build_id.json`, `mana2/defs.json`, `layouts_keys.json`, … — cmini-web-19, 09:45Z; the 04:23Z rebuild broke it the same way unnoticed: I checked only the HTML 200). B28a (agent): build a complete base for the new catalog (catalog artifacts via `sync_cmini_data.py --source db` + `build_web.py` for the new shape, cell tables reused, static files from the old base, `layouts.json` rows carrying the publisher `hash`), `bot/scripts/publish-base.mjs` uploader + pointer swap; lead publishes. B28a **landed** fad402ed3 (LDB-B250..B253: `sync_cmini_data.py --source db` reads the new shape, `build_magic_rules.py` untags magic, `web/src/data/catalog.ts` reads an object-shaped `layouts.json` via `.meta`, `bot/scripts/publish-base.mjs` + `verify-base.mjs`; base dir 167 files / 571 MB, 237/237 DATA_PATHS ok, 4,177 hashes preserved). **Publishing 10:50Z.** KNOWN INCONSISTENCY → **B28b**: the agent keyed the base by layout NAME (build_web convention, `meta.id` = name, tables keyed by name) while the publisher writes overlays keyed by the DB id (`meta.id` = ULID) — the first bot edit after the swap shows as a duplicate row on the site. Unify on the DB id (saltorbit: ids are the identity): `sync_cmini_data.py --source db` writes `layouts/<ulid>.json` with `id` = ULID so every build_web artifact is ULID-keyed, then `rebuild.js` runs `build_web.py` on the machine and emits the complete base itself — no rekeying, one convention. The db-alias site must be redeployed from ≥ fad402ed3 (catalog reader). **B28b in flight** (agent `b28b`: ULID-keyed `sync_cmini_data.py --source db`, every build_web artifact ULID-keyed, rebuild.ts emits the complete base); a live overlay `o/20260913T150529Z` already exists on the pointer (a write at 15:05Z) — the duplicate-row case is live until B28b lands. | **B28b landed** c478a58e3 (LDB-B263..B266, I-393/I-394): `sync_cmini_data.py --source db` writes `layouts/<ULID>.json` with `id` = ULID (cmini source byte-identical; also fixed a latent bug dropping inline likes on the batch path); every build_web artifact ULID-keyed; `rebuild.ts` assembles the COMPLETE base itself (`rebuildAssets.ts`: real sync/build_web/build_magic_rules/mana2bridge subprocesses, baked assets + current-base reuse, meme_filter inline, `missingDataPaths` gate on the real `DATA_PATHS` before any swap, also under `--dry-run`). Next: deploy v55, `DRY_RUN=1 sh scripts/rebuild-on-fly.sh`, real rebuild + swap (also picks up L9's 40 thumb layouts), db-alias redeploy from ≥ c478a58e3.
| B29 | **memory growth = the wasm engine's linear memory** (`/health.memory_breakdown` 10:14Z: engine_worker.wasmMemoryBytes 781 MB of RSS 1,641; main heap 442, cell store 100, row tables 208). Grows ~1 MB per layout the eager sweep computes and never shrinks; LDB-B95's budget counts tables, not bytes, so it never tripped. Fix: `ENGINE_WASM_MAX_MB` cap → recycle the worker via B95's reboot path between computes; counters on /health. Machine restarted 10:14Z as a stopgap. | **landed** (LDB-B260..B262; `ENGINE_WASM_MAX_MB` default 384; bot 1,364 green); deploying |
| B30 | **magic fallback poisoning** (saltorbit 2026-09-13 14:51Z: `!spark view twirl` → "(magic rules not applied)" for a record WITH magic; "this should literally never happen. i would rather there be an error than see these incorrect stats") — hypothesis: `engine/host.ts compute()` falls back to the plain engine when `ensureMagicCorpus` fails for any reason (the 04:23–10:50Z incomplete base had no `ngrams-extended`), `cells.ts ensureCell` PERSISTS that cell under the magic hash with `magicApplied:false`, every later view is a store hit; plus the url-keyed `loadText` cache keeps the rejected fetch for the process lifetime. Rule now: a rule-carrying layout is never rendered/stored/published without its rules — unavailability is an ERROR (typed), never a degraded number; poisoned store entries purged at boot (versioned marker); failed fetches not sticky; fallbacks counted on /health. | **landed** 8bfc61734 (LDB-B296..B299, B304, B305): `compute()` throws `MagicEngineUnavailable` for a rule-carrying layout when the magic engine cannot run (no fallback branch exists any more); a store hit with `magicApplied !== true` under a magic request is poisoned → recomputed + overwritten; failed extended-table fetches retry with 30 s→5 min backoff via a driver reboot; one-time `PURGE_VERSION` boot sweep deletes poisoned cells; `/health.memory_breakdown.engine_magic_unavailable_count`. EXTRA ROOT CAUSE: `cellstore.ts loadTable` dropped `magicApplied` on every disk reload, so every restart re-poisoned the whole store (B305). Shared-code line for the lead: `web/src/core/swap-engine.ts:449-452` caches a rejected `loadText` forever (affects akl.gg too). Ships with the next spark deploy; watch the purge count in the boot log |
| B31 | **incomplete commands show help** (saltorbit 2026-09-13: `!sp transfer` → "that layout not found"; "incomplete commands like this [should] show help text, not try to search for a zero-length layout") — declarative required-arg shape per verb, checked by the dispatcher before the handler runs; the reply is the verb's EXISTING help text verbatim; matrix test over the registry. | **landed** (LDB-B300..B303): `Command.minArgs()` per verb, checked in `main.ts` before the ticket/fresh/exec; reply = `commandHelpText` (byte-identical to `help <verb>`); matrix over every verb × shortfall with a fully-trapped runtime; `assign/transfer/rename/board!/compare` = 2, `swap/swap!/cycle/cycle!` = 2 (lead), most read verbs = 1, `rank/random/list/search/filter/...` = 0. Ships with the next spark deploy |
| API-V | **API versioning** (saltorbit 2026-09-13: "make sure the akldb api itself is well versioned too") — audit of every version signal (`/v1`, WIRE_VERSION, lineages, event schema, dump) + policy (`/v1` = compatibility promise, additive minor in a header + `/v1/meta.api`, `/v2` beside never in place, deprecation headers, `CHANGELOG-API.md`) + enforcement (generated route/shape golden that fails on an unbumped change; wire fixtures validate against it). Doc `design/layout-db/25-api-versioning.md`. | **landed** 5d74f9ac3 (LDB-V1..V7) + lead fix-forward 1.8: `X-AKLDB-API: 1.<minor>` on every response (minor = `WIRE_VERSION`, one counter), `/v1/meta.api` + `deprecations`, `db/CHANGELOG-API.md`, `db/tests/contract/route-table.golden.json` generated from the conformance CASES × adoption.md's auth column (a drift fails until the minor bumps + a changelog line; proven by mutation), `Deprecation`/`Sunset` header registry (90-day minimum, empty). Audit `design/layout-db/25-api-versioning.md`: like_adjust added+removed in a day with no signal; webhooks/SSE deleted in place 2026-09-12; INTEGRATION.md still documented them (adoption-docs pass fixes). Golden caught my own wait= change on landing → API 1.8 (WIRE_VERSION 8). Owed: adoption.md "Versioning" paragraph (proposed text in the design doc) — copy sign-off |
| B32 | **bot link verbs** (saltorbit 2026-09-13: "there doesn't seem to be a way to view or set links in the sparkbot?") — `link <layout>` (approved link from the cached record; the owner also sees a pending submission), `link! <layout> <url>` (owner-only PUT → moderation queue, reply never folds into the cache until the feed delivers the approval), `unlink <layout>` (owner-only DELETE, immediate — saltorbit: "unlink (no !) should always work and not need to go through the admin queue"; renamed from `unlink!` by the view-magic slice); links ride the feed into `record.link`/`pendingLink`. **UNSIGNED COPY awaiting saltorbit** (`bot/src/copy.ts`): `` `${name}` has no link set `` · `` `${name}`'s link: <${url}> `` · `(a submission is pending review: <${url}>)` · `` Your link for `${name}` has been submitted and is awaiting moderation `` · `` `${name}`'s link has been set: <${url}> `` · `` `${name}`'s link has been cleared `` | **landed** 6f00c5d1c (LDB-B310..B312); not yet deployed |
| B33 | **view shows magic rules + link** (saltorbit 2026-09-13: "when running view commands, we should also show the magic rules in the view output"; "links should show up too when people do !view") — `view`/`random` append the `magic` verb's exact rules block when the record has rules, and the `linkLine` when `record.link` is set; no new copy. | **landed** df1b6b72d (LDB-B320..B322): `magicReplyBody` extracted from the `magic` verb and appended by `view`/`random` iff `rulesFor(rec) !== null` (one resolve, same record the stats used); approved `linkLine` before it, never `pendingLink`; `unlink!` → `unlink` (saltorbit). Largest real rules block is 227 chars (adaptative-magic-sturdy) so no reply nears 2000. Open Qs: post-write replies (`swap!` etc.) also get the block? `random` still has no site link. Not yet deployed |
| B34 | **S4 like-for-like mismatches after v54** (15:57Z: 42/52 sampled cells disagree on big/skip/fsp/fspw with a fresh recompute by the SAME engine kind; the named layouts' thumb labels agree with their columns, so not L9) — hypothesis: the cell key's `enginePin` (mana2 pin + statsVersion) does not change when bridgecore/vendor patches/the wasm bake change, so cells computed by older wasm builds are served as hits. Fix: engine content hash in the key + stale-vs-mismatch in S4 + recompute-on-hit. | **landed** 1a8a61913 (LDB-B330/B331): ROOT CAUSE mostly float noise -- a fresh native compute of `apple` differs from the published cell only in `fsp`/`fspw` by 1 ULP and two identical builds differ from each other (goroutine summation order; tolerated at rel 1e-9 by LDB-B5/B9) while S4 `diffCells` compared bit-exact -> now rel 1e-9. The engine-pin gap is real too: `mana2_pin()` = vendor HEAD regardless of vendor patches, and `bot/Dockerfile` curls `engine.wasm` from the LIVE akl.gg site at image build (the bot wasm follows main's pipeline, not this branch's bridgecore; L9 reaches the bot only via an akl.gg rebuild). Fix: the worker sha256s the wasm it loads (`wasmHash`, stamped in `swap/meta.json` by build_wasm.py); a wasm-sourced hit under another hash is stale -> recomputed in place, never served; S4 counts `staleEngine` separately; O(1) marker on hash change. Copy for the link verbs signed by saltorbit 16:25Z ("Copy ok") |
| CG | **trusted-client abuse containment** (saltorbit 2026-09-13: "Do we have recourse if [dmini as a trusted client] crash out and abuse their trusted powers to wipe everything?") — per-client rolling destructive-write budget → auto-suspend (new `suspended` status) + changelog event; `/v1/meta.health.clients`; admin `suspend`/`reactivate`; `POST /v1/admin/clients/:id/revert {since, dry_run}` from event history (never overrides another actor's later write). | **landed** 6f1256e5c + API 1.10 (LDB-A10..A13): destructive budget max(200, 5 % of live)/rolling hour counted inside the write's own D1 batch (spark's real peak = 10/h); `clients.status` gains `suspended` (no migration); `403 client_suspended`; `/v1/meta.health.clients`; `POST /v1/admin/clients/:id/suspend|reactivate|revert {since, dry_run}` (revert walks the client's own events newest→oldest, never overrides another actor's later write); runbook "Rogue trusted client". Conformance 403 fixtures moved to the `modnonadmin` actor (two slices' not_admin probes together exhausted the owner token's write window → 429). Copy owed to saltorbit: adoption §2.1 sentence about the budget (proposed text in the agent's report / README) |
| IG | **hostile/vanished upstream** (saltorbit: "concerned about the cmini owner crashing out and deleting their db") — beyond LDB-I3/I6: 24 h rolling deletion budget, empty-listing cases pinned, `/v1/meta.health.import` visibility, admin `unstall` + `restore-deleted {since}`, `IMPORT_DELETES=off` kill switch, runbook. | **landed** fe7ab8753 + API 1.9 (WIRE_VERSION 9, e1cd98ccc) (LDB-I22/I23/I25/I26/M3): rolling 24 h budget max(20, 2 % of live) — production has NEVER emitted an `upstream_deleted` event (45,257 events walked); `IMPORT_DELETES=on|off` var; `/v1/meta.health.import`; `POST /v1/admin/import/unstall`, `POST /v1/admin/import/restore-deleted {since, limit?, dry_run?}` (ops-call lines in 13-ledger §3); README runbook "Hostile or vanished upstream"; site docs purpose lines added by the lead (aee2c51ca) |
| RN | **user-facing copy says akldb, never layoutdb** (saltorbit 2026-09-13 via cmini-web-71, confirmed "yes") — bot `copy.ts` (2 strings), adoption.md/INTEGRATION.md/22-spark-spec.md prose (10), akldb.org tagline + `<title>`/meta (3); identifiers (`akl-db`, `spark-bot`, env vars, routes, paths, design docs) untouched; one no-"layoutdb" test per surface (LDB-B332, LDB-G14, SITE-38). `web/src/copy/**` is cmini-web-71's on `aklgg-publish`. | **landed** c1cb4712c; bot strings ship with the next spark deploy |
| LR | **data line roll** (L9 changed the stats derivation → `pipeline_inputs.py` line_key 2cb8a634… → 70b38d1c…; saltorbit picked A: roll, keep the old line as rollback) — real B28b rebuild landed a complete ULID base on the OLD line (`20260913T183956Z-9767d77c7732`, pointer swapped 18:41Z, smoke OK); `bot/fly.toml` `DATA_LINE` → 70b38d1c… (d00b8396b, spark v61: publisher logs "no pointer for line … yet", skips publishing, DMs `no_pointer`); bootstrap rebuild #1 on the new line computed 4,177 then the completeness gate refused: 54 files missing (corpus n-gram tables, words, heat_ceilings.json, swap/meta.json — the reuse-from-current-base set; a fresh line has no current base; swap/meta.json was never baked). → **B28d** (agent `b28d-freshline`): `REBUILD_REUSE_FROM=<line>/<base>` with provenance guard, bake swap/meta.json, regenerate heat_ceilings. db alias stays PINNED to the old line until the new line has a base. Safari hover fix SH landed c879aefde (I-395..397). | **done 19:59Z**: B28d landed eaa535241 (LDB-B268..B270: `REBUILD_REUSE_FROM=<line>/<base>` with a mana2_pin/stats_version provenance guard, swap/meta.json baked, heat_ceilings regenerated); spark v62; bootstrap rebuild on the new line = base `20260913T195347Z-2b7d7f09229f`, first pointer created, smoke OK, `derive_hash` == line key; bot restarted → publisher index 4,177 on line 70b38d1c…; db alias deployed UNPINNED (deployment fc57bca3, boots on the new base, 3,763/4,125 listed, rows/hashes ULID-keyed, no app console errors). Old line 2cb8a634… keeps base 20260913T183956Z-9767d77c7732 as rollback (`DATA_LINE` back + `VITE_DATA_LINE` pin). Laptop launcher was OOM-killed mid-run once — the Fly machine finished on its own (`--rm`); poll `flyctl logs` instead of trusting the laptop poller |
| CT | **akldb.org admin: Clients tab** (saltorbit: "make sure the admin console has this client suspension list in the moderation") — every client with status, suspended first + since/reason, Suspend (inline reason) / Reactivate / Revoke (inline confirm, never `window.confirm`); pinned fixture `db-responses/admin-clients.json` recorded from the real route; SITE-35..37 (37 = no budget/threshold string in any bundle chunk, LDB-A14 site-side); Chrome-checked desktop + 390×844. Follow-ups: revert UI; Changes page has no actor filter to link a client's writes. **UNSIGNED COPY awaiting saltorbit**: tab "Clients"; columns Client/Owner/Capabilities/Status/Suspended/Revoked; Active/Suspended/Revoked; `Since <at>`; Suspend/Reactivate/Revoke; "This cannot be undone."; "Could not load the client list from akldb"; "No clients registered"; "This client is already revoked". | **landed** d18b93d1a (copy signed by saltorbit 17:50Z "copy good"); deploys via the CI site job |
| S1 | **spark/1 spec page** rewritten per 24's skeleton, every rule cited to code; 7 spec/code discrepancies listed (keys[].char single code point unchecked; no 64 KiB body cap; `from=` not reserved; `/keys` vs `/keys/<i>` error paths; `reserved_rule_type` untested; stale LDB-F22 clause; served schema is `additionalProperties:false` vs "readers ignore unknown fields"). Follow-up pass (saltorbit: "spark/1 is akl/1 renamed — delete all such historical references … assume that readers have never seen any of this before"; "this key isn't a secret right?"; quickstart with snippets for every basic operation): adoption.md history purge + stale examples fixed to the current shape + pinned by test + Quickstart with tested snippets. | spec **landed** d844c949e; adoption pass **landed** 2bf3d227a (LDB-G12/G13: every `json spark-payload` fence in adoption.md validates; the Quickstart's 8 snippets hit real routes and the Node signing snippet gets a 2xx against the in-test Worker). History purged from adoption.md/INTEGRATION.md/22-spark-spec.md; INTEGRATION.md §4/§5 now describe long-poll (SSE/webhooks were dead); §1.1 keypair wording; §1.4 "The API's own version"; discrepancies → row S1a. Gotcha: the quickstart test uses the undocumented `__VITEST_POOL_WORKERS_UNSAFE_EVAL` binding to run the doc's JS under workerd (pinned pool ^0.22) |
| A2 | the atomic flip (Pages prod `DB_BASE_URL`, CI var, pointer build, meta-watch removal) — **saltorbit runs**; then delete the prod stats path (workflows, D1 tables, Functions, scripts, `layout-dates.json`, `cmini-backup`), rotate `GITHUB_DISPATCH_TOKEN`. | todo |
| A3 | S6 monthly reference harvest workflow (scratch line, diff, alarm). | todo |

### Verification (saltorbit, 2026-09-12: "once you are done, run a latency analysis … bot, aklgg, anything you can think of. are we meeting spec?")
| id | slice | brief | status |
|---|---|---|---|
| V1 | **latency analysis vs spec** (round 2 CLOSED 2026-09-13 05:00Z — LATENCY.md §13 verdict; B21/B22 are the open follow-ups) | after B1–B4 and A1 land and are deployed: measure against PROPOSAL.md §3 targets. Bot: per-verb latency histogram (p50/p95/max) for view/stats/sfbs/add/swap!/rename/like/rank, cold first command after a restart, ruled (magic) layouts, under a burst (headless harness `bot/scripts/e2e-headless.mjs` + `scripts/e2e/bot_db_site.mjs`). layoutdb: `/v1/meta` 304 path, record GET, PUT round trip, long-poll wake latency, import tick duration. Publisher: edit → pointer swap (plain and magic layouts), daily fold time. Site: pointer age after an edit, own-publish visibility, page load in pointer mode (Lighthouse), tab poll pickup. Memory/CPU on the Fly machine over 24 h. Report as `design/layout-db/review/LATENCY.md` + a page on the site, with a pass/fail per requirement R1–R5 and what to change if anything fails. | round 1 written: `LATENCY.md` (bot verbs from the laptop harness: reads p50 20–27 ms, writes 360–510 ms; layoutdb 304 path 45 ms; boot 3.6 s; site fetches ≤ 280 ms; long-poll was degraded → fixed; edit→pointer pending the publisher's first pass). Round 2 = Fly-side `/health.latency` after real usage + publisher timings. |

### Wave 4
| id | slice | status |
|---|---|---|
| W1 | **akldb.org** — the DB's own website (public read-only + moderation view); plan `design/akldb-site/01-plan.md`; Worker `akldb-site` in account `akl`, dir `dbsite/`. W1a scaffold+public+auth+proxy **landed 5367d484f** (SITE-1..11; + lead fix: logout CSRF gate); W1b owner+admin UI after L5 | **W1a/W1b/W1c all landed** (796507c42): owner actions, author display names, row-click, moderation console (Layouts/Authors/Bans/Link queue/Admins/Import; no Set likes per H24), readable event kinds; Docs overhaul. Open: copy sign-off (`db/site/src/copy.ts`), `[contact: …]` line in adoption.md §2.3, no route lists tombstoned layouts (Restore reachable only via the retained id / event log) |
| W2 | governance: second admin row, domain in `akl` account | todo (saltorbit) |
| W3 | spark/1 format discussion (saltorbit: "not done yet") — own design round | **DECIDED 2026-09-13** (rounds 1–3 on branch `ldb-geometry`): `design/layout-db/23-geometry.md` — `board` = one word `ansi\|iso\|ortho\|colstag` (ansi default, no stagger/split), thumbs by label (`TB` gone), fingerings by name (angle/nokwts/meteorite, ansi-only, cmini looks kept), bot `!sp add mine iso` + `board`/`fingers` lookup·preview·write, faithful import, spark/1 in place + wipe/re-import; space side → #333; rendered page `design/artifacts/ldb-geometry.html`. Open Q1–Q4 have defaults; implementation per §10 starts from that branch |

## Agent recipe

1. Spawn with `isolation: "worktree"`; the brief's first two steps are `git reset --hard ldb-arch-review` and `npm ci` (+ `cd db && npm ci` / the bot build steps from `bot/README.md`).
2. Run slices in parallel when they touch disjoint files; name the sibling slices in each brief so agents avoid each other's files. Brief = the slice row above + the relevant audit section + "state invariants, add tests, run the suite, commit on your branch, do not push, do not deploy, report branch + sha + test counts".
3. Lead reviews the diff, runs the suite once more, rebases the slice onto `ldb-arch-review` (script), fast-forwards, pushes, updates this ledger, removes the worktree.
4. Deploy at pause points only: `cd db && npx wrangler deploy` (prod `akl-db`, delegated), `flyctl deploy` for spark (delegated), the db-alias site via the scratch-copy recipe in `design/pipeline-313/02-handoff.md` §Recipes.

## Log

- 2026-09-13 20:05Z · **Day closed.** Everything on `ldb-arch-review` is deployed: spark v62, akl-db API 1.11 (CI), akldb.org (CI, Clients tab + history-free docs), db.cmini-web.pages.dev at eaa535241 on the NEW data line. Landed today (lead + 17 Sonnet slices): S1 spec + adoption docs, L8, L9, I1, B26/B27, B28b/B28c/B28d, B30, B31, B32, B33 (+layout revision), B34, API-V (+1.8..1.11), IG, CG, CT, RN, SH (Safari hover), VE (validate errors), LR (line roll), aklgg-publish (cmini-web-71). Open for saltorbit: PR #334 (weekly GitHub backups → main), the 72 `web/src/copy/db.ts` strings, delete the stopped iad machine + volume after 09-14, revoke the latency client. Open engineering: B20 (publisher notices an external swap — still restart-after-rebuild), B21 (seed cells from the base → shared CPU), revert UI on akldb.org, Changes-page actor filter, #333 space side, D3 doc retirement.

- 2026-09-13 17:45Z · **B28b dry-runs on Fly**: #1 died on a missing `sync_cmini_data.py` in the image (→ LDB-B267, .dockerignore allowlist test); #2 on `build_web.py`'s missing `data/corpora` (→ allowlisted, 6.7 MB); #3 ran the whole assembly (compute 927 s on performance-8x + extras 59 s + upload 20 s) and the completeness gate reported ONE missing file, `layout_authors.json` — today a verbatim copy of the repo's cmini-era name-keyed `data/layout-authors.json`, not in the image and wrong for a ULID base → **B28c** (agent `b28c-authors`): derive it from the DB. v58 briefly showed pending link submissions in `view` (lead misread "user-submitted link"; saltorbit: "links should not show until approved") → reverted in v59 (~3 min live); snapshot v5 carries `link`/`pendingLink` so pre-existing approved links appear (LDB-B235b). API 1.11: budgets removed from every public route (LDB-A14).

- 2026-09-13 17:02Z · **spark v55/v56/v57 deployed by the lead** (v55: B32 links + B33 view rules/link + `unlink` + B34 engine hash + B28b rebuild entry; v56: B33 revised layout per saltorbit -- one fence, rules body under the grid, link line above the akl.gg line; v57: LDB-B267 image ships the assembly scripts). S4 on v55: "recovered -- 0 mismatches, 44 stale (earlier wasm build)". First B28b dry-run (v55 image, performance-8x): compute 4,177 layouts in 822 s OK, then died -- `.dockerignore` allowlist lacked `sync_cmini_data.py`/`build_magic_rules.py` (+ tri_table/layout-dates/magic_rules data) → fixed with a source-derived test; second dry-run running on v57. CG landed (API 1.10). Lead slips today: reclaimed two agent worktrees mid-task (spec-22's redo lost → re-spawned as adoption-docs; S4's mid-rebase → branch intact), and a `;`-chained `branch -D` after a failed ff-merge deleted CG's branch (recovered from the dangling commit) -- rule: never chain a destructive step after a merge/landing with `;`.

- 2026-09-13 16:09Z · **B30 confirmed live**: the boot purge on v54 logged `checked 3198, purged 1314` — 1,314 poisoned magic cells (41 % of the magic-cell store) had been serving unapplied-magic stats. Landed since 15:55Z: adoption docs (S1), IG (+API 1.9), B32 links, B33 view rules/link + `unlink`. Ready for spark v55 once saltorbit signs the six link-verb strings.

- 2026-09-13 15:55Z · **spark v54 deployed by the lead** (saltorbit: "i give you permission to run the deployment yourself"): B26/B27, B30, B31 live; `DB_BASE_URL` → https://api.akldb.org; boot 3.0 s from snapshot, publisher index 4,177. First `view twirl` after boot took 4.1 s = the poisoned cell recomputed through the magic engine (compute 2.7 s), then store hits ~1 s. S4 reported 42/52 like-for-like mismatches → B34. Backups: first GitHub release `akldb-backup-2026-09-13` taken by hand; weekly workflow = PR #334 (main). Landed today so far: S1 spec, L9, L8, B31, API-V (+1.8 fix-forward), I1, B26+B27, B30, B32 links. Merged-tip gates green (web 6,788; bot 1,578; db 17,994+contract).

- 2026-09-13 15:20Z (lead cmini-web-65 resumed; saltorbit: "keep going on whatever is remaining … on a worktree") · Confirmed the single common branch: `ldb-arch-review` contains ldb-geometry + B28a + all site work (only 2 superseded `ldb-formats` commits outside it: #325 loop probe, replaced by B11's loop_lag). db alias = deployment 9878d28c (d0aeea9c8); spark v53 = B29 image (bot/ at c34395d57) in lax. Dispatched 9 Sonnet agents in isolated worktrees: B28b, I1, B26+B27, L8, L9, S1 spec, API-V, B30, B31. Decisions from saltorbit today, all applied/in flight: **api.akldb.org** is the API hostname (custom domain on akl-db, adefee45d; workers.dev kept for deployed clients; docs/defaults swept 1bee86eff — TLS cert still issuing at 15:20Z); **`wait=` for every registered client** (8a5f949c7, LDB-CH3 restated, `feed:wait` implied); **no magic fallback ever** (B30); **incomplete commands → help** (B31); **API versioning** (API-V); **docs without history** (S1 follow-up). Chrome check of the publish-sheet board chips (I-38x CSS, ldb-geometry site slice): PASS at desktop — pill row right-aligned, selected chip in the draft accent, no overflow. S1 spec landed d844c949e. Still owed to saltorbit: copy sign-offs (`db/site/src/copy.ts`, adoption §1.1 keypair wording — done under his "delete history" ask —, versioning section), the cell-store poison count (my `flyctl ssh` is denied), `flyctl deploy` for the bot slices once landed, delete the stopped iad machine after 09-14.

- 2026-09-13 11:00Z · **CUTOVER COMPLETE** (cmini-web-19 verified step 6): db.cmini-web.pages.dev (deployment 9878d28c from d0aeea9c8) boots from base `20260913T104711Z-e5fac4119679`, 3,763/4,125 layouts listed (meme filter; the 14-row delta vs the old base = the wipe), stats populated, no console errors. Prod layoutdb on the new spark/1, spark on the new reader in lax with the wasm cap, pointer on a complete base. Open: B28b (ids), B26, B27, L8/L9/I1/#333 (other sessions), delete the stopped iad machine + volume after 24 h.
- 2026-09-13 10:50Z · **Complete base published**: `20260913T104711Z-e5fac4119679` (167 files, 571 MB, uploaded in ~3 min), pointer swapped (db_seq 45251, overlay null), bot restarted → index 4,177 published, S2 clean. cmini-web-19 redeploys the db-alias site from ≥ fad402ed3 (step 6 retry). B28b (unify ids on the DB id inside rebuild.js) is the follow-up.
- 2026-09-13 10:35Z · B29 landed + deploying: engine worker recycled through B95's reboot path when wasm linear memory > 384 MB (checked every 5 computes / 30 s), counters on /health.
- 2026-09-13 10:15Z · **Memory mystery solved by B25's breakdown**: 781 MB is the wasm engine's linear memory (never shrinks), grown by the eager sweep. B29 dispatched (byte cap + recycle). B25 row: growth attributed.
- 2026-09-13 09:50Z · Cutover step 6 blocked: db-alias site empty — the rebuilt base lacks the catalog artifacts (B28). Agent building a complete base; publish + pointer swap to follow.
- 2026-09-13 09:40Z · **Cutover step 5 DONE**: spark on the new-shape reader (b64c1c7c2) in lax; rebuild #3 (new ids/shape) 4,177/4,177 in 936 s → base `20260913T093648Z-33db407ad3cf`, pointer db_seq 45251, overlay null; bot restarted, index 4,177 published, S2 0 missing publication. cmini-web-19 runs step 6 (db-alias site build). The spark/1 rewrite is live end to end on prod layoutdb.
- 2026-09-13 08:19Z · **INCIDENT 4** (lax machine, v51): cmini-web-19's wipe + import started while the OLD bot ran → feed delivered thousands of deletes + new-shape creates; publisher batch mode recomputed them 200 at a time; main-thread stalls 5–16 s in phase `compute` (schema-3 result JSON parsed on the main thread — B26), `/v1/meta` timeouts, RSS 83 %. Stopped the machine at 08:19Z until step 5 (deploy starts it on the new image). Rule for the runbook: stop the old bot at the START of a wipe/import, deploy the new one before the import completes.
- 2026-09-13 08:46Z · **Smart Placement pinned the prod route to the version it was analysed with** (5343a268, the 06:27Z deploy that enabled it): two later CI deploys reached 100 % and served the new schema on their version preview URLs, but `akl-db.akl-58a.workers.dev` kept serving the old one (and the first re-import wrote 4,177 old-shape rows through it). cmini-web-19 removed `[placement]`; after that deploy DEN and LAX both serve the new schema. Rule: never enable Smart Placement on a Worker that is actively being redeployed; if ever retried, verify the prod hostname's version after the NEXT deploy. B24 step-1 result: no latency gain measured either.
- 2026-09-13 09:25Z · **Cutover step 5** in progress: spark deployed from b64c1c7c2 (new-shape reader, SNAPSHOT_VERSION 4) → boot from the fresh dump OK (4,177 layouts, S1 clean); bot stopped for the rebuild; `rebuild-on-fly.sh` fixed to read env from a stopped machine; rebuild running in lax.
- 2026-09-13 06:36Z · Memory on the lax machine with the publisher idle: 1,326 MB at boot → 927 (06:19) → 897 (06:29, 06:36), heap flat ≈ 420 MB. So the 300 MB/h climb on iad tracked publisher activity (ticks / batch compute / worker jobs), not idle time. B25's breakdown log will attribute it during the post-cutover rebuild + first ticks.
- 2026-09-13 06:45Z · B25 landed (memory breakdown + log). Tip frozen for cmini-web-19's step 4 once announced; my step 5 (spark deploy, rebuild, restart) follows their "step 4 done".
- 2026-09-13 · **Like-override removal landed** (82dc276c4, H24: "likes should always be tied to the users who liked it"); cmini-web-19 notified (their bot slice rebases over it; SNAPSHOT 4 / WIRE 6 theirs). Two production site bugs after W1b, both fixed + pinned: (1) row-click overlay — a `position: relative` <tr> is not a containing block in Safari, so the last row's stretched link swallowed every click (→ /l/zxcvb, nav dead); now JS delegation, no overlay (74eef47c5, SITE-20 rewritten). (2) signed-in layout page read `.likes` off the `{user_ids}` likes wire → TypeError in a memo → Solid disposed the whole root ("none of the buttons work"); fixed + SITE-21 pins the wire against `db-responses/likes.json`; SITE-22 adds an `Errored` boundary under the header so a page error can never kill the shell (d7e6b6874). Lesson: every wire shape the site reads must be tested against a recorded fixture (the agent had hit the same class with `/v1/authors`).
- 2026-09-13 06:20Z · Idempotency-Key reserve measured: +70–100 ms per write; Worker write path ≈ 300 ms from anywhere; Fly path adds 150–250. Remaining lever = one D1 batch per write in `core/events.ts` (after the format rewrite lands there) + drop the bot's post-write re-read. LATENCY §14b.
- 2026-09-13 06:10Z · **spark moved iad → lax** (saltorbit: "quiet hour, make those changes"); reads 55–65 ms, write PUT 530–700 ms (−15 %, not the 2.5× hoped — laptop oracle was a 400). Memory alert 1,622 MB (82 %) on the iad machine before the move; fresh lax boot already 1,326 MB with heap 389 MB → RSS is dominated by off-heap (cell store now full, two worker threads) — B25 dispatched to break it down and find any growth.
- 2026-09-13 · **W1b landed** (796507c42): owner actions (like, rename, delete/restore, transfer, link submit/clear; inline confirms), author display names everywhere (liked rows keep ids), whole-row click on Home, moderation console at /admin (6 tabs, DOM-free for non-admins, SITE-13..20), dev-only `?mock=admin|owner` proven absent from prod builds. Real-DB QA found+fixed: `/v1/authors` is an object not an array; 403s showed as empty lists; `wrangler dev` needed an explicit ASSETS binding. **saltorbit confirmed the like-override reversal (H24)** — removal agent running (db + bot + site api types). Copy button moved top-right on Docs (d2bbd2c12). API gap: no list of tombstoned layouts.
- 2026-09-13 05:55Z · Write latency root cause: geography (D1 in WNAM, bot in IAD; each D1 statement ≈ 65 ms cross-country; laptop via DEN: record GET 25 ms, write 250–280 ms). Fix = move the bot machine west (B24), tomorrow. Round 2 complete.
- 2026-09-13 05:45Z · B22 paced round on v51: reads handler median 79 ms + Discord post ≈ 300 ms; writes handler 1.0–1.3 s = layoutdb PUT 640–760 ms from Fly (+ fresh + resolve). Smart Placement pushed for the Worker (B24 step 1); re-measuring.
- 2026-09-13 · **W1c landed** (ebbdbc881): Docs page = generated API brief + two inline-SVG sequence diagrams (auth/ownership, trusted client; measured in Chrome) + Copy-as-Markdown guide + `/adoption.md` raw; adoption.md §2.3 "Becoming a trusted client" (contact line `[contact: …]` for saltorbit). Site fixes today: Event log paged from since=0 → now backwards from `/v1/meta.seq` (SITE-19); nowrap cells; sign-in contrast; payload collapsed + Copy; "Layouts"/"Event log" copy; site CI job decoupled from the db suite (stale CI deploys clobbered hand deploys twice). **Bot: LDB-B224** (644c7ef5f) — S1 flagged the one layout liked between the 03:00Z dump and each boot as divergent and regressed its count; `likeSeq` per record now exempts likes above the dump head. **Relayed saltorbit decision (via cmini-web-65/19, awaiting his direct confirmation here): NO admin like override — remove `admin.likes_set`/`like_adjust` everywhere; likes stay per-user rows (H24).**
- 2026-09-13 06:15Z · **H24** (saltorbit): likes are per-user rows, never a settable number — recorded; import-plan like_count comparison flagged for audit (handed to the import/moderation owners via cmini-web-19).
- 2026-09-13 06:10Z · **saltorbit reversed the like override**: moderators may not change like counts. Relayed to cmini-web-19 → 6b (authors of ldb-b197) to REMOVE it rather than fix the S1 divergence. REQUIREMENTS.md updated.
- 2026-09-13 06:00Z · B22 landed. Other sessions are pushing to ldb-arch-review too (akldb-site slices; `c900dc0a3` bot like-override left tsc/eslint red — fixed here). Deploying v51, then a `--pace 1500` tester round for the clean write-verb numbers.
- 2026-09-13 05:40Z · Cutover for the spark/1 rewrite (cmini-web-19, runbook in `design/layout-db/23-geometry.md` §10), saltorbit via that session: wipe + fresh cmini import is fine (layoutdb not in use; native rows lost, accepted). Order: push ldb-arch-review (Worker) → wipe + import → **force a dump** (daily otherwise; both rebuild.js and the bot boot read it — flagged) → my spark deploy → `rebuild-on-fly.sh` + machine restart → db-alias site build. Bot slice there bumps SNAPSHOT_VERSION so the replica never boots from an old-shape snapshot; it starts after B22 lands.
- 2026-09-13 05:20Z · Coordination with another Claude session (cmini-web-19) on branch `ldb-geometry` (off ldb-arch-review): rewriting spark/1 (board word, ordered keys with duplicates, no TB/free, magic `repeat|char`), then bot verbs, then `link` (#323) — i.e. W3 is happening there. Agreed: common branch = ldb-arch-review; they fast-forward when green; their bot/ slice lands after B22; they take LDB-B230+. Warned: a push to ldb-arch-review auto-deploys PROD layoutdb (db.yml); spark deploys manual, clean tree, performance-1x pinned; format change needs db+bot in one window.
- 2026-09-13 05:10Z · LATENCY published as an artifact page (`design/artifacts/ldb-latency.html`, https://claude.ai/code/artifact/83cae7c5-3795-49f3-8cf2-4551ea8d65bc); index.json updated — the db-alias site copy goes out with the next site deploy.
- 2026-09-13 05:00Z · Final steady-state round on performance-1x: 0 stalls, heap 270 MB, 12/49 over 1 s all explained by the Discord send bucket under the tester's burst (B22 to prove it). Round 2 closed in LATENCY §13; no more deploys tonight (v50 is live: heap cap 1.4 GB, batch compute on, performance-1x, B217 index merge, B218 heap on /health).
- 2026-09-13 04:55Z · Steady-state rounds on v49/v50: still 16–17/49 over 1 s with 4–7 s `idle` stalls. NOT GC (heap 255/260 MB after raising the cap to 1.4 GB, LDB-B218) — **shared-cpu-2x throttling**: CPU probe 45 M iter/2 s vs 2.2 B unthrottled; the eager wasm sweep (3,236 layouts missing cells post-rebuild) burns the burst credits continuously. Machine back to performance-1x, pinned; B21 (seed cells from the base) is the path back to shared. Final round running on performance-1x.
- 2026-09-13 · **L5 landed** (c900dc0a3): bans / like override (`like_adjust`) / admin author name / `link` + queue; db suite 15,912 green, 48 new conformance fixtures, adoption guide §10. saltorbit picked direction A for the site and cut the board renderer + akl.gg link-out (c1a161bc0, SITE-12); the four direction mockups live in `design/akldb-site/mockups/` (canvas: claude.ai artifact 42f5c8bc). **Known gap:** ~20 bot commands still display `likedBy.size` instead of `like_count` (only `mirror!`/`history` fixed) — B-follow-up. B197 confirmed in the running spark v50 bundle (deployed by the bot session after landing). W1b launched. Site: sign-in link dark-mode contrast fixed (249811e8b), deployed by hand; CI redeploys are idempotent.
- 2026-09-13 04:50Z · **Found LDB-B217**: the index loader merged base/overlay per FILE, so the overlay's layouts.json hid the base's 4,179 rows → after the rebuild the live publisher re-queued everything (pre-existing; masked while the base had no hashes). Fixed + test. Deploying v49 = B217 + batch compute ON + shared-cpu-2x + S3 timeout 120 s (saltorbit: "do this").
- 2026-09-13 04:35Z · Post-swap: pointer intact (new base + a 25-layout overlay from the live tick that was in flight). Live publisher did NOT re-plan (stale in-memory index → B20); restarted the machine to reload the index. Steady-state tester round queued after the restart.
- 2026-09-13 04:23Z · **First real B17 rebuild swapped the pointer** (16.8 min, 4,179/4,179, smoke ok). Live bot RSS 1.36 GB (69 %) right after — watch; expected to drop once its overlay state resets on re-plan.
- 2026-09-13 04:20Z · B17 dry run #2 succeeded (15.6 min, all 4,179; oracle equivalent). Real rebuild launched — swaps the pointer to a fresh full base, emptying the live publisher's backlog (and the contention behind LATENCY §11). Then: steady-state re-measure with the tester bot.
- 2026-09-13 · **W1a landed** (5367d484f): `db/site/` Worker `akldb-site` (sealed-cookie session, Discord OAuth, `/api/v1/*` proxy, public pages Home/Layout/Author/Changes/Docs), CI `site` job in db.yml, boundary + ciwiring extended; 65 site tests, db suite 15,813 green. Chrome QA found+fixed: `next_cursor` paging (list truncated at 1000/4179), a `ref` prop colliding with Solid's ref-forwarding, docs mobile overflow. Lead review fix: `/auth/logout` needs the CSRF header. Infra done earlier the same night: akldb.org zone active in `akl`, Worker created (placeholder), custom domains bound, 3 secrets set. First real deploy = this push's CI.
- 2026-09-13 03:50Z · B17 dry run #1: compute 4,179 layouts in **963 s** on performance-8x (as predicted), catalog rows derived, then ONE 30 s R2 PUT timeout killed the run (R2 slow; the live bot's upload phase was 129 s the same tick). Fixed LDB-B216 (PUT retries w/ backoff, 412 = landed, `REBUILD_S3_TIMEOUT_MS` 180 s, failure summary keeps counts). Wrapper fixed too (`--`, live env copy, Machine ID parse, bash). Deploying v48, then dry run #2.
- 2026-09-13 03:40Z · Real Discord round (spark-tester → prod, 49 commands, during a tick + the rebuild): 19/49 over 1 s, image 5–6 s — CPU starvation on 1 vCPU (worker thread shares the core), not handler time. LATENCY §11. Plan: finish the backlog with B17 (dry run at 3,200/4,179 in 734 s), then re-measure at steady state; passes never on the bot machine again.
- 2026-09-13 · B16b+B17 landed; bot 1,268 green (the `[LDB-B200]` notify-dedup test flaked once under full-suite load, passes alone and on rerun — harden later). Deploying v47 so the image has `dist/rebuild.js` + the `--layouts` Python; then `DRY_RUN=1 bash scripts/rebuild-on-fly.sh`.
- 2026-09-13 · B14 landed (S2 waits for the publisher index / reports "unknown" when degraded; S4 compares like-with-like by stored engine; worker crash → one alert, rejections routed; watchdog onset memory persisted to /data across restarts). Deploying v46 = B14 + B16a (batch flag OFF) + LDB-B168 DM mirror.
- 2026-09-13 · B16a landed. B16b (wiring, `PUBLISHER_BATCH_COMPUTE` flag default off until the on-machine oracle passes) + B17 (rebuild entry + `scripts/rebuild-on-fly.sh`) dispatched as one slice.
- 2026-09-13 · v45 first tick: publish ok, **0 stalls**, loop-lag max 473 ms, merge-overlay 46 → 23 s. B12 confirmed in production. LDB-B168 (alert DMs mirrored to stderr) committed, ships with the next deploy; a persistent log monitor now mirrors the DMs into the lead session.
- 2026-09-12 · **Naming: the product is now `akldb`, site `akldb.org`** (saltorbit). W1 + L5 kicked off from `design/akldb-site/01-plan.md` (Fable-reviewed plan): L5 and W1a in parallel Sonnet worktrees, W1b after both land. `akldb.org` DNS still at Namecheap — saltorbit adds the zone to the `akl` account (§7 of the plan).
- 2026-09-13 03:35Z · **INCIDENT 3** (v44, ~02:38–03:30Z): B12's `defaultPublisherWorkerUrl` said `./worker.js` (dist root) while the build emits `dist/publisher/worker.js` → every tick failed "Cannot find module /app/dist/worker.js" + an `unhandledRejection` from the crash path; the bot itself kept answering. Fix LDB-B167 (92356a8c4, dist-relative like the engine worker) deployed as v45 03:33Z. Lesson: a worker-URL change needs a built-dist assertion, not just unit tests (B167's test checks `dist/publisher/worker.js` exists beside `dist/main.js`). B14 dispatched (S2 waits for the publisher index, S4 like-with-like by cell source, worker-crash rejections routed, watchdog onset memory persisted across restarts).
- 2026-09-13 03:10Z · B12 landed: publisher heavy JSON/hash work on a `worker_threads` worker (main-thread gap ≤ 100 ms on 105 MB deltas in tests); B15 folded in. Deploying as v44 (fly.toml now pins performance-1x). B16a (grouped compute, Python + batchCompute.ts) running; B16b wiring + B17 next.
- 2026-09-13 03:00Z · **Found**: the v42/v43 deploys reverted the machine to fly.toml's `shared-cpu-2x` (a deploy applies `[[vm]]`) — the machine has been throttled again since 01:56Z, which explains compute 203 s/tick and the p95 contention in LATENCY §10. Resized back to performance-1x and pinned in fly.toml for the pass. LATENCY §10 numbers were taken on shared-cpu-2x, not performance-1x — re-run after B12.
- 2026-09-13 02:50Z · v43 first tick: smoke 151 → 39 s, worst stall 111 s → 2.3 s (LDB-B180 confirmed in production). Remaining 1–2.3 s stalls are CLI-result parse + merge → B12.
- 2026-09-13 02:40Z · edit→visible during backlog ≈ 20 min (deploy-confounded; LATENCY §10). B16 (grouped compute, scaling tick size) designed with saltorbit; queued after B12.
- 2026-09-13 02:30Z · v43 deployed (LDB-B180 smoke fix + B13). Fly-side per-verb harness run: p50 65–80 ms every verb (image 428); p95 outliers compare 3.1 s / sfbs-magic 1.0 s = CPU contention with the CLI child on 1 vCPU → B15. LATENCY.md §10. Deploy restarts cost the publisher two in-flight ticks (pointer frozen 01:59→02:24Z).
- 2026-09-13 02:15Z · B11 histogram live: one **111 s** main-thread stall per publish tick in phase `smoke` (tombstones × files re-parse) → fixed LDB-B180 (5bdf9216b) + B13 landed (2c8931754), deploying together. LATENCY.md §9b. B12 told to rebase and to take smoke.ts + the CLI-result parse too.
- 2026-09-13 01:58Z · v42 first `/health`: S1 clean ("1 newer than dump (not divergent)"), loop lag since boot p50 20 / p99 78 / max 478 ms with 0 stalls before the first publish; S2 boot-order and S4 like-with-like gaps → B14.
- 2026-09-13 · saltorbit: deploy granted, all feature work on ONE branch (no main merge yet), keep going on latency. #330 fix cherry-picked onto ldb-arch-review as f11315b62 (module is `core/` here → clock injected from app/main.ts, no Date.now default); PR #332 closed pointing here. First deploy attempt FAILED because `flyctl deploy` uploads the WORKING TREE — my unresolved cherry-pick conflict markers went into the image build (lesson: deploy only from a clean tree; land-slice/lead-commit already guard this). Redeployed clean: spark v42 (B10+B11) 01:56Z. B12 (publisher off main thread) and B13 (harness on Fly) dispatched in parallel. edit2pointer probe running during the backlog.
- 2026-09-13 · B11 landed: `health/looplag.ts` (since-boot + last-10-min `loop_lag_ms`, 200 ms drift-check stalls with publisher phase), per-tick publisher health fields + phase timings, `history.json` `db_seq` (was a missing type field). B12 hot spots named: `overlay.ts applyDelta` (sync parse+stringify per cell table), `manifest.ts buildManifest/sha256Hex`, `fold.ts mergeOneFile`. Adjacent gap flagged: publisher health check only goes red on smoke failure, not on `catalog_rows_failed`/`pointer_contention`. Spark deploy (B10+B11) still needs saltorbit.
- 2026-09-13 · #330 fix verified in Chrome on a local build (forced init failure: 3 created / 3 terminated, cooldown-bounded; success path computes) → PR #332 `fix/330-swap-worker-leak` against main (saltorbit merges). Heap-analysis scripts kept in `review/scripts/heap{diff,new,maps}.mjs`.
- 2026-09-13 · LATENCY.md §9 interim: performance-1x publisher ≈ 25 layouts / 4 min (≈ 10.5 h first pass); feed timeouts proven to be bot-side event-loop stalls (laptop long-poll 25.4 s flat); B11 dispatched. Spark still on v41 (B10 undeployed: deploy denied by the permission classifier twice; saltorbit runs it).
- 2026-09-13 · #330 root cause found (Chrome + Safari `footprint`): `web/src/data/swap-engine.ts` never terminates a swap worker whose init handshake failed, and every compute/hover kick retries with a fresh worker → worker-per-attempt; 9 leaked timed-out workers = +327 MB renderer RSS (≈ 36 MB each, wasm + Go runtime, invisible to heap snapshots); saltorbit's 4-day Safari tab had 6 worker threads. Fix agent on a branch off main (terminate on failure + backoff + 60 s worker-side ready wait).
- 2026-09-12 · B10 landed (b8cb1f2e6): bot 1,168 green. spark-bot resized to performance-1x for the backlog pass (saltorbit: yes). Spark deploy of B10 blocked by the permission classifier; publisher continues on v41.
- 2026-09-12 · B8 in review (branch `worktree-agent-ac101a6c435d96188`, `8858f53fe`): `publisher/index.ts`'s `publishPlan` split into an outer chunking loop (`orderForPublish` + a `do...while` over `publishOneTick`) and the pre-B8 body (now `publishOneTick`, unchanged logic, parameterized over one tick's own bounded slice). `PUBLISHER_TICK_MAX_LAYOUTS` (default 25) bounds a tick's `toCompute`; tombstones bundle into tick 1 only (cheap, unbounded); a multi-tick pass (`ordered.length > max`) pauses the eager sweep via a new `PublisherDeps.eagerSweep` hook (`cache/cells.ts`'s `eagerPause`/`eagerResume`, threaded from `main.ts`) unless `PUBLISHER_BACKLOG_PAUSES_SWEEP=false`. `PUBLISHER_CLI_JOBS` (default 2) threads into `ComputeDeps.jobs` via a new `main.ts` `computeDepsFrom` (compute.ts already supported `--jobs`; nothing had ever configured it, so it silently sent `--jobs 1`). `computeOne` times + logs each compute and keeps a rolling mean. Found + fixed along the way: `cache/eager.ts`'s `resume(){ this.start(); }` throws once `resume` is passed around unbound (exactly how `main.ts`/`cells.ts` now do) -- replaced with a shared named `startPump`, caught by a new pause/resume wiring test. LDB-B141 (bounded ticks), B142 (restart recovery), B143 (recency ordering), B144 (--jobs + compute timing), B145 (sweep pause/resume), B146 (sweep never redundantly recomputes a CLI-covered hash -- true by construction via `ensureCell`'s engine-agnostic content-hash hit, pinned down with a direct test rather than left incidental), B147 (`/health.publisher` backlog/throughput fields) registered with tests (`tests/publisher/tickChunking.test.ts`, `tests/cache/cells.test.ts` additions, `tests/publisher/compute.test.ts` additions, `tests/main/bootRuntime.test.ts` additions). Bot suite: 94 files / 1,150 passed / 73 skipped (the same pre-existing `tests/health/index.test.ts` ms-boundary flake B7 already documented); `tsc --noEmit` + `eslint` clean. Did NOT touch `scripts/fast_sync_patch.py`'s work-dir reuse: the workdir is already shared/persistent across runs by construction (`main()` never passes `--workdir`, so `run()` always defaults to the same `build/fastsyncwork/`) -- but `build_group_workdir` unconditionally `shutil.rmtree`s and rebuilds each `group-<corpus>-<flag>/` dir every call regardless. Reasoned (could not run the real CLI in this worktree): that rebuild is filesystem churn only (rmtree/mkdir/symlink/small config write), cheap next to the dominant cost -- a FRESH subprocess per (corpus, spacegrams) group re-parsing its corpus JSON from disk, unavoidable without a persistent daemon or a Go-side parse cache (out of scope, a change to the real tool's own internals). It's also not unit-testable in this worktree (needs a real corpus + built Go binaries; `scripts/tests/test_fast_sync_patch.py`'s own docstring says as much for the whole grouped-harvest path). Flagged instead: the always-shared workdir is a LATENT CONCURRENCY HAZARD if `PUBLISHER_CLI_CONCURRENCY` is ever raised above its default of 1 -- two concurrent `fast_sync_patch.py` processes would race rmtree/recreate on the same group directories. NOT deployed, NOT pushed.
- 2026-09-12 · B7 in review (branch `worktree-agent-ae72910c80dfb55dd`, `9554b5664`): fix for INCIDENT 2 (below). `publisher/bucket.ts` gained `readNamedFiles` (fetch only the named files a manifest lists, skipping absent ones -- never a guess); `loadPublishedIndex()` uses it against BOTH base and overlay manifests for `layouts.json`/`rules.json` only, replacing `currentFullFiles()`'s full-base `readFiles`. `publishPlan`'s CAS loop now reads the CURRENT OVERLAY ONLY (never base) -- an overlay is bounded by construction (at most `corpora x boards x spaces` cell tables, "changes since the last fold"), so reading it whole is safe where reading a base whole was the incident; a tombstoned id whose only footprint is in the base (untouched by any overlay tick since the last fold) is left to the daily fold's own reconciliation rather than ever reading the base at publish time. `fold.ts`'s `runFold` rewritten to stream one table at a time (bounded worker-pool concurrency, default 4) instead of `readFiles`-ing the whole base AND the whole overlay: small catalog files are merged fully (cheap); each `mana2/*` table is either server-side `CopyObject`'d (untouched by the overlay AND no pending tombstone's own board could have dirtied it, per `dirtyTombstoneCellNames` looked up from the base's pre-fold `layouts.json` row) or downloaded+merged+uploaded and dropped before the next -- `s3.ts` gained `copyObject()`. `s3.ts`'s `createS3Client` now takes `timeoutMs` (env `S3_REQUEST_TIMEOUT_MS`, default 30s) and threads `AbortSignal.timeout` through every request (put/get/head/delete/list/copy). `main.ts`: `bootRuntime` no longer builds+awaits a `publisherReady` promise before `ensureFresh()` -- `publisher.start()` is called (fire-and-forget) from `openBackgroundWork()` instead, alongside the eager sweep, well after `client.login()`'s call site; `Publisher.start()` itself races its own index load against `publisherStartTimeoutMs` (config `PUBLISHER_START_TIMEOUT_MS`, default 120s) and, past that bound, marks `degraded()` (surfaced on `/health`'s `publisher` check and `PublisherHealthSource`) and retries every 5 minutes via `setInterval` until it succeeds. LDB-B136 (bounded index load), B137 (overlay-only publish), B138 (streamed fold + CopyObject), B139 (boot never awaits start(); degraded/retry), B140 (S3 timeout) registered with tests: `tests/publisher/boundedReads.test.ts` (a synthetic 4,200-layout base with a full `mana2/*` table set -- index load reads ~1MB of ~15.8MB total, zero `mana2/` GETs; a publish tick that computes one layout and tombstones a base-only one issues zero base `mana2/` GETs), `tests/publisher/foldStreaming.test.ts` (a fold over two board words' worth of tables downloads+merges exactly the tombstoned board's full universe and `CopyObject`s every other board's tables, peak concurrent GETs bounded by the configured worker count), `tests/main/publisherNeverBlocksBoot.test.ts` (`bootRuntime` resolves in single-digit milliseconds with every R2 request hanging forever, answered only by this process's own abort signal; the publisher reaches `degraded()` without ever blocking), `tests/publisher/s3.test.ts` additions (real local HTTP server: `copyObject` round-trips without a GET; a request to a never-answering key is aborted at the configured bound, not the test's own timeout). Existing publisher/fold/index tests adjusted where they assumed full-base reads (none did, in this worktree's own fixtures -- every existing test already used a small in-memory base, so all 90+ pre-existing publisher tests pass unchanged). Bot suite green: 93 files / 1,133 tests / 73 skipped (one pre-existing, unrelated memory-RSS-reading flake in `tests/health/index.test.ts`, reproduced and confirmed pre-existing by rerunning in isolation); `tsc --noEmit` + `eslint` clean. NOT deployed, NOT pushed, NOT merged -- this worktree's own branch only. Uncertain/unverified: real R2's `CopyObject` semantics (`x-amz-copy-source` header) were exercised only against the hand-written fake HTTP server in `tests/publisher/s3.test.ts`, never a real R2 bucket (matches B2's own original caveat about this SigV4 client); the exact ~553-file/~2GB figure from the incident narrative doesn't literally reproduce in this worktree's own schema (`corpora x boards x spaces` caps a real base's own `mana2/*` file COUNT at ~78, though each file can be large in bytes at catalog scale) -- the fix targets the real mechanism (never touching those tables at all) rather than that exact number.
- 2026-09-12 · B5 in review (this worktree, branch `worktree-agent-a8d792d5a4f9cac13`, rebased onto V0): `scripts/overlay_rows.py` (calls `build_web.py`'s `layout_meta_and_keys` directly, stdlib only) + `bot/src/publisher/pyrows.ts` (`deriveCatalogRows`, one batched python3 subprocess per publish tick, wired into `publisher/index.ts`'s `publishPlan` BEFORE the pointer is read so a Python failure never writes a partial overlay). `OverlayLayoutRow.meta` widened from B2's punted stand-in to `@akl/core/stats/merge`'s real `LayoutMeta`. LDB-B123..B126 registered (renumbered after V0's B116..B122 landed first: golden parity, field completeness, atomicity, SPA-loader acceptance). `.dockerignore` gained a `scripts/*` allowlist (mirrors the existing `data/*` one) so the image ships only the 5 scripts either Python entrypoint imports. `cfg.overlayRowsPy` now threads into `PublisherConfig` via `main.ts`'s new `publisherConfigFrom` (the pure half of its `createPublisher({config: ...})` construction, unit-tested directly). Also fixed, at the coordinator's ask: `web/tests/core/stats-cache.vitest.ts`'s I-330 static scan asserted `bot/src/cache/provenance.ts` exists -- that module was deleted by slice B1 (retirement already documented in `bot/INVARIANTS.md` LDB-B64), so the assertion was a stale leftover on the integration branch; removed it (kept the `cells.ts` assertion and the actual no-wall-clock scan, both still valid), and repointed `design/INVARIANTS.md`'s I-330 entry off the now-nonexistent LDB-B63. Bot suite: 92 files (86 pass, 6 skipped) / 1,187 tests (1,114 pass, 73 skipped); `scripts/tests`: 425 pass; `web` vitest: 291/291 files green (6,688 pass), the pre-existing stats-cache failure now fixed; `gates.sh --fast`: all pass.
- 2026-09-12 · Publisher live: ticks 1–5 published (25 layouts each, 11–16 min per tick throttled), smoke ok, pointer db_seq 23968. Edit→visible during the backlog: 15 min 36 s (created 21:29:26Z, pointer 21:45:02Z). Backlog ≈ 4,100 layouts ≈ 33 h at the throttled rate; performance-1x would make it ≈ 2–3 h. Test layouts created for measurement are tombstoned.
- 2026-09-12 · **Fly shared-CPU throttling measured**: CPU probe 2.20 B iter/2 s right after boot vs 0.20 B after ~2 min of sustained CLI compute (11×); native compute 2 s → 22–30 s/layout. Machine decision handed to saltorbit (performance-1x recommended for backlog passes). Smoke test false negative (no Origin header → no CORS header from R2) fixed (b349e5849's sibling commit).
- 2026-09-12 · Publisher live-fix chain after B8: (1) `overlay_rows.py` path resolved to /scripts in the flattened dist → fixed + `OVERLAY_ROWS_PY` in fly.toml (aa128cd18); (2) pointer CAS failed 412 on an unchanged pointer: R2 served the pointer's ETag as `W/"…"` and the client stripped only quotes → `normalizeEtag` (real R2 conditional-PUT semantics verified with curl: quoted/unquoted If-Match 200, stale 412, If-None-Match:* on existing 412). Compute after B8: 1.6–3.5 s/layout.
- 2026-09-12 · Feed long-poll confirmed live after 42e2cf2b7 (no X-Wait-Ignored; wake p50 ≈ 1 s bucket, max 1.4 s over 6 events). Publisher first-pass throughput problem found (55 s/layout, publish only after the whole plan) → B8.
- 2026-09-12 · LATENCY.md round 1 written. Found via measurement: long-poll actor-before-login downgrade (fixed, BOT_USER_ID + retry), the measurement script's own two error-path artefacts (fixed), the old stats service still publishing (scaled to zero), the CLI wrapper not executable (fixed).
- 2026-09-12 · **spark WAVE 2 DEPLOYED** (attempt 3, 19:35Z, machine 7841609c5d3628): boot 3.6 s total (replica from dump 1.6 s, engine 0.3 s, ensureFresh 0.85 s, warm-up 0.78 s), ready 11 s after the restart, `/health` passing, publisher index loaded in 1.5 s (1,046 ids, 1.8 MB read — vs. the ~2 GB that caused incident 2), background sweep + publisher started after clientReady. Bot outage during the three attempts: 17:55–18:02 and 18:45–18:58.
- 2026-09-12 · B7 landed (8eac203bd); bot 1,133 green. Deploying spark, attempt 3.
- 2026-09-12 · **INCIDENT 2** (18:45–18:58Z): spark v34 (with B6) booted past replica+engine in 2 s and `/health` passed, but `publisher.start()` hung ~10 min on fetches to R2 (`publisher: start failed -- fetch failed`) and the following `ensureFresh()` never completed; rolled back to v32 again. CONFIRMED: `loadPublishedIndex` → `currentFullFiles` → `readFiles(base manifest)` downloads the whole base (≈2 GB, 553 objects) into memory just to read layout hashes; the in-machine network probe on the old image is healthy (akl-db 264 ms, R2 origin 308 ms, S3 endpoint 102 ms, akl.gg 145 ms). B7 dispatched: index load reads only manifests + layouts.json + rules.json; overlay publish touches only the tables it rewrites; fold streams one file at a time with server-side CopyObject; publisher start is background work with a bounded timeout and `degraded` state; every S3 request has a timeout.
- 2026-09-12 · B6 landed (5bbf8f62d); bot 1,125 green. Redeploying spark (second attempt).
- 2026-09-12 · B5 landed (eeae69346).
- 2026-09-12 · V0 landed (262d7d201); bot suite 1,100 green.
- 2026-09-12 · **INCIDENT**: spark deploy of wave 2 (v33) never logged in; rolled back to v32 after ~7 min. Two image-build fixes landed first (submodule `.git` pointer breaks `git apply`; `ca-certificates` for the bake curl). B6 dispatched. Lesson: watch `fly logs` for `ready as` within 60 s after every spark deploy; rollback = `flyctl machine update <id> --image <previous release image>`.
- 2026-09-12 · B3 landed (8ed066934). **Wave 2 complete**: bot suite 83 files / 1,063 passed / 0 failed in 19 s (live suite gated behind LIVE_TESTS). fly.toml set for the deploy (afa05096a). Next: deploy spark, then V1.
- 2026-09-12 · B2 landed (6131ed5f3). spark client granted `feed:wait` on prod (direct D1 update). R2 secrets staged on spark-bot by saltorbit/lead.
- 2026-09-12 · A1 landed (0acdc368b, gates --fast pass); B4 landed (7260dd2cc). Remaining in flight: B2, B3.
- 2026-09-12 · **Wave 1 DEPLOYED to prod akl-db** (Worker version 1fa2965c, migrations 0010–0013 applied; D1 bookmark taken first). Smoke: /v1/meta 200 with `health` (dump stale:true until the first catch-up tick), `?wait=` anonymous → `X-Wait-Ignored: unauthorized`, `/v1/changes/stream` 404 (live bot's keep-warm loops harmlessly until B3). `db.yml` pr-deploy now rides pushes to `ldb-arch-review` (91a81670e).
- 2026-09-12 · L1 landed (515088dc6). Wave 1 complete: db suite 15,810 green on ldb-arch-review.
- 2026-09-12 · L2 landed (850388595). Bot suite on the integration branch: 961 pass, 1 known red (LDB-B5 live parity, B3 gates it).
- 2026-09-12 · B1 landed (23435c60b); B2, B3, B4 launched in parallel. L1/L2 rebasing.
- 2026-09-12 · L3 landed (5b5a96ea0).
- 2026-09-12 · L4 landed (c6c003142). A1 launched (isolation worktree).
- 2026-09-12 · L1, L2, L3, B1 launched in parallel (isolation worktrees) alongside L4.
- 2026-09-12 · D1 (layoutdb architecture page) + D2 (ecosystem rewrite) landed; all four artifact pages measured in Chrome for SVG clipping and fixed.
- 2026-09-12 · review round 1 (audits, requirements, proposal, data-flow) landed; saltorbit answered §6; long-poll, Fly, rebuild cadence, safety nets decided; implementation kicked off with L4 first.
- 2026-09-12 · L4 in review (branch `ldb-L4`): webhooks+lease, SSE stream, Fly drill, per-major dump files, `[env.preview]` deleted (nothing else calls any of them; the bot's SSE keep-warm still targets the deleted stream until Wave 2 B3 switches it to the long-poll — do not deploy `db/` ahead of that, or coordinate first); the daily upstream diff shrunk to layout-count + a random 50-record sampled compare; `GET /v1/changes?wait=` (long-poll) added, gated on client cap `feed:wait` (`clients.caps` is now a comma-separated set), rate-limited, clamped to 25s. **Deferred, not deleted**: the multi-major format chain (`formats/registry.ts`'s `up`/`down`/`chainFn`/`chainViolations`/`path` composition, `core/write.ts`'s `chainToLatest`/`format_behind`/`written_as`, LDB-F18/F19/P13) — frozen in place per the audit's own offered alternative; full removal touches `put-format.test.ts` (LDB-P13's own suite), `held.test.ts`, `list.test.ts` and `docs-site.test.ts`'s stub-lineage T2/T3 fixtures, real multi-major coverage, not dead weight, and didn't fit this slice's budget. Suite green (88 files/15,756 tests/40 skipped, was 94/15,897/1); `bot/` typechecks clean against the rebuilt `db/formats` package.
- 2026-09-12 · A1 in review (branch `worktree-agent-a7c09faaaa6ce5163`, through `bb22333c6` + this ledger update): `npm run build` now runs `build_pointer.mjs` (pointer-only); the old bundled pipeline survives as `npm run build:bundled`, which every CI/local caller that actually needs it (gate.yml ×2, build.yml, full-rebuild.yml, gates.sh, dev_server.sh) was repointed to, so nothing's behavior changed today, only the default's name. `state/ownedOverlay.ts` (new) renders an owned-but-uncatalogued id from its own record with a private one-shot compute (never joins the DRAFTS shelf), wired from `state/ownedReconcile.ts`. `app/refreshPolling.ts`: a new 60s `POINTER_POLL_MS` interval (visible-gated) replaces the old 20-minute-piggybacked pointer/patch probe (the build-check poll itself is untouched); a `publish/promote`-triggered fast-confirm loop (`FAST_POLL_MS`=5s, capped at `FAST_POLL_MAX_ATTEMPTS`=60) force-polls until `isOwnedOverlayResolved`. `functions/_lib/dbproxy.mjs`: `UPSTREAM_TIMEOUT_MS` (10s, AbortController) → 504, `MAX_BODY_BYTES` (4 MiB) → 413, `dispatchAfterWrite`/`triggerDbSiteWrite`/`syncTargetFromEnv` deleted (confirmed zero consumers repo-wide, tripwired by `dispatchremoval.test.mjs`); `session.mjs` cookie gained `Secure`. I-371..I-376 registered. Full suite green (typecheck/lint/archlint/behaviors/vitest 6686/nodetest 484, both `--fast` and full `gates.sh`); Playwright not run (no `web/data` in the worktree). `db/` package untouched, its own suite skipped (no `npm ci` there) — orthogonal to this slice. `GITHUB_DISPATCH_TOKEN` still needs rotating by saltorbit.
