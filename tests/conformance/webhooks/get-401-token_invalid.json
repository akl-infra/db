{
  "request": { "method": "GET", "path": "/v1/webhooks", "bearer": "conformance-invalid-token" },
  "response": {
    "status": 401,
    "headers": { "Content-Type": "application/json", "WWW-Authenticate": "Bearer error=\"invalid_token\"" },
    "body": { "error": "token_invalid", "message": "the bearer token is invalid or expired" }
  }
}
