name = "akl-db"
main = "src/index.ts"
compatibility_date = "2026-08-22" # newest date the installed workerd/miniflare (wrangler ^4.130.0) supports as of this writing; bump when the toolchain does
compatibility_flags = ["nodejs_compat"]

# api.akldb.org (saltorbit, 2026-09-13: "can we change this to api.akldb.org") -- the
# public hostname for the API; the zone lives in the akl account beside the
# site's akldb.org/www.akldb.org (db/site/wrangler.toml). `workers_dev = true`
# keeps https://akl-db.akl-58a.workers.dev answering: every deployed client
# (spark's DB_BASE_URL on Fly, the site Worker's DB_BASE_URL var, akl.gg's
# pointer-mode config) still names it until each is repointed at a deploy of
# its own. A custom-domain route alone would silently disable that subdomain.
workers_dev = true

[[routes]]
pattern = "api.akldb.org"
custom_domain = true

[[d1_databases]]
binding = "DB"
database_name = "akl-db"
database_id = "53f596d5-9cb5-43b0-9026-17518d18052f" # account "akl" (58a5eb82…), created 2026-09-09

[[r2_buckets]]
binding = "DUMPS"
bucket_name = "akl-db-dumps"

[triggers]
crons = ["*/5 * * * *"] # ONE trigger for three jobs -- src/index.ts's scheduled() dispatches by event.scheduledTime, not event.cron

[vars]
IMPORT_SOURCE_URL = "https://clemenpine.com/layoutapi/v3"
IMPORT_MAX_WRITES_PER_TICK = "500"
IMPORT_UA = "akl-db-import/1.0"
DISCORD_API_URL = "https://discord.com/api"

[observability]
enabled = true   # Workers Logs: keep console.error() from onError (the 500 path) queryable in the dashboard; 2026-09-09, after two untraceable 500s

# There is one layoutdb, production (design/layout-db/review/PROPOSAL.md
# §2.1, LEDGER.md L4). The preview environment (`akl-db-preview`) was
# retired 2026-09-11 -- nothing read it and it accepted no clients; H13
# says one layoutdb. Do not re-add an `[env.preview]` block without a real
# consumer.

# LDB risk C.9 (audit-db.md §H item 9): `buildDump` materializes the whole
# DB in Worker memory before gzipping -- not urgent below ~50k events
# (today's ~4k layouts), but the default CPU budget is tighter than a
# larger dump would need. Raised on the Workers Paid plan already in use
# (`akl-db` bills $5/mo per PROPOSAL.md §3); `writeDump`'s own size log
# line is the signal for when this needs revisiting for real (streaming
# the gzip instead of materializing it).
[limits]
cpu_ms = 30000

# 2026-09-13 (LATENCY.md §14): a write PUT costs 640-760 ms and a changed-record GET 300-500 ms
# from Fly iad, vs /v1/meta at 65 ms -- per-D1-statement distance dominates. Smart Placement runs
# the Worker next to D1 for these D1-heavy requests. Reversible: delete this block.
# [placement] mode = "smart" was enabled 2026-09-13 06:27Z (D1 latency); REMOVED the same day:
# after two later deploys (versions e6f2b6bd, 64626574 at 100%) the production hostname kept
# serving the 06:27 version (5343a268) -- old schema, missing routes -- while each version's
# preview URL served the right code. Re-enable only with a verified deploy afterwards.
